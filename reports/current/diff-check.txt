OmniScout-preview.html:1463: trailing whitespace.
+let catalog = CATALOG;
OmniScout-preview.html:1465: trailing whitespace.
+const icons = {
OmniScout-preview.html:1466: trailing whitespace.
+ radar: '<circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="4"/><path d="M12 12l7-7M12 2v2M2 12h2M12 20v2M20 12h2"/>',
OmniScout-preview.html:1467: trailing whitespace.
+ board: '<rect x="3" y="4" width="18" height="16" rx="3"/><path d="M9 4v16M15 4v16M5 9h2M11 13h2M17 9h2"/>',
OmniScout-preview.html:1468: trailing whitespace.
+ bookmark: '<path d="M6 4h12v17l-6-4-6 4z"/>',
OmniScout-preview.html:1469: trailing whitespace.
+ globe: '<circle cx="12" cy="12" r="9"/><ellipse cx="12" cy="12" rx="4" ry="9"/><path d="M3 12h18M5 6h14M5 18h14"/>',
OmniScout-preview.html:1470: trailing whitespace.
+ sliders: '<path d="M4 6h16M4 12h16M4 18h16"/><circle cx="8" cy="6" r="2"/><circle cx="16" cy="12" r="2"/><circle cx="10" cy="18" r="2"/>',
OmniScout-preview.html:1471: trailing whitespace.
+ clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',
OmniScout-preview.html:1472: trailing whitespace.
+ search: '<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',
OmniScout-preview.html:1473: trailing whitespace.
+ arrow: '<path d="M5 12h14m-6-6 6 6-6 6"/>',
OmniScout-preview.html:1474: trailing whitespace.
+ close: '<path d="m6 6 12 12M6 18 18 6"/>',
OmniScout-preview.html:1475: trailing whitespace.
+ plus: '<path d="M12 5v14M5 12h14"/>',
OmniScout-preview.html:1476: trailing whitespace.
+ check: '<path d="m5 12 4 4L19 6"/>',
OmniScout-preview.html:1477: trailing whitespace.
+ export: '<path d="M12 3v12m-4-4 4 4 4-4M4 15v5h16v-5"/>',
OmniScout-preview.html:1478: trailing whitespace.
+ eye: '<path d="M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12z"/><circle cx="12" cy="12" r="3"/>',
OmniScout-preview.html:1479: trailing whitespace.
+ warning: '<path d="m12 3 10 18H2zM12 9v5M12 17v1"/>',
OmniScout-preview.html:1480: trailing whitespace.
+ compare: '<path d="M5 3v18M19 3v18M3 7h6M15 17h6M9 7l3 3M15 17l-3-3"/>',
OmniScout-preview.html:1481: trailing whitespace.
+ file: '<path d="M5 3h10l4 4v14H5zM14 3v5h5M8 12h8M8 16h8"/>'
OmniScout-preview.html:1482: trailing whitespace.
+};
OmniScout-preview.html:1483: trailing whitespace.
+const icon = (name, cls = '') => `<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name] || icons.file}</svg>`;
OmniScout-preview.html:1484: trailing whitespace.
+const esc = value => String(value ?? '').replace(/[&<>"']/g, x => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[x]));
OmniScout-preview.html:1485: trailing whitespace.
+const fmt = (value, decimals = 1) => value == null || !Number.isFinite(value) ? 'Onbekend' : value.toLocaleString('nl-NL', { maximumFractionDigits: decimals });
OmniScout-preview.html:1486: trailing whitespace.
+const initials = name => name.split(' ').filter(Boolean).slice(0, 2).map(x => x[0]).join('');
OmniScout-preview.html:1487: trailing whitespace.
+const dates = value => value ? new Date(value).toLocaleDateString('nl-NL', { day: 'numeric', month: 'short' }) : 'Onbekend';
OmniScout-preview.html:1488: trailing whitespace.
+const newId = () => globalThis.crypto?.randomUUID?.() || `demo-${Date.now()}-${Math.random().toString(16).slice(2)}`;
OmniScout-preview.html:1489: trailing whitespace.
+const blankState = () => ({ version: 1, decisions: [], tasks: [], brief: { role: '', minAge: 18, maxAge: 23, task: '', budgetScope: '' }, audit: [] });
OmniScout-preview.html:1490: trailing whitespace.
+const storageKey = 'omniscout.synthetic-demo.v1';
OmniScout-preview.html:1491: trailing whitespace.
+const datasetKey = 'omniscout.dataset.v1';
OmniScout-preview.html:1492: trailing whitespace.
+let state = blankState(), backend = null, storageWorking = true, dataset = 'demo', preferredDataset = 'demo', datasetLoading = true, mutationCount = 0, workspaceError = '';
OmniScout-preview.html:1493: trailing whitespace.
+try { const saved = JSON.parse(localStorage.getItem(storageKey) || 'null'); if (saved?.version === 1 && ['decisions', 'tasks', 'audit'].every(k => Array.isArray(saved[k]))) state = saved; } catch { storageWorking = false; }
OmniScout-preview.html:1494: trailing whitespace.
+try { if (localStorage.getItem(datasetKey) === 'import') preferredDataset = 'import'; } catch { /* Dataset preference is optional. */ }
OmniScout-preview.html:1495: trailing whitespace.
+let demoState = state;
OmniScout-preview.html:1496: trailing whitespace.
+const importUI = { payload: null, preview: null, filename: '', error: '', busy: false, sequence: 0, jobs: [], snapshots: [], jobsError: '', jobsLoading: false, rolledBack: new Set() };
OmniScout-preview.html:1497: trailing whitespace.
+let jobsTimer;
OmniScout-preview.html:1498: trailing whitespace.
+let view = 'radar', filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 23, newOnly: false, lowerOnly: false };
OmniScout-preview.html:1499: trailing whitespace.
+let comparison = new Set(), openPlayerId = null;
OmniScout-preview.html:1500: trailing whitespace.
+const main = document.querySelector('#main'), dossierDialog = document.querySelector('#dossier-dialog'), actionDialog = document.querySelector('#action-dialog');
OmniScout-preview.html:1501: trailing whitespace.
+const viewLabels = { radar: 'Wereldradar', tasks: 'Onderzoeksbord', shortlist: 'Shortlist', coverage: 'Datadekking', import: 'Bronimport', brief: 'Clubvraag', log: 'Beslislogboek' };
OmniScout-preview.html:1502: trailing whitespace.
+let toastTimeout;
OmniScout-preview.html:1503: trailing whitespace.
+function notify(message, error = false) { const el = document.querySelector('#toast'); el.textContent = message; el.className = `toast show ${error ? 'error' : ''}`; clearTimeout(toastTimeout); toastTimeout = setTimeout(() => el.classList.remove('show'), 4500); }
OmniScout-preview.html:1504: trailing whitespace.
+function persist() { if (dataset !== 'demo') throw new Error('Importgegevens worden uitsluitend in de lokale backend opgeslagen.'); demoState = state; try { localStorage.setItem(storageKey, JSON.stringify(state)); } catch { storageWorking = false; notify('Browseropslag niet beschikbaar. Wijzigingen blijven alleen in deze tab.', true); } }
OmniScout-preview.html:1505: trailing whitespace.
+function addAudit(action, objectId, detail = '') { state.audit.push({ id: newId(), action, objectId, detail, at: new Date().toISOString(), actor: 'browser-demo-user' }); }
OmniScout-preview.html:1506: trailing whitespace.
+const scopedPath = (path, selected = dataset) => `${path}${path.includes('?') ? '&' : '?'}dataset=${encodeURIComponent(selected)}`;
OmniScout-preview.html:1507: trailing whitespace.
+const canImport = () => Boolean(backend?.supportsImport && !window.OMNI_INLINE);
OmniScout-preview.html:1508: trailing whitespace.
+const sourceLabel = source => source?.status === 'synthetic' ? 'Synthetische testimport' : source?.status === 'approved' ? 'Importverklaring · niet onafhankelijk geverifieerd' : 'Bronstatus ontbreekt of is ongeldig';
OmniScout-preview.html:1509: trailing whitespace.
+const playerLabel = player => dataset === 'demo' ? 'Fictief profiel' : player?.synthetic ? 'Synthetische testimport' : 'Importverklaring · niet onafhankelijk geverifieerd';
OmniScout-preview.html:1510: trailing whitespace.
+function datasetLabel() { return dataset === 'demo' ? 'Fictieve testgegevens' : !catalog.players.length ? 'Lokale import · geen beschikbare profielen' : catalog.players.every(p => p.synthetic) ? 'Synthetische testimport' : 'Importgegevens · verklaring niet onafhankelijk geverifieerd'; }
OmniScout-preview.html:1511: trailing whitespace.
+async function readJSON(path) {
OmniScout-preview.html:1512: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
OmniScout-preview.html:1513: trailing whitespace.
+  const result = await response.json();
OmniScout-preview.html:1514: trailing whitespace.
+  if (!response.ok) throw new Error(result.error || 'Lokale gegevens ophalen mislukt.');
OmniScout-preview.html:1515: trailing whitespace.
+  return result;
OmniScout-preview.html:1516: trailing whitespace.
+}
OmniScout-preview.html:1517: trailing whitespace.
+async function refreshWorkspace(selected = dataset) {
OmniScout-preview.html:1518: trailing whitespace.
+  const result = await readJSON(scopedPath('/api/state', selected));
OmniScout-preview.html:1519: trailing whitespace.
+  if (dataset === selected) state = result;
OmniScout-preview.html:1520: trailing whitespace.
+}
OmniScout-preview.html:1521: trailing whitespace.
+async function request(path, method, data, { refresh = !path.startsWith('/api/import/') } = {}) {
OmniScout-preview.html:1522: trailing whitespace.
+  if (!backend) throw new Error('Deze actie vereist de lokale Node-backend.');
OmniScout-preview.html:1523: trailing whitespace.
+  const selected = dataset, workspaceMutation = !path.startsWith('/api/import/');
OmniScout-preview.html:1524: trailing whitespace.
+  mutationCount++; updateCounts();
OmniScout-preview.html:1525: trailing whitespace.
+  try {
OmniScout-preview.html:1526: trailing whitespace.
+    const r = await fetch(workspaceMutation ? scopedPath(path, selected) : path, { method, headers: { 'Content-Type': 'application/json', 'X-Omniscout-CSRF': backend.csrf }, body: JSON.stringify(workspaceMutation ? { ...data, dataset: selected } : data) });
OmniScout-preview.html:1527: trailing whitespace.
+    const result = await r.json();
OmniScout-preview.html:1528: trailing whitespace.
+    if (!r.ok) { const error = new Error(result.error || 'Opslaan mislukt.'); error.details = result.details; throw error; }
OmniScout-preview.html:1529: trailing whitespace.
+    if (refresh) await refreshWorkspace(selected);
OmniScout-preview.html:1530: trailing whitespace.
+    return result;
OmniScout-preview.html:1531: trailing whitespace.
+  } finally { mutationCount--; updateCounts(); }
OmniScout-preview.html:1532: trailing whitespace.
+}
OmniScout-preview.html:1533: trailing whitespace.
+async function selectDataset(selected, { remember = true } = {}) {
OmniScout-preview.html:1534: trailing whitespace.
+  if (!['demo', 'import'].includes(selected)) return;
OmniScout-preview.html:1535: trailing whitespace.
+  if (selected === 'import' && !canImport()) throw new Error('Lokale import vereist de Node-app. De zelfstandige en edge-demo tonen alleen fictieve gegevens.');
OmniScout-preview.html:1536: trailing whitespace.
+  datasetLoading = true; updateCounts();
OmniScout-preview.html:1537: trailing whitespace.
+  try {
OmniScout-preview.html:1538: trailing whitespace.
+    const [nextCatalog, nextState] = backend ? await Promise.all([readJSON(scopedPath('/api/catalog', selected)), readJSON(scopedPath('/api/state', selected))]) : [CATALOG, demoState];
OmniScout-preview.html:1539: trailing whitespace.
+    dataset = selected; catalog = nextCatalog; state = nextState; workspaceError = '';
OmniScout-preview.html:1540: trailing whitespace.
+    comparison.clear(); filters = { ...filters, search: '', competition: '', region: '', quality: '' };
OmniScout-preview.html:1541: trailing whitespace.
+    dossierDialog.close(); actionDialog.close(); openPlayerId = null;
OmniScout-preview.html:1542: trailing whitespace.
+    if (remember) { preferredDataset = selected; try { localStorage.setItem(datasetKey, selected); } catch { /* Never cache imported data. */ } }
OmniScout-preview.html:1543: trailing whitespace.
+    render();
OmniScout-preview.html:1544: trailing whitespace.
+  } finally { datasetLoading = false; updateCounts(); }
OmniScout-preview.html:1545: trailing whitespace.
+}
OmniScout-preview.html:1546: trailing whitespace.
+async function saveDecision(playerId, action, reason, note = '') {
OmniScout-preview.html:1547: trailing whitespace.
+  if (backend) await request('/api/decisions', 'POST', { playerId, action, reason, note });
OmniScout-preview.html:1548: trailing whitespace.
+  else { const id = newId(); state.decisions.push({ id, playerId, action, reason, note, at: new Date().toISOString() }); addAudit('decision.created', id, `${playerId}: ${action} (${reason})`); persist(); }
OmniScout-preview.html:1549: trailing whitespace.
+  updateCounts();
OmniScout-preview.html:1550: trailing whitespace.
+}
OmniScout-preview.html:1551: trailing whitespace.
+function latestDecision(id) { return state.decisions.filter(x => x.playerId === id).at(-1); }
OmniScout-preview.html:1552: trailing whitespace.
+function isShortlisted(id) { return latestDecision(id)?.action === 'follow'; }
OmniScout-preview.html:1553: trailing whitespace.
+function updateCounts() {
OmniScout-preview.html:1554: trailing whitespace.
+  document.querySelector('#task-count').textContent = state.tasks.filter(t => t.status === 'todo').length;
OmniScout-preview.html:1555: trailing whitespace.
+  document.querySelector('#shortlist-count').textContent = catalog.players.filter(p => isShortlisted(p.id)).length;
OmniScout-preview.html:1556: trailing whitespace.
+  document.querySelector('#player-count').textContent = catalog.players.length;
OmniScout-preview.html:1557: trailing whitespace.
+  document.querySelector('#storage-status').textContent = workspaceError ? 'Backendgegevens niet geladen' : backend ? backend.persistence === 'memory' ? 'Node-backend · tijdelijk geheugen' : 'Lokaal opgeslagen op dit apparaat' : storageWorking ? 'Preview · browseropslag' : 'Preview · tijdelijk geheugen';
OmniScout-preview.html:1558: trailing whitespace.
+  const picker = document.querySelector('#dataset-select');
OmniScout-preview.html:1559: trailing whitespace.
+  picker.value = dataset; picker.disabled = datasetLoading || mutationCount > 0;
OmniScout-preview.html:1560: trailing whitespace.
+  picker.querySelector('[value="import"]').disabled = !canImport();
OmniScout-preview.html:1561: trailing whitespace.
+  document.querySelector('#dataset-status').textContent = datasetLoading ? 'Gegevens ophalen…' : workspaceError ? 'Backendgegevens konden niet worden geladen. Kies de gegevensset opnieuw om te proberen.' : dataset === 'import' ? 'Importcatalogus en onderzoek blijven in de lokale backend.' : canImport() ? 'Demo en import hebben een afzonderlijk onderzoekslogboek.' : 'Alleen fictieve demo · import vereist de lokale Node-app.';
OmniScout-preview.html:1562: trailing whitespace.
+  document.querySelector('.date-label').textContent = `PEILDATUM ${dates(catalog.asOf)}`;
OmniScout-preview.html:1563: trailing whitespace.
+  document.querySelector('.demo-chip').textContent = dataset === 'demo' ? 'DEMO' : 'IMPORT';
OmniScout-preview.html:1564: trailing whitespace.
+  document.querySelector('.demo-banner p').innerHTML = dataset === 'demo' ? '<strong>Fictieve testomgeving.</strong> Alle spelers, clubs, competities en statistieken zijn voorbeelden. <strong>0 live databronnen.</strong>' : `<strong>${esc(datasetLabel())}.</strong> Bronrechten en inhoud zijn verklaringen van de importeur. Geen live providerverbinding of onafhankelijk geverifieerde scoutingdekking.`;
OmniScout-preview.html:1565: trailing whitespace.
+}
OmniScout-preview.html:1566: trailing whitespace.
+function heading(kicker, title, description, actions = '') { return `<div class="page-heading"><div><div class="eyebrow">${esc(kicker)}</div><h1>${esc(title)}</h1><p>${esc(description)}</p></div><div class="heading-actions">${actions}</div></div>`; }
OmniScout-preview.html:1567: trailing whitespace.
+function empty(title, detail) { return `<div class="empty-state">${icon('radar')}<h3>${esc(title)}</h3><p>${esc(detail)}</p><button class="button secondary" data-action="reset">Toon alle spelers in deze set</button></div>`; }
OmniScout-preview.html:1568: trailing whitespace.
+function options(obj, current, all) { return `<option value="">${esc(all)}</option>` + Object.entries(obj).map(([key, label]) => `<option value="${esc(key)}" ${String(current) === key ? 'selected' : ''}>${esc(label)}</option>`).join(''); }
OmniScout-preview.html:1569: trailing whitespace.
+function qualityTag(d) { return `<span class="status-tag ${d.quality === 'documented' ? 'green' : 'amber'}"><i></i>${d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'}</span>`; }
OmniScout-preview.html:1570: trailing whitespace.
+function metric(d) { return d.roleMetric ? `${fmt(d.metrics[d.roleMetric.metric])}${d.metrics[d.roleMetric.metric] == null ? '' : ' ' + d.roleMetric.unit}` : 'Onbekend'; }
OmniScout-preview.html:1571: trailing whitespace.
+function worldMap() {
OmniScout-preview.html:1572: trailing whitespace.
+  const regions = [ ['Noord-Amerika', 151, 112, 'NOORD-AMERIKA'], ['Zuid-Amerika', 250, 208, 'ZUID-AMERIKA'], ['Europa', 385, 76, 'EUROPA'], ['Afrika', 369, 160, 'AFRIKA'], ['Azië', 558, 98, 'AZIË'], ['Oceanië', 603, 226, 'OCEANIË'] ];
OmniScout-preview.html:1573: trailing whitespace.
+  return `<div class="world-card panel"><div class="panel-head"><div><span class="eyebrow">WERELDWIJD ONTDEKKEN</span><h2>Niet elke kans staat al op de radar.</h2></div><span class="status-tag neutral">${dataset === 'demo' ? 'SCHEMA + TESTDATA' : 'LOKALE IMPORT'}</span></div>
OmniScout-preview.html:1574: trailing whitespace.
+   <div class="map-wrap"><svg viewBox="0 0 720 285" class="world-map" role="img" aria-label="Schematisch overzicht van de geselecteerde dataset. Geen kaart van live scoutingdekking."><defs><pattern id="grid" width="36" height="28" patternUnits="userSpaceOnUse"><path d="M36 0H0V28" fill="none" stroke="#274357" stroke-width=".5"/></pattern><pattern id="dots" width="5" height="5" patternUnits="userSpaceOnUse"><circle cx="1" cy="1" r=".9" fill="#36526a"/></pattern><radialGradient id="glow"><stop stop-color="#a6f27a" stop-opacity=".13"/><stop offset="1" stop-color="#a6f27a" stop-opacity="0"/></radialGradient></defs><rect width="720" height="285" fill="url(#grid)" opacity=".5"/><ellipse cx="385" cy="140" rx="190" ry="140" fill="url(#glow)"/>
OmniScout-preview.html:1575: trailing whitespace.
+    <g fill="url(#dots)" stroke="#3e596c" stroke-width=".65" opacity=".85"><path d="M48 70l20-23 44-9 23-17 53 9 35 25-21 22-16 29-28 2-9 26-24 2 2 22 32 20 21 1 12 15-19 7-19-12-22-8-24-26-22-25-28-13z"/><path d="m244 193 28-14 28 15 12 23-17 29-11 30-14-11-9-34-16-18z"/><path d="m248 17 35-9 23 12-14 26-32 3-14-13z"/><path d="m337 70 21-20 12 9 15-16 14 9 13-23 16 16-1 24-29 6-14 15-9 18-34-6 6-17z"/><path d="m340 115 20-17 40 9 27 24 5 25-28 24-9 30-18-4-12-29-27-14-12-28z"/><path d="m418 43 36-11 26 9 18-16 36 9 45-1 27 17 27-4 30 22-23 18-29 5-8 17-31-3-3 28-26 21-14-12-13 14-19-6-14-29-30-12-13-15-22-3-22-14z"/><path d="m545 151 12 8 14 17 27 8 8 11-28-2-28-20z"/><path d="m579 209 26-13 33 8 8 24-15 21-30-5-27-15z"/><path d="m665 246 12-11 3 15-15 19z"/><path d="m409 194 7-5 3 16-8 18-5-12z"/></g>
OmniScout-preview.html:1576: trailing whitespace.
+    <g stroke="#94d675" stroke-opacity=".2" fill="none" stroke-dasharray="3 7"><path d="M385 76Q223 3 151 112M385 76Q291 139 250 208M385 76Q390 102 369 160M385 76Q462 21 558 98M385 76Q553 150 603 226"/></g>
OmniScout-preview.html:1577: trailing whitespace.
+    ${regions.map(([name, x, y, label]) => { const count = catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length; return `<g><circle cx="${x}" cy="${y}" r="14" fill="${count ? '#aff57d' : '#64798b'}" opacity=".1"/><circle cx="${x}" cy="${y}" r="5" fill="${count ? '#b8f384' : '#64798b'}"/><text x="${x + 13}" y="${y - 2}" class="map-label">${label}</text><text x="${x + 13}" y="${y + 13}" class="map-value">${count ? `${count} spelers` : 'Niet aangesloten'}</text></g>`; }).join('')}
OmniScout-preview.html:1578: trailing whitespace.
+   </svg></div><div class="region-pills">${regions.map(([name]) => `<button data-region="${esc(name)}">${esc(name)}<b>${catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length}</b></button>`).join('')}</div><div class="map-footer"><span><i class="legend-dot"></i>${catalog.competitions.filter(c => catalog.players.some(p => p.competitionId === c.id)).length} competities met spelers</span><span><i class="legend-dot muted"></i>${catalog.competitions.filter(c => !catalog.players.some(p => p.competitionId === c.id)).length} zonder spelersdata</span><button class="text-button" data-view="coverage">Alle dekking ${icon('arrow')}</button></div></div>`;
OmniScout-preview.html:1579: trailing whitespace.
+}
OmniScout-preview.html:1580: trailing whitespace.
+function tableRows(dossiers) {
OmniScout-preview.html:1581: trailing whitespace.
+  return dossiers.map((d, index) => `<tr><td class="check-cell"><input type="checkbox" aria-label="Vergelijk ${esc(d.player.name)}" data-compare="${esc(d.player.id)}" ${comparison.has(d.player.id) ? 'checked' : ''}></td><td><button class="player-link" data-open="${esc(d.player.id)}"><span class="player-avatar variant-${index % 4}">${esc(initials(d.player.name))}</span><span><strong>${esc(d.player.name)}</strong><small>${esc(d.player.club)}</small>${dataset === 'import' ? `<small>${esc(playerLabel(d.player))}</small>` : ''}<small class="mobile-player-meta">${esc(d.player.role)} · ${d.age} jaar · ${esc(d.competition.country)} · niveau ${d.competition.tier}</small></span></button></td><td><span class="role-label">${esc(d.player.role)}</span><small>${d.age} jaar · ${esc(d.player.preferredFoot)}</small></td><td><strong class="cell-normal">${esc(d.competition.country)}</strong><small>Niveau ${esc(d.competition.tier)} · ${esc(d.competition.name)}</small></td><td><strong class="numeric">${esc(metric(d))}</strong><small>${esc(d.roleMetric?.label || 'Rolgegevens')}</small></td><td><strong class="cell-normal">${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></td><td>${qualityTag(d)}<small>${d.minutesChange !== null && d.minutesChange >= 120 ? `+${d.minutesChange} min · gelijke vensters` : d.quality === 'exploration' ? 'Aanvullend onderzoek nodig' : 'Geen toekomstvoorspelling'}</small></td><td><button class="icon-button ${isShortlisted(d.player.id) ? 'selected' : ''}" data-save="${esc(d.player.id)}" aria-label="${isShortlisted(d.player.id) ? 'Verwijder van shortlist' : 'Zet op shortlist'}: ${esc(d.player.name)}">${icon('bookmark')}</button><button class="icon-button" data-open="${esc(d.player.id)}" aria-label="Open dossier ${esc(d.player.name)}">${icon('arrow')}</button></td></tr>`).join('');
OmniScout-preview.html:1582: trailing whitespace.
+}
OmniScout-preview.html:1583: trailing whitespace.
+function playerTable(dossiers) {
OmniScout-preview.html:1584: trailing whitespace.
+  return dossiers.length ? `<div class="table-scroll"><table class="player-table"><thead><tr><th aria-label="Vergelijken"></th><th>SPELER / CLUB</th><th>ROL / LEEFTIJD</th><th>COMPETITIE</th><th>ROLSIGNAAL</th><th>MINUTEN</th><th>BEWIJSROUTE</th><th aria-label="Acties"></th></tr></thead><tbody>${tableRows(dossiers)}</tbody></table></div>` : empty('Geen kandidaten binnen deze filters', 'Dit resultaat geldt alleen voor de geselecteerde gegevensset. Het zegt niets over talent in niet-aangesloten competities.');
OmniScout-preview.html:1585: trailing whitespace.
+}
OmniScout-preview.html:1586: trailing whitespace.
+function renderRadar(shortlist = false) {
OmniScout-preview.html:1587: trailing whitespace.
+  const all = findPlayers(catalog, {}, state), documented = all.filter(d => d.quality === 'documented').length;
OmniScout-preview.html:1588: trailing whitespace.
+  const current = findPlayers(catalog, { ...filters, shortlistOnly: shortlist }, state);
OmniScout-preview.html:1589: trailing whitespace.
+  const regions = Object.fromEntries([...new Set(catalog.competitions.map(c => c.region))].map(x => [x, x]));
OmniScout-preview.html:1590: trailing whitespace.
+  const compOptions = Object.fromEntries(catalog.competitions.map(c => [c.id, `${c.country} · ${c.name}`]));
OmniScout-preview.html:1591: trailing whitespace.
+  main.innerHTML = heading(shortlist ? 'JOUW SCOUTINGSELECTIE' : 'VERBORGEN TALENT · ZICHTBAAR BEWIJS', shortlist ? 'Jouw shortlist.' : 'Zie wat anderen missen.', shortlist ? 'Opgeslagen onderzoekskandidaten. Een shortlist is geen contractbeslissing.' : 'Een wereld aan mogelijkheden. Eén werkplek voor gericht scoutingonderzoek.', `<button class="button secondary" data-action="export">${icon('export')} Export selectie</button><button class="button primary" data-view="brief">${icon('sliders')} Clubvraag instellen</button>`) +
OmniScout-preview.html:1592: trailing whitespace.
+  (!shortlist ? `<div class="kpi-grid"><div class="kpi-card"><span class="kpi-label">${dataset === 'demo' ? 'SPELERS IN DE TESTSET' : 'BESCHIKBARE IMPORTSPELERS'} ${icon('eye')}</span><div class="kpi-number">${all.length}<span>${esc(datasetLabel())}</span></div><p>${catalog.competitions.length} competities in deze gegevensset</p></div><div class="kpi-card"><span class="kpi-label">MEER ONDERLIGGEND BEWIJS ${icon('file')}</span><div class="kpi-number">${documented}<span class="green-text">onderzoekskandidaten</span></div><p>Voldoende minuten voor de voorbeeldregel</p></div><div class="kpi-card"><span class="kpi-label">VERKENNINGSROUTE ${icon('radar')}</span><div class="kpi-number">${all.length - documented}<span class="amber-text">nog nader te bekijken</span></div><p>Weinig data betekent niet weinig talent</p></div></div><div class="radar-top-grid">${worldMap()}<div class="focus-card panel"><div class="eyebrow">ONDERZOEK, GEEN GOKWERK</div><div class="focus-orbit">${icon('radar')}</div><h2>De beste volgende<br>vraag.</h2><p>Geen onverklaarbare talentscore. Wel de bron, het tegenbewijs en wat je nu moet onderzoeken.</p><div class="focus-steps"><span><b>01</b>Ontdek een relevant signaal</span><span><b>02</b>Controleer wat nog ontbreekt</span><span><b>03</b>Geef de scout een gerichte taak</span></div><button class="text-button" data-view="tasks">Open onderzoeksbord ${icon('arrow')}</button></div></div>` : '') +
OmniScout-preview.html:1593: trailing whitespace.
+  `<section class="panel candidates"><div class="candidates-top"><div><h2>${shortlist ? 'Opgeslagen spelers' : 'Ontdekkingsradar'} <span class="count-badge">${current.length}</span></h2><p>Gesorteerd op beschikbare informatie. Niet op voorspeld talent.</p></div><button class="button secondary compact" id="compare-button" data-action="compare" ${comparison.size < 2 ? 'disabled' : ''}>${icon('compare')} Vergelijk <span>${comparison.size}/3</span></button></div>
OmniScout-preview.html:1594: trailing whitespace.
+    <div class="filters"><label class="search-input">${icon('search')}<input id="search" data-filter="search" type="search" value="${esc(filters.search)}" placeholder="Zoek speler, club of land" aria-label="Zoek speler, club of land"></label><label class="select-filter"><span class="sr-only">Positie</span><select data-filter="role" aria-label="Positie">${options(ROLE_LABELS, filters.role, 'Alle posities')}</select></label><label class="select-filter"><span class="sr-only">Regio</span><select data-filter="region" aria-label="Regio">${options(regions, filters.region, 'Alle regio’s')}</select></label><label class="age-filter"><span>Leeftijd</span><input type="number" min="18" max="60" data-filter="minAge" aria-label="Minimumleeftijd" value="${esc(filters.minAge)}"><span>–</span><input type="number" min="18" max="60" data-filter="maxAge" aria-label="Maximumleeftijd" value="${esc(filters.maxAge)}"></label></div>
OmniScout-preview.html:1595: trailing whitespace.
+    <div class="filter-secondary"><div class="segment-control"><button data-quality="" class="${filters.quality === '' ? 'active' : ''}">Alle kandidaten</button><button data-quality="documented" class="${filters.quality === 'documented' ? 'active' : ''}">Meer bewijs</button><button data-quality="exploration" class="${filters.quality === 'exploration' ? 'active' : ''}">Verkenning</button></div><div class="quick-filters"><label><input type="checkbox" data-filter="lowerOnly" ${filters.lowerOnly ? 'checked' : ''}> Niveau 3 en lager</label><label><input type="checkbox" data-filter="newOnly" ${filters.newOnly ? 'checked' : ''}> Nieuw voor onze club</label></div></div>
OmniScout-preview.html:1596: trailing whitespace.
+    ${filters.competition ? `<div class="active-filter">Competitie: ${esc(compOptions[filters.competition])}<button data-action="clear-competition">${icon('close')} Wissen</button></div>` : ''}
OmniScout-preview.html:1597: trailing whitespace.
+    ${playerTable(current)}<div class="table-foot"><span><i class="legend-dot"></i>${esc(datasetLabel())} · Statistieken zijn niet tussen competities genormaliseerd</span><span>Peildatum ${dates(catalog.asOf)}</span></div></section>`;
OmniScout-preview.html:1598: trailing whitespace.
+}
OmniScout-preview.html:1599: trailing whitespace.
+function renderCoverage() {
OmniScout-preview.html:1600: trailing whitespace.
+  main.innerHTML = heading('TRANSPARANT OVER WAT WE WETEN', 'Dekking, zonder blinde claims.', 'Een competitienaam in de database is geen volledige spelersanalyse.') + `<div class="notice-card">${icon('warning')}<div><strong>${esc(datasetLabel())}.</strong><p>${dataset === 'demo' ? 'Onderstaande statussen demonstreren het dekkingsregister met fictieve competities. 0 echte databronnen aangesloten.' : 'Onderstaande bronrechten en dekkingsvelden zijn aangeleverd door de importeur; ze zijn niet onafhankelijk geverifieerd. Geen live providerverbinding.'}</p></div></div><section class="panel"><div class="panel-head"><div><span class="eyebrow">DEKKINGSREGISTER</span><h2>Per bron. Per competitie. Per gegevenstype.</h2></div></div><div class="table-scroll"><table class="coverage-table"><thead><tr><th>COMPETITIE</th><th>NIVEAU</th>${COVERAGE_FIELDS.map(k => `<th>${esc(COVERAGE_LABELS[k])}</th>`).join('')}<th>ACTUALITEIT</th></tr></thead><tbody>${catalog.competitions.map(c => `<tr><td><button class="coverage-link" data-competition="${esc(c.id)}">${esc(c.country)} ${icon('arrow')}</button><small>${esc(c.name)} · ${esc(c.season || 'Seizoen onbekend')} · ${esc(dataset === 'demo' ? 'fictief' : sourceLabel(catalog.sources.find(s => s.id === c.sourceId)))}</small></td><td>${esc(c.tier)}</td>${COVERAGE_FIELDS.map(k => `<td><span class="coverage-dot ${esc(c.coverage[k])}" title="${esc(COVERAGE_STATES[c.coverage[k]])}" aria-label="${esc(COVERAGE_LABELS[k] + ': ' + COVERAGE_STATES[c.coverage[k]])}"><span class="sr-only">${esc(COVERAGE_STATES[c.coverage[k]])}</span>${c.coverage[k] === 'available' ? '✓' : c.coverage[k] === 'partial' ? '◐' : '—'}</span></td>`).join('')}<td>${dates(c.lastReceivedAt)}<small>${fmt(c.observedPlayers, 0)} spelers</small></td></tr>`).join('')}</tbody></table></div><div class="coverage-legend">${Object.entries(COVERAGE_STATES).map(([k, v]) => `<span><i class="coverage-dot ${k}"></i>${esc(v)}</span>`).join('')}</div></section><div class="info-grid"><div class="panel info-panel"><h3>Onbekend blijft onbekend</h3><p>Geen minuten betekent geen berekening per 90 minuten. Geen volledige noemer betekent geen dekkingspercentage. Een niet-aangesloten competitie blijft zichtbaar als een gat.</p></div><div class="panel info-panel"><h3>Bronrechten vóór verwerking</h3><p>Voor bronnen zijn rechten nodig voor ophalen, bewaren, weergeven, analyse en export. De importeur verklaart deze rechten; de toepassing controleert velden en vervaldatums, geen externe overeenkomsten. Er worden geen externe verbindingen gelegd.</p></div></div>`;
OmniScout-preview.html:1601: trailing whitespace.
+}
OmniScout-preview.html:1602: trailing whitespace.
+function taskCard(task) {
OmniScout-preview.html:1603: trailing whitespace.
+  const p = catalog.players.find(p => p.id === task.playerId);
OmniScout-preview.html:1604: trailing whitespace.
+  return `<article class="task-card"><div class="task-card-head"><span class="status-tag ${task.status === 'done' ? 'green' : 'amber'}">${task.status === 'done' ? 'Afgerond' : 'Te onderzoeken'}</span><span>${dates(task.at)}</span></div><button class="task-player" data-open="${esc(task.playerId)}">${esc(p?.name || task.playerId)} ${icon('arrow')}</button><p>${esc(task.question)}</p>${task.result ? `<div class="task-result"><strong>Waargenomen uitkomst</strong><p>${esc(task.result)}</p></div>` : ''}<div class="task-card-bottom"><span>Interne opdracht · niet verstuurd</span><button class="button secondary compact" data-task="${esc(task.id)}">${task.status === 'done' ? 'Heropenen' : 'Uitkomst vastleggen'}</button></div></article>`;
OmniScout-preview.html:1605: trailing whitespace.
+}
OmniScout-preview.html:1606: trailing whitespace.
+function renderTasks() {
OmniScout-preview.html:1607: trailing whitespace.
+  const todo = state.tasks.filter(t => t.status !== 'done'), done = state.tasks.filter(t => t.status === 'done');
OmniScout-preview.html:1608: trailing whitespace.
+  main.innerHTML = heading('VAN SIGNAAL NAAR WAARNEMING', 'Onderzoek met een doel.', 'Leg eerst de vraag vast. Beoordeel daarna wat het nieuwe bewijs werkelijk verandert.', `<button class="button primary" data-view="radar">${icon('plus')} Kies een kandidaat</button>`) + `<div class="board"><section class="board-column"><div class="board-title"><span class="legend-dot amber-dot"></span><h2>Te onderzoeken</h2><span class="count-badge">${todo.length}</span></div>${todo.length ? todo.map(taskCard).join('') : '<div class="board-empty">Nog geen opdrachten. Open een spelersdossier en kies “Maak onderzoeksopdracht”.</div>'}</section><section class="board-column"><div class="board-title"><span class="legend-dot"></span><h2>Uitkomst vastgelegd</h2><span class="count-badge">${done.length}</span></div>${done.length ? done.map(taskCard).join('') : '<div class="board-empty">Een afgeronde opdracht krijgt een geschreven uitkomst. Een klik alleen is geen bewijs.</div>'}</section></div>`;
OmniScout-preview.html:1609: trailing whitespace.
+}
OmniScout-preview.html:1610: trailing whitespace.
+function renderBrief() {
OmniScout-preview.html:1611: trailing whitespace.
+  const b = state.brief;
OmniScout-preview.html:1612: trailing whitespace.
+  main.innerHTML = heading('BEGIN BIJ DE CLUBVRAAG', 'Welke rol ontbreekt?', 'Benoem taken en grenzen. De scout bepaalt hoe de informatie wordt gewogen.') + `<div class="brief-grid"><form id="brief-form" class="panel form-panel"><div class="panel-head"><h2>Recruitmentprofiel</h2><span class="status-tag neutral">${dataset === 'demo' ? 'LOKALE DEMO' : 'LOKALE IMPORT'}</span></div><label>Gezochte positie<select name="role">${options(ROLE_LABELS, b.role, 'Nog niet beperkt')}</select></label><div class="form-row"><label>Minimumleeftijd<input required name="minAge" type="number" min="18" max="60" value="${b.minAge}"></label><label>Maximumleeftijd<input required name="maxAge" type="number" min="18" max="60" value="${b.maxAge}"></label></div><label>Welke taken moet deze speler vervullen?<textarea name="task" maxlength="2400" rows="5" placeholder="Bijvoorbeeld: ruimte achter een hoge verdediging verdedigen en onder druk vooruit spelen.">${esc(b.task)}</textarea></label><label>Budgetscope en nog te verifiëren voorwaarden<textarea name="budgetScope" maxlength="1000" rows="3" placeholder="Bijvoorbeeld: totaalbudget inclusief salaris, niet alleen transfersom. Onbekende bedragen blijven onbekend.">${esc(b.budgetScope)}</textarea></label><p class="form-note">De tekst wordt bewaard als menselijke onderzoeksinstructie. Deze versie vertaalt de tekst niet met AI naar een automatische rolscore.</p><div class="form-actions"><button class="button primary" type="submit">${icon('check')} Profiel opslaan</button><button class="button secondary" type="button" data-action="apply-brief">Gebruik positie en leeftijd</button></div></form><div class="panel info-panel brief-explanation"><span class="eyebrow">VIER APARTE VRAGEN</span><h2>Geen alles-in-één<br>talentcijfer.</h2><div class="principle"><b>01</b><div><strong>Wat zien we nu?</strong><p>Prestaties en waarnemingen met hun bron.</p></div></div><div class="principle"><b>02</b><div><strong>Wat kan veranderen?</strong><p>Een ontwikkelhypothese is nog geen bewezen groeipad.</p></div></div><div class="principle"><b>03</b><div><strong>Past de gevraagde taak?</strong><p>Dit beoordeelt de scout met passend bewijs.</p></div></div><div class="principle"><b>04</b><div><strong>Is de overstap haalbaar?</strong><p>Budget, beschikbaarheid en registratie blijven afzonderlijk te controleren.</p></div></div></div></div>`;
OmniScout-preview.html:1613: trailing whitespace.
+}
OmniScout-preview.html:1614: trailing whitespace.
+function renderLog() {
OmniScout-preview.html:1615: trailing whitespace.
+  const actions = { 'decision.created': 'Scoutbesluit vastgelegd', 'task.created': 'Onderzoek aangemaakt', 'task.updated': 'Onderzoek bijgewerkt', 'brief.updated': 'Clubvraag gewijzigd' };
OmniScout-preview.html:1616: trailing whitespace.
+  main.innerHTML = heading('HET GEHEUGEN VAN JE SCOUTING', 'Bewaar de reden.', 'Een budgetafwijzing is geen negatief talentlabel. Alle wijzigingen blijven als nieuwe gebeurtenissen zichtbaar.', `<button class="button secondary" data-action="export-state">${icon('export')} Exporteer logboek</button>`) + `<section class="panel log-panel">${state.audit.length ? `<div class="timeline">${[...state.audit].reverse().map(e => `<article class="timeline-item"><span class="timeline-icon">${icon(e.action.startsWith('task') ? 'board' : e.action.startsWith('brief') ? 'sliders' : 'bookmark')}</span><div><h3>${esc(actions[e.action] || e.action)}</h3><p>${esc(e.detail || 'Lokaal onderzoeksprofiel opgeslagen')}</p><small>${esc(new Date(e.at).toLocaleString('nl-NL'))} · ${esc(e.actor)}</small></div></article>`).join('')}</div>` : '<div class="board-empty">Nog geen scoutbesluiten of onderzoeksacties. Het logboek wordt gevuld zodra je een actie opslaat.</div>'}<div class="table-foot">Dit lokale logboek is geen productie-audit met onafhankelijk gecontroleerde integriteit.</div></section>`;
OmniScout-preview.html:1617: trailing whitespace.
+}
OmniScout-preview.html:1618: trailing whitespace.
+function importIssues(items, title, kind) {
OmniScout-preview.html:1619: trailing whitespace.
+  if (!Array.isArray(items) || !items.length) return '';
OmniScout-preview.html:1620: trailing whitespace.
+  return `<section class="import-issues ${kind}" aria-label="${esc(title)}"><h3>${esc(title)} (${items.length})</h3><ul>${items.slice(0, 100).map(item => `<li><code>${esc(item.path)}</code> ${esc(item.message)}</li>`).join('')}</ul>${items.length > 100 ? '<p>De eerste 100 meldingen worden getoond. Herstel deze en controleer opnieuw.</p>' : ''}</section>`;
OmniScout-preview.html:1621: trailing whitespace.
+}
OmniScout-preview.html:1622: trailing whitespace.
+function importSummary() {
OmniScout-preview.html:1623: trailing whitespace.
+  if (!importUI.preview) return '';
OmniScout-preview.html:1624: trailing whitespace.
+  const payload = importUI.payload && typeof importUI.payload === 'object' ? importUI.payload : {}, preview = importUI.preview, summary = preview.summary || {}, source = summary.source || payload.source || {};
OmniScout-preview.html:1625: trailing whitespace.
+  const competitions = Array.isArray(payload.competitions) ? payload.competitions : [], players = Array.isArray(payload.players) ? payload.players : [];
OmniScout-preview.html:1626: trailing whitespace.
+  const timestamps = ['eventAt', 'publishedAt', 'retrievedAt', 'availableAt'];
OmniScout-preview.html:1627: trailing whitespace.
+  const timeLabels = { eventAt: 'Gebeurtenis', publishedAt: 'Gepubliceerd', retrievedAt: 'Opgehaald', availableAt: 'Beschikbaar' };
OmniScout-preview.html:1628: trailing whitespace.
+  const stampRange = key => {
OmniScout-preview.html:1629: trailing whitespace.
+    const values = players.flatMap(p => [p?.[key], p?.stats?.[key]]).filter(value => typeof value === 'string').sort();
OmniScout-preview.html:1630: trailing whitespace.
+    return values.length ? `${values[0]} — ${values.at(-1)}` : 'Onbekend';
OmniScout-preview.html:1631: trailing whitespace.
+  };
OmniScout-preview.html:1632: trailing whitespace.
+  return `<section class="panel import-preview" id="import-preview" tabindex="-1"><div class="panel-head"><div><span class="eyebrow">CONTROLE ZONDER OPSLAG</span><h2>${preview.valid ? 'Bestand voldoet aan de importcontroles' : 'Import geblokkeerd'}</h2></div><span class="status-tag ${preview.valid ? 'green' : 'amber'}">${preview.valid ? 'Klaar voor bevestiging' : 'Herstel fouten / conflicten'}</span></div>
OmniScout-preview.html:1633: trailing whitespace.
+    <div class="import-preview-body"><p class="muted-copy">${esc(sourceLabel(source))}. Een geslaagde controle verifieert geen externe bronrechten of voetbalclaims.</p>
OmniScout-preview.html:1634: trailing whitespace.
+    <dl class="import-summary"><div><dt>Snapshot</dt><dd>${esc(payload.snapshotId)}</dd></div><div><dt>Correctie van</dt><dd>${esc(payload.correctionOf || 'Geen')}</dd></div><div><dt>Peildatum</dt><dd>${esc(payload.asOf)}</dd></div><div><dt>Aantallen in upload</dt><dd>${esc(summary.playerCount ?? players.length)} spelers · ${esc(summary.competitionCount ?? competitions.length)} competities · ${esc(summary.sourceCount ?? (payload.source ? 1 : 0))} bron</dd></div><div><dt>Bron</dt><dd>${esc(source.name)} (${esc(source.id)})</dd></div><div><dt>Toegestane verwerking</dt><dd>${esc(Array.isArray(source.allowedUses) ? source.allowedUses.join(', ') : 'Ontbreekt')}</dd></div><div><dt>Rechten verklaard</dt><dd>${source.rightsAttested === true ? 'Ja, volgens de importeur' : 'Nee'}</dd></div><div><dt>Rechten geldig vanaf</dt><dd>${esc(source.validFrom || 'Ontbreekt')}</dd></div><div><dt>Rechten vervallen</dt><dd>${esc(source.expiresAt === null && source.status === 'synthetic' ? 'Geen vervaldatum (synthetische bron)' : source.expiresAt || 'Ontbreekt')}</dd></div><div><dt>Rechtennotitie</dt><dd>${esc(source.rightsNote || 'Ontbreekt')}</dd></div>${timestamps.map(key => `<div><dt>${timeLabels[key]} (bereik)</dt><dd>${esc(stampRange(key))}</dd></div>`).join('')}</dl>
OmniScout-preview.html:1635: trailing whitespace.
+    <p class="form-note">${esc(summary.missingMinutes ?? players.filter(p => p?.stats?.minutes === null).length)} spelers met onbekende minuten; deze krijgen geen per-90-meting. ${summary.idempotent ? 'Deze ongewijzigde snapshot bestaat al: herhaling maakt geen dubbele waarnemingen.' : ''}</p><h3>Competities en seizoenen</h3><ul class="import-seasons">${competitions.slice(0, 100).map(c => `<li>${esc(c?.name)} · ${esc(c?.season || 'Seizoen ontbreekt')} · ${esc(c?.country)} · ontvangen ${esc(c?.lastReceivedAt || 'Onbekend')}</li>`).join('') || '<li>Geen competities aangeleverd.</li>'}</ul>
OmniScout-preview.html:1636: trailing whitespace.
+    ${importIssues(preview.errors, 'Blokkerende fouten en identiteitsconflicten', 'error')}${importIssues(preview.warnings, 'Waarschuwingen — geen automatische samenvoeging', 'warning')}
OmniScout-preview.html:1637: trailing whitespace.
+    ${preview.valid ? `<form id="import-confirm-form"><label class="import-consent"><input type="checkbox" id="import-confirm-check" required><span>Ik heb de bron, rechtenverklaring, peildatum en waarschuwingen gecontroleerd en wil deze snapshot in de lokale importdataset opslaan.</span></label><button type="submit" class="button primary" id="import-confirm" ${importUI.busy ? 'disabled' : ''}>${icon('check')} Bevestig lokale import</button><p class="form-note">Pas na bevestiging wordt een verwerkingsopdracht aangemaakt. De backend controleert rechten en identiteit opnieuw.</p></form>` : '<p class="form-note">Er is niets geïmporteerd. Corrigeer het bronbestand en kies het opnieuw om de controle te herhalen.</p>'}</div></section>`;
OmniScout-preview.html:1638: trailing whitespace.
+}
OmniScout-preview.html:1639: trailing whitespace.
+function renderImportJobs() {
OmniScout-preview.html:1640: trailing whitespace.
+  const host = document.querySelector('#import-jobs'); if (!host) return;
OmniScout-preview.html:1641: trailing whitespace.
+  const statusLabels = { queued: 'In wachtrij', running: 'Wordt verwerkt', succeeded: 'Geslaagd', failed: 'Mislukt' };
OmniScout-preview.html:1642: trailing whitespace.
+  host.innerHTML = `${importUI.jobsError ? `<p class="import-error" role="alert">${esc(importUI.jobsError)}</p>` : ''}${importUI.jobsLoading && !importUI.jobs.length ? '<p class="muted-copy">Werkelijke taakstatus ophalen…</p>' : !importUI.jobs.length ? '<p class="muted-copy">Nog geen importopdrachten in deze lokale backend.</p>' : ''}${[...importUI.jobs].reverse().map(job => `<article class="import-job" data-job-id="${esc(job.id)}" data-job-status="${esc(job.status)}"><div class="import-job-heading"><h3>${esc(job.snapshotId)}</h3><span class="status-tag ${job.status === 'succeeded' ? 'green' : 'amber'}">${esc(statusLabels[job.status] || job.status)}</span></div><p class="muted-copy">Poging ${esc(job.attempts)} / ${esc(job.maxAttempts)} · Bijgewerkt ${esc(job.updatedAt)}</p><small>Taak ${esc(job.id)}</small>${job.error ? `<p class="import-error">${esc(typeof job.error === 'string' ? job.error : job.error.message)}</p>${importIssues(job.error.details, 'Foutdetails', 'error')}` : ''}<div class="form-actions">${job.status === 'failed' && job.attempts < job.maxAttempts ? `<button class="button secondary" data-import-retry="${esc(job.id)}" ${importUI.busy ? 'disabled' : ''}>Opnieuw proberen</button>` : ''}${job.status === 'succeeded' ? (snapshotStatus(job.snapshotId) === 'rolled_back' || importUI.rolledBack.has(job.snapshotId)) ? '<p class="muted-copy">Snapshot teruggedraaid · backendgeschiedenis blijft bewaard.</p>' : snapshotStatus(job.snapshotId) === 'superseded' ? '<p class="muted-copy">Vervangen door een correctie. Draai eerst die correctie terug.</p>' : `<button class="button secondary" data-import-rollback="${esc(job.snapshotId)}" ${importUI.busy ? 'disabled' : ''}>Snapshot terugdraaien</button>` : ''}</div></article>`).join('')}`;
OmniScout-preview.html:1643: trailing whitespace.
+}
OmniScout-preview.html:1644: trailing whitespace.
+function snapshotStatus(snapshotId) { return importUI.snapshots.find(snapshot => snapshot.snapshotId === snapshotId)?.status; }
OmniScout-preview.html:1645: trailing whitespace.
+function renderImport() {
OmniScout-preview.html:1646: trailing whitespace.
+  main.innerHTML = heading('LOKALE BRONIMPORT', 'Controleer. Bevestig. Volg.', 'Importeer één JSON-snapshot met expliciete bronrechten, seizoenen, provideridentiteiten en tijdstempels.') + (!canImport() ? `<section class="panel info-panel"><h2>Start de lokale Node-app voor bronimport</h2><p>De zelfstandige HTML-preview en de edge-demo bevatten alleen synthetische testgegevens. Import, taakverwerking en rollback vereisen de lokale backend.</p><p>Open de toepassing via het adres dat <code>npm start</code> toont. Je huidige demogegevens blijven in de fictieve gegevensset.</p></section>` : `<section class="panel form-panel import-upload"><div class="panel-head"><h2>1. Kies en controleer een bronbestand</h2><button class="button secondary compact" data-action="import-sample">${icon('export')} Synthetisch voorbeeld</button></div><label for="import-file">JSON-bestand · maximaal 1 MiB<input type="file" id="import-file" accept=".json,application/json" ${importUI.busy ? 'disabled' : ''} aria-describedby="import-file-help"></label><p id="import-file-help" class="form-note">Alleen het gekozen bestand wordt gelezen. Een voorvertoning schrijft niets naar de importcatalogus. Bronlocators worden als tekst behandeld.</p>${importUI.filename ? `<p class="muted-copy">Bestand: <strong>${esc(importUI.filename)}</strong></p>` : ''}${importUI.busy ? '<p class="muted-copy" role="status">Lokale backend verwerkt het verzoek…</p>' : ''}${importUI.error ? `<p class="import-error" id="import-error" role="alert">${esc(importUI.error)}</p>` : ''}</section>${importSummary()}<section class="panel import-jobs-panel"><div class="panel-head"><div><span class="eyebrow">STATUS UIT DE BACKEND</span><h2>Verwerkingsopdrachten</h2></div><button class="button secondary compact" data-action="import-refresh">${icon('clock')} Vernieuwen</button></div><p class="form-note">Lopende taken worden automatisch gecontroleerd. Een geslaagde taak blijft in de geschiedenis staan na rollback; de actuele catalogus bepaalt welke gegevens beschikbaar zijn.</p><div id="import-jobs" aria-live="polite" aria-atomic="false"></div></section>`);
OmniScout-preview.html:1647: trailing whitespace.
+  renderImportJobs();
OmniScout-preview.html:1648: trailing whitespace.
+}
OmniScout-preview.html:1649: trailing whitespace.
+async function previewFile(file) {
OmniScout-preview.html:1650: trailing whitespace.
+  if (!file || !canImport()) return;
OmniScout-preview.html:1651: trailing whitespace.
+  const sequence = ++importUI.sequence;
OmniScout-preview.html:1652: trailing whitespace.
+  importUI.busy = true; importUI.payload = null; importUI.preview = null; importUI.error = ''; importUI.filename = file.name; renderImport();
OmniScout-preview.html:1653: trailing whitespace.
+  try {
OmniScout-preview.html:1654: trailing whitespace.
+    if (file.size > 1024 * 1024) throw new Error('Bestand is groter dan 1 MiB. Kies een kleinere JSON-snapshot.');
OmniScout-preview.html:1655: trailing whitespace.
+    let payload; try { payload = JSON.parse(await file.text()); } catch { throw new Error('Ongeldige JSON. Er is niets geïmporteerd.'); }
OmniScout-preview.html:1656: trailing whitespace.
+    const preview = await request('/api/import/preview', 'POST', payload);
OmniScout-preview.html:1657: trailing whitespace.
+    if (sequence !== importUI.sequence) return;
OmniScout-preview.html:1658: trailing whitespace.
+    importUI.payload = payload; importUI.preview = preview;
OmniScout-preview.html:1659: trailing whitespace.
+  } catch (error) { if (sequence === importUI.sequence) importUI.error = error.message || 'Voorvertoning mislukt.'; }
OmniScout-preview.html:1660: trailing whitespace.
+  finally { if (sequence === importUI.sequence) { importUI.busy = false; if (view === 'import') { renderImport(); document.querySelector('#import-preview')?.focus(); } } }
OmniScout-preview.html:1661: trailing whitespace.
+}
OmniScout-preview.html:1662: trailing whitespace.
+async function refreshImportCatalog() {
OmniScout-preview.html:1663: trailing whitespace.
+  if (dataset !== 'import') return;
OmniScout-preview.html:1664: trailing whitespace.
+  const [nextCatalog, nextState] = await Promise.all([readJSON(scopedPath('/api/catalog', 'import')), readJSON(scopedPath('/api/state', 'import'))]);
OmniScout-preview.html:1665: trailing whitespace.
+  if (dataset !== 'import') return;
OmniScout-preview.html:1666: trailing whitespace.
+  catalog = nextCatalog; state = nextState;
OmniScout-preview.html:1667: trailing whitespace.
+  comparison = new Set([...comparison].filter(id => catalog.players.some(player => player.id === id)));
OmniScout-preview.html:1668: trailing whitespace.
+  if (dossierDialog.open) { if (catalog.players.some(player => player.id === openPlayerId)) showDossier(openPlayerId); else dossierDialog.close(); }
OmniScout-preview.html:1669: trailing whitespace.
+  if (view !== 'import') render(); else updateCounts();
OmniScout-preview.html:1670: trailing whitespace.
+}
OmniScout-preview.html:1671: trailing whitespace.
+async function loadImportJobs() {
OmniScout-preview.html:1672: trailing whitespace.
+  if (!canImport() || importUI.jobsLoading) return;
OmniScout-preview.html:1673: trailing whitespace.
+  clearTimeout(jobsTimer); importUI.jobsLoading = true;
OmniScout-preview.html:1674: trailing whitespace.
+  try {
OmniScout-preview.html:1675: trailing whitespace.
+    const previous = new Map(importUI.jobs.map(job => [job.id, job.status]));
OmniScout-preview.html:1676: trailing whitespace.
+    const previousSnapshots = JSON.stringify(importUI.snapshots);
OmniScout-preview.html:1677: trailing whitespace.
+    const [result, importedCatalog] = await Promise.all([readJSON('/api/import/jobs'), readJSON(scopedPath('/api/catalog', 'import'))]);
OmniScout-preview.html:1678: trailing whitespace.
+    importUI.jobs = result.jobs; importUI.snapshots = importedCatalog.importSnapshots || []; importUI.jobsError = '';
OmniScout-preview.html:1679: trailing whitespace.
+    if (JSON.stringify(importUI.snapshots) !== previousSnapshots || importUI.jobs.some(job => job.status === 'succeeded' && previous.get(job.id) !== 'succeeded')) await refreshImportCatalog();
OmniScout-preview.html:1680: trailing whitespace.
+  } catch (error) { importUI.jobsError = `${error.message} Gebruik Vernieuwen om opnieuw te controleren.`; }
OmniScout-preview.html:1681: trailing whitespace.
+  finally {
OmniScout-preview.html:1682: trailing whitespace.
+    importUI.jobsLoading = false; renderImportJobs();
OmniScout-preview.html:1683: trailing whitespace.
+    if (!importUI.jobsError && importUI.jobs.some(job => ['queued', 'running'].includes(job.status))) jobsTimer = setTimeout(loadImportJobs, 1200);
OmniScout-preview.html:1684: trailing whitespace.
+  }
OmniScout-preview.html:1685: trailing whitespace.
+}
OmniScout-preview.html:1686: trailing whitespace.
+async function downloadResponse(path, name, type) {
OmniScout-preview.html:1687: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
OmniScout-preview.html:1688: trailing whitespace.
+  if (!response.ok) { const error = await response.json(); throw new Error(error.error || 'Export niet toegestaan.'); }
OmniScout-preview.html:1689: trailing whitespace.
+  download(name, await response.text(), type);
OmniScout-preview.html:1690: trailing whitespace.
+}
OmniScout-preview.html:1691: trailing whitespace.
+function render() {
OmniScout-preview.html:1692: trailing whitespace.
+  document.querySelector('#breadcrumb').textContent = viewLabels[view]; document.title = `Omni-Scout · ${viewLabels[view]}`;
OmniScout-preview.html:1693: trailing whitespace.
+  document.querySelectorAll('#nav [data-view]').forEach(el => { const active = el.dataset.view === view; el.classList.toggle('active', active); if (active) el.setAttribute('aria-current', 'page'); else el.removeAttribute('aria-current'); });
OmniScout-preview.html:1694: trailing whitespace.
+  ({ radar: () => renderRadar(), shortlist: () => renderRadar(true), tasks: renderTasks, coverage: renderCoverage, import: renderImport, brief: renderBrief, log: renderLog })[view]();
OmniScout-preview.html:1695: trailing whitespace.
+  updateCounts();
OmniScout-preview.html:1696: trailing whitespace.
+}
OmniScout-preview.html:1697: trailing whitespace.
+function navigate(next) { if (!viewLabels[next]) return; view = next; render(); if (next === 'import') loadImportJobs(); window.scrollTo({ top: 0, behavior: 'instant' }); }
OmniScout-preview.html:1698: trailing whitespace.
+function showDossier(id) {
OmniScout-preview.html:1699: trailing whitespace.
+  const d = buildDossier(catalog.players.find(p => p.id === id), catalog); if (!d) return;
OmniScout-preview.html:1700: trailing whitespace.
+  openPlayerId = id;
OmniScout-preview.html:1701: trailing whitespace.
+  const decisions = state.decisions.filter(x => x.playerId === id);
OmniScout-preview.html:1702: trailing whitespace.
+  document.querySelector('#dossier-content').innerHTML = `<div class="dialog-top"><span class="eyebrow">SPELERSDOSSIER · ${esc(playerLabel(d.player))}</span><button class="icon-button" data-close="dossier" aria-label="Dossier sluiten">${icon('close')}</button></div><div class="dossier-header"><span class="player-avatar huge variant-0">${esc(initials(d.player.name))}</span><div><h2 id="dossier-title">${esc(d.player.name)}</h2><p>${esc(d.player.club)} · ${esc(d.competition.country)}</p><div class="dossier-tags"><span class="status-tag neutral">${esc(ROLE_LABELS[d.player.role])}</span><span class="status-tag neutral">${d.age} jaar</span>${qualityTag(d)}</div></div></div><div class="dossier-actions"><button class="button ${isShortlisted(id) ? 'secondary' : 'primary'}" data-save="${esc(id)}">${icon('bookmark')}${isShortlisted(id) ? 'Van shortlist halen' : 'Zet op shortlist'}</button><button class="button secondary" data-action="decision" data-player="${esc(id)}">${icon('file')} Leg besluit vast</button></div><div class="dossier-metrics"><div><span>${esc(d.roleMetric.label)}</span><strong>${esc(metric(d))}</strong><small>Testregel · geen universele norm</small></div><div><span>Speelminuten</span><strong>${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></div><div><span>Competitieniveau</span><strong>${d.competition.tier}</strong><small>${esc(d.competition.name)}</small></div></div>
OmniScout-preview.html:1703: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot"></span><h3>Waarom nader onderzoeken?</h3></div>${d.signals.length ? d.signals.map(s => `<div class="evidence-item"><strong>${esc(s.title)}</strong><p>${esc(s.detail)}</p><small>Beschikbaar sinds ${dates(s.availableAt)} · ${esc(d.source.name)}</small></div>`).join('') : '<p>Geen aantoonbaar signaal binnen de beschikbare gegevens. Niet automatisch geïnterpreteerd als een slechte speler.</p>'}</section>
OmniScout-preview.html:1704: trailing whitespace.
+    <section class="next-action"><span class="eyebrow">DE BESTE VOLGENDE VRAAG · REGELGEBASEERD</span><h3>${esc(d.nextQuestion)}</h3><p>Gericht op ontbrekend bewijs. Niet op bevestiging van een vooraf gekozen conclusie.</p><button class="button primary" data-action="create-task" data-player="${esc(id)}">${icon('plus')} Maak onderzoeksopdracht</button></section>
OmniScout-preview.html:1705: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot amber-dot"></span><h3>Wat spreekt tegen de aanbeveling?</h3></div>${d.counters.length ? d.counters.map(e => `<div class="evidence-item counter"><strong>${esc(e.title)}</strong><p>${esc(e.text)}</p><small>${esc(e.locator)} · ${d.player.synthetic ? 'menselijke testobservatie' : 'aangeleverde menselijke observatie'}</small></div>`).join('') : '<p>Geen tegenobservaties vastgelegd. Dat is niet hetzelfde als afwezigheid van risico.</p>'}</section>
OmniScout-preview.html:1706: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('warning')}<h3>Wat weten we nog niet?</h3></div><div class="gap-list">${d.gaps.map(g => `<p><span>?</span>${esc(g)}</p>`).join('')}</div></section>
OmniScout-preview.html:1707: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('file')}<h3>Bronnen en bewijs</h3></div>${d.evidence.map(e => `<div class="source-row"><strong>${esc(e.locator)}</strong><span>${esc(e.kind === 'counter' ? 'Tegenobservatie' : 'Positieve observatie')}</span><small>Waargenomen ${dates(e.eventAt)} · Beschikbaar ${dates(e.availableAt)}<br>${esc(e.text)}</small></div>`).join('')}<p class="muted-copy">${d.player.synthetic ? 'Dit profiel bevat synthetische softwaretestgegevens.' : 'De aangeleverde broninhoud en rechtenverklaring zijn niet onafhankelijk geverifieerd.'} “Meer bewijs” betekent dat de beschikbare metingen aan een demonstratieregel voldoen. Bronlocators zijn tekst, geen geverifieerde beelden.</p></section>
OmniScout-preview.html:1708: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('clock')}<h3>Eerdere besluiten</h3></div>${decisions.length ? [...decisions].reverse().map(e => `<div class="source-row"><strong>${esc(ACTION_LABELS[e.action])} · ${esc(REASON_LABELS[e.reason])}</strong><small>${esc(e.note || 'Geen aanvullende notitie')}<br>${esc(new Date(e.at).toLocaleString('nl-NL'))}</small></div>`).join('') : '<p class="muted-copy">Nog niet beoordeeld door deze lokale werkruimte. Geen uitspraak over bekendheid bij andere clubs.</p>'}</section><div class="dialog-footer">Bron: ${esc(d.source.name)} · Peildatum ${dates(d.asOf)}</div>`;
OmniScout-preview.html:1709: trailing whitespace.
+  if (!dossierDialog.open) dossierDialog.showModal();
OmniScout-preview.html:1710: trailing whitespace.
+}
OmniScout-preview.html:1711: trailing whitespace.
+function openAction(title, body) {
OmniScout-preview.html:1712: trailing whitespace.
+  document.querySelector('#action-content').innerHTML = `<div class="dialog-top"><h2 id="action-title">${esc(title)}</h2><button class="icon-button" data-close="action" aria-label="Dialoog sluiten">${icon('close')}</button></div>${body}`;
OmniScout-preview.html:1713: trailing whitespace.
+  actionDialog.showModal();
OmniScout-preview.html:1714: trailing whitespace.
+}
OmniScout-preview.html:1715: trailing whitespace.
+function download(name, content, type) { const url = URL.createObjectURL(new Blob([content], { type })); const a = document.createElement('a'); a.href = url; a.download = name; a.click(); setTimeout(() => URL.revokeObjectURL(url), 2000); }
OmniScout-preview.html:1716: trailing whitespace.
+function applyFiltersFromControl(el) {
OmniScout-preview.html:1717: trailing whitespace.
+  const key = el.dataset.filter; if (!key) return;
OmniScout-preview.html:1718: trailing whitespace.
+  filters[key] = el.type === 'checkbox' ? el.checked : el.value;
OmniScout-preview.html:1719: trailing whitespace.
+  const pos = el.type === 'search' ? el.selectionStart : null;
OmniScout-preview.html:1720: trailing whitespace.
+  render(); const next = main.querySelector(`[data-filter="${key}"]`); next?.focus(); if (pos !== null) next?.setSelectionRange(pos, pos);
OmniScout-preview.html:1721: trailing whitespace.
+}
OmniScout-preview.html:1722: trailing whitespace.
+main.addEventListener('input', event => { if (event.target.matches('input[type="search"][data-filter]')) applyFiltersFromControl(event.target); });
OmniScout-preview.html:1723: trailing whitespace.
+document.addEventListener('change', event => {
OmniScout-preview.html:1724: trailing whitespace.
+  const el = event.target;
OmniScout-preview.html:1725: trailing whitespace.
+  if (el.id === 'dataset-select') { selectDataset(el.value).catch(error => { updateCounts(); notify(error.message, true); }); return; }
OmniScout-preview.html:1726: trailing whitespace.
+  if (el.id === 'import-file') { previewFile(el.files?.[0]); return; }
OmniScout-preview.html:1727: trailing whitespace.
+  if (el.matches('[data-filter]') && el.type !== 'search') applyFiltersFromControl(el);
OmniScout-preview.html:1728: trailing whitespace.
+  if (el.dataset.compare) {
OmniScout-preview.html:1729: trailing whitespace.
+    if (el.checked && comparison.size >= 3) { el.checked = false; return notify('Vergelijk maximaal drie profielen tegelijk.'); }
OmniScout-preview.html:1730: trailing whitespace.
+    el.checked ? comparison.add(el.dataset.compare) : comparison.delete(el.dataset.compare);
OmniScout-preview.html:1731: trailing whitespace.
+    const button = document.querySelector('#compare-button'); if (button) { button.disabled = comparison.size < 2; button.innerHTML = `${icon('compare')} Vergelijk <span>${comparison.size}/3</span>`; }
OmniScout-preview.html:1732: trailing whitespace.
+  }
OmniScout-preview.html:1733: trailing whitespace.
+});
OmniScout-preview.html:1734: trailing whitespace.
+document.addEventListener('click', async event => {
OmniScout-preview.html:1735: trailing whitespace.
+  const el = event.target.closest('button, a.brand'); if (!el) return;
OmniScout-preview.html:1736: trailing whitespace.
+  try {
OmniScout-preview.html:1737: trailing whitespace.
+    if (el.matches('a.brand')) { event.preventDefault(); return navigate('radar'); }
OmniScout-preview.html:1738: trailing whitespace.
+    if (el.dataset.close) { (el.dataset.close === 'dossier' ? dossierDialog : actionDialog).close(); return; }
OmniScout-preview.html:1739: trailing whitespace.
+    if (el.dataset.view) return navigate(el.dataset.view);
OmniScout-preview.html:1740: trailing whitespace.
+    if (el.dataset.importRetry) {
OmniScout-preview.html:1741: trailing whitespace.
+      importUI.busy = true; el.disabled = true;
OmniScout-preview.html:1742: trailing whitespace.
+      try { await request(`/api/import/jobs/${encodeURIComponent(el.dataset.importRetry)}/retry`, 'POST', {}); await loadImportJobs(); notify('Nieuwe poging geregistreerd. Volg de werkelijke taakstatus hieronder.'); }
OmniScout-preview.html:1743: trailing whitespace.
+      catch (error) { importUI.jobsError = error.message; throw error; }
OmniScout-preview.html:1744: trailing whitespace.
+      finally { importUI.busy = false; renderImportJobs(); }
OmniScout-preview.html:1745: trailing whitespace.
+      return;
OmniScout-preview.html:1746: trailing whitespace.
+    }
OmniScout-preview.html:1747: trailing whitespace.
+    if (el.dataset.importRollback) {
OmniScout-preview.html:1748: trailing whitespace.
+      openAction('Importsnapshot terugdraaien', `<form id="import-rollback-form" data-snapshot="${esc(el.dataset.importRollback)}"><p class="muted-copy">Snapshot <strong>${esc(el.dataset.importRollback)}</strong> wordt uit de actieve importcatalogus teruggenomen. Eerdere versies en de mutatiegeschiedenis blijven bewaard. Onderzoeksbesluiten worden niet stilzwijgend herschreven.</p><button type="submit" class="button primary">Bevestig rollback</button></form>`); return;
OmniScout-preview.html:1749: trailing whitespace.
+    }
OmniScout-preview.html:1750: trailing whitespace.
+    if (el.dataset.open) return showDossier(el.dataset.open);
OmniScout-preview.html:1751: trailing whitespace.
+    if (el.dataset.quality !== undefined) { filters.quality = el.dataset.quality; return render(); }
OmniScout-preview.html:1752: trailing whitespace.
+    if (el.dataset.region) { filters.region = el.dataset.region; filters.competition = ''; render(); document.querySelector('.candidates')?.scrollIntoView({ behavior: 'smooth', block: 'start' }); return; }
OmniScout-preview.html:1753: trailing whitespace.
+    if (el.dataset.competition) { filters = { ...filters, competition: el.dataset.competition, region: '', role: '', quality: '', search: '' }; return navigate('radar'); }
OmniScout-preview.html:1754: trailing whitespace.
+    if (el.dataset.save) {
OmniScout-preview.html:1755: trailing whitespace.
+      el.disabled = true; const id = el.dataset.save, removing = isShortlisted(id);
OmniScout-preview.html:1756: trailing whitespace.
+      await saveDecision(id, removing ? 'archive' : 'follow', removing ? 'other' : 'positive', removing ? 'Van shortlist gehaald; dit is geen negatief talentlabel.' : 'Opgeslagen als onderzoekskandidaat.');
OmniScout-preview.html:1757: trailing whitespace.
+      render(); if (dossierDialog.open) showDossier(id); notify(removing ? 'Van shortlist gehaald. Reden is vastgelegd.' : 'Opgeslagen op je shortlist.'); return;
OmniScout-preview.html:1758: trailing whitespace.
+    }
OmniScout-preview.html:1759: trailing whitespace.
+    if (el.dataset.task) {
OmniScout-preview.html:1760: trailing whitespace.
+      const task = state.tasks.find(t => t.id === el.dataset.task);
OmniScout-preview.html:1761: trailing whitespace.
+      openAction(task.status === 'done' ? 'Onderzoek heropenen' : 'Uitkomst vastleggen', `<form id="task-result-form" data-id="${esc(task.id)}" data-status="${task.status === 'done' ? 'todo' : 'done'}"><p class="muted-copy">${esc(task.question)}</p><label>Wat heb je waargenomen? Wat bleef onzeker?<textarea name="result" rows="5" maxlength="2400" ${task.status !== 'done' ? 'required' : ''}>${esc(task.result)}</textarea></label><button class="button primary" type="submit">${icon('check')} ${task.status === 'done' ? 'Heropenen' : 'Uitkomst opslaan'}</button></form>`); return;
OmniScout-preview.html:1762: trailing whitespace.
+    }
OmniScout-preview.html:1763: trailing whitespace.
+    const action = el.dataset.action;
OmniScout-preview.html:1764: trailing whitespace.
+    if (action === 'import-refresh') { await loadImportJobs(); await refreshImportCatalog(); return; }
OmniScout-preview.html:1765: trailing whitespace.
+    if (action === 'import-sample') { await downloadResponse('/api/import/sample', 'OmniScout-import-SYNTHETISCH.json', 'application/json'); return; }
OmniScout-preview.html:1766: trailing whitespace.
+    if (action === 'reset') { filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 35, newOnly: false, lowerOnly: false }; return navigate('radar'); }
OmniScout-preview.html:1767: trailing whitespace.
+    if (action === 'clear-competition') { filters.competition = ''; return render(); }
OmniScout-preview.html:1768: trailing whitespace.
+    if (action === 'apply-brief') { filters.role = state.brief.role; filters.minAge = state.brief.minAge; filters.maxAge = state.brief.maxAge; return navigate('radar'); }
OmniScout-preview.html:1769: trailing whitespace.
+    if (action === 'export') {
OmniScout-preview.html:1770: trailing whitespace.
+      const selectedFilters = { ...filters, shortlistOnly: view === 'shortlist' };
OmniScout-preview.html:1771: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath(`/api/export?${new URLSearchParams(selectedFilters)}`), `OmniScout-selectie-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.csv`, 'text/csv;charset=utf-8');
OmniScout-preview.html:1772: trailing whitespace.
+      else download('OmniScout-selectie-FICTIEF.csv', dossierCSV(findPlayers(catalog, selectedFilters, state)), 'text/csv;charset=utf-8');
OmniScout-preview.html:1773: trailing whitespace.
+      return notify('Selectie geëxporteerd met bron- en gegevensstatus.');
OmniScout-preview.html:1774: trailing whitespace.
+    }
OmniScout-preview.html:1775: trailing whitespace.
+    if (action === 'export-state') {
OmniScout-preview.html:1776: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath('/api/export/state'), `OmniScout-logboek-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.json`, 'application/json');
OmniScout-preview.html:1777: trailing whitespace.
+      else download('OmniScout-demologboek.json', JSON.stringify({ mode: 'synthetic_demo', exportedAt: new Date().toISOString(), ...state }, null, 2), 'application/json');
OmniScout-preview.html:1778: trailing whitespace.
+      return;
OmniScout-preview.html:1779: trailing whitespace.
+    }
OmniScout-preview.html:1780: trailing whitespace.
+    if (action === 'create-task') {
OmniScout-preview.html:1781: trailing whitespace.
+      const d = buildDossier(catalog.players.find(p => p.id === el.dataset.player), catalog);
OmniScout-preview.html:1782: trailing whitespace.
+      openAction('Gerichte onderzoeksopdracht', `<form id="create-task-form" data-player="${esc(d.player.id)}" data-request="${newId()}"><p class="muted-copy">${esc(d.player.name)} · ${esc(d.player.club)} · ${esc(playerLabel(d.player))}</p><label>Welke onzekerheid ga je onderzoeken?<textarea name="question" rows="5" required maxlength="1600">${esc(d.nextQuestion)}</textarea></label><p class="form-note">Alleen intern vastleggen. Deze toepassing verstuurt geen bericht en koopt geen beelden in.</p><button class="button primary" type="submit">${icon('plus')} Opdracht vastleggen</button></form>`); return;
OmniScout-preview.html:1783: trailing whitespace.
+    }
OmniScout-preview.html:1784: trailing whitespace.
+    if (action === 'decision') {
OmniScout-preview.html:1785: trailing whitespace.
+      openAction('Scoutbesluit vastleggen', `<form id="decision-form" data-player="${esc(el.dataset.player)}"><label>Vervolgactie<select name="action" required>${Object.entries(ACTION_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Reden<select name="reason" required>${Object.entries(REASON_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Onderbouwing<textarea name="note" required maxlength="2400" rows="4" placeholder="Wat ondersteunt dit besluit? Wat blijft onzeker?"></textarea></label><p class="form-note">Een budget- of rolafwijzing wordt niet omgezet in een label ‘slechte speler’.</p><button class="button primary" type="submit">${icon('check')} Besluit opslaan</button></form>`); return;
OmniScout-preview.html:1786: trailing whitespace.
+    }
OmniScout-preview.html:1787: trailing whitespace.
+    if (action === 'compare') {
OmniScout-preview.html:1788: trailing whitespace.
+      const ds = [...comparison].map(id => buildDossier(catalog.players.find(p => p.id === id), catalog));
OmniScout-preview.html:1789: trailing whitespace.
+      const rows = [['Competitie', d => d.competition.name], ['Rol', d => ROLE_LABELS[d.player.role]], ['Leeftijd', d => `${d.age} jaar`], ['Minuten', d => fmt(d.minutes, 0)], ['Rolmeting', d => `${d.roleMetric.label}: ${metric(d)}`], ['Bewijsroute', d => d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'], ['Ontbrekend', d => d.gaps[0]], ['Volgende vraag', d => d.nextQuestion]];
OmniScout-preview.html:1790: trailing whitespace.
+      openAction('Vergelijk bewijs, niet een talentscore', `<p class="form-note">${esc(datasetLabel())}. De statistieken zijn niet gecorrigeerd voor verschillen tussen competities of rollen.</p><div class="table-scroll"><table class="compare-table"><thead><tr><th>Onderdeel</th>${ds.map(d => `<th>${esc(d.player.name)}</th>`).join('')}</tr></thead><tbody>${rows.map(([label, get]) => `<tr><th>${label}</th>${ds.map(d => `<td>${esc(get(d))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`); return;
OmniScout-preview.html:1791: trailing whitespace.
+    }
OmniScout-preview.html:1792: trailing whitespace.
+  } catch (error) { notify(error.message || 'Actie mislukt.', true); } finally { if (el.isConnected) el.disabled = false; }
OmniScout-preview.html:1793: trailing whitespace.
+});
OmniScout-preview.html:1794: trailing whitespace.
+document.addEventListener('submit', async event => {
OmniScout-preview.html:1795: trailing whitespace.
+  const form = event.target; if (!['brief-form', 'create-task-form', 'task-result-form', 'decision-form', 'import-confirm-form', 'import-rollback-form'].includes(form.id)) return;
OmniScout-preview.html:1796: trailing whitespace.
+  event.preventDefault(); const submit = form.querySelector('[type="submit"]'); submit.disabled = true;
OmniScout-preview.html:1797: trailing whitespace.
+  const data = Object.fromEntries(new FormData(form));
OmniScout-preview.html:1798: trailing whitespace.
+  try {
OmniScout-preview.html:1799: trailing whitespace.
+    if (form.id === 'import-confirm-form') {
OmniScout-preview.html:1800: trailing whitespace.
+      if (importUI.busy || !importUI.preview?.valid || !form.querySelector('#import-confirm-check').checked) throw new Error('Controleer het bestand en bevestig de importverklaring eerst.');
OmniScout-preview.html:1801: trailing whitespace.
+      importUI.busy = true; importUI.error = '';
OmniScout-preview.html:1802: trailing whitespace.
+      try {
OmniScout-preview.html:1803: trailing whitespace.
+        const result = await request('/api/import/confirm', 'POST', { payload: importUI.payload, digest: importUI.preview.digest });
OmniScout-preview.html:1804: trailing whitespace.
+        importUI.preview = null; importUI.payload = null;
OmniScout-preview.html:1805: trailing whitespace.
+        importUI.jobs = [result.job, ...importUI.jobs.filter(job => job.id !== result.job.id)];
OmniScout-preview.html:1806: trailing whitespace.
+        await selectDataset('import'); await loadImportJobs();
OmniScout-preview.html:1807: trailing whitespace.
+        notify('Importopdracht geregistreerd. De taakstatus toont of verwerking is geslaagd.');
OmniScout-preview.html:1808: trailing whitespace.
+      } catch (error) { importUI.error = `${error.message} Kies het bestand opnieuw voor een actuele controle.`; importUI.preview = null; throw error; }
OmniScout-preview.html:1809: trailing whitespace.
+      finally { importUI.busy = false; if (view === 'import') renderImport(); }
OmniScout-preview.html:1810: trailing whitespace.
+      return;
OmniScout-preview.html:1811: trailing whitespace.
+    }
OmniScout-preview.html:1812: trailing whitespace.
+    if (form.id === 'import-rollback-form') {
OmniScout-preview.html:1813: trailing whitespace.
+      importUI.busy = true;
OmniScout-preview.html:1814: trailing whitespace.
+      try {
OmniScout-preview.html:1815: trailing whitespace.
+        await request('/api/import/rollback', 'POST', { snapshotId: form.dataset.snapshot });
OmniScout-preview.html:1816: trailing whitespace.
+        importUI.rolledBack.add(form.dataset.snapshot); importUI.preview = null;
OmniScout-preview.html:1817: trailing whitespace.
+        await refreshImportCatalog(); await loadImportJobs(); actionDialog.close();
OmniScout-preview.html:1818: trailing whitespace.
+        notify('Rollback bevestigd door de backend. Actieve importcatalogus bijgewerkt.');
OmniScout-preview.html:1819: trailing whitespace.
+      } finally { importUI.busy = false; if (view === 'import') renderImport(); }
OmniScout-preview.html:1820: trailing whitespace.
+      return;
OmniScout-preview.html:1821: trailing whitespace.
+    }
OmniScout-preview.html:1822: trailing whitespace.
+    if (form.id === 'brief-form') {
OmniScout-preview.html:1823: trailing whitespace.
+      const brief = { ...data, minAge: Number(data.minAge), maxAge: Number(data.maxAge) };
OmniScout-preview.html:1824: trailing whitespace.
+      if (brief.minAge > brief.maxAge) throw new Error('Minimumleeftijd mag niet hoger zijn dan maximumleeftijd.');
OmniScout-preview.html:1825: trailing whitespace.
+      if (backend) await request('/api/brief', 'PUT', brief); else { state.brief = brief; addAudit('brief.updated', 'local-brief'); persist(); }
OmniScout-preview.html:1826: trailing whitespace.
+      notify('Clubvraag opgeslagen. Positie en leeftijd kunnen op de radar worden toegepast.');
OmniScout-preview.html:1827: trailing whitespace.
+    }
OmniScout-preview.html:1828: trailing whitespace.
+    if (form.id === 'create-task-form') {
OmniScout-preview.html:1829: trailing whitespace.
+      if (!data.question.trim()) throw new Error('Vul een onderzoeksvraag in.');
OmniScout-preview.html:1830: trailing whitespace.
+      const payload = { playerId: form.dataset.player, question: data.question.trim(), requestId: form.dataset.request };
OmniScout-preview.html:1831: trailing whitespace.
+      if (backend) await request('/api/tasks', 'POST', payload);
OmniScout-preview.html:1832: trailing whitespace.
+      else if (!state.tasks.some(t => t.requestId === payload.requestId)) { const task = { id: newId(), ...payload, status: 'todo', result: '', at: new Date().toISOString(), completedAt: null }; state.tasks.push(task); addAudit('task.created', task.id, task.playerId); persist(); }
OmniScout-preview.html:1833: trailing whitespace.
+      actionDialog.close(); dossierDialog.close(); navigate('tasks'); notify('Onderzoeksopdracht vastgelegd. Er is niets extern verstuurd.');
OmniScout-preview.html:1834: trailing whitespace.
+    }
OmniScout-preview.html:1835: trailing whitespace.
+    if (form.id === 'task-result-form') {
OmniScout-preview.html:1836: trailing whitespace.
+      const payload = { status: form.dataset.status, result: data.result.trim() };
OmniScout-preview.html:1837: trailing whitespace.
+      if (payload.status === 'done' && !payload.result) throw new Error('Beschrijf eerst de waargenomen uitkomst.');
OmniScout-preview.html:1838: trailing whitespace.
+      if (backend) await request(`/api/tasks/${form.dataset.id}`, 'PATCH', payload);
OmniScout-preview.html:1839: trailing whitespace.
+      else { const task = state.tasks.find(t => t.id === form.dataset.id); Object.assign(task, payload, { completedAt: payload.status === 'done' ? new Date().toISOString() : null }); addAudit('task.updated', task.id, payload.status); persist(); }
OmniScout-preview.html:1840: trailing whitespace.
+      actionDialog.close(); render(); notify('Onderzoeksuitkomst opgeslagen.');
OmniScout-preview.html:1841: trailing whitespace.
+    }
OmniScout-preview.html:1842: trailing whitespace.
+    if (form.id === 'decision-form') {
OmniScout-preview.html:1843: trailing whitespace.
+      if (!data.note.trim()) throw new Error('Een geschreven onderbouwing is vereist.');
OmniScout-preview.html:1844: trailing whitespace.
+      await saveDecision(form.dataset.player, data.action, data.reason, data.note.trim()); actionDialog.close(); showDossier(form.dataset.player); render(); notify('Besluit en reden vastgelegd.');
OmniScout-preview.html:1845: trailing whitespace.
+    }
OmniScout-preview.html:1846: trailing whitespace.
+    updateCounts();
OmniScout-preview.html:1847: trailing whitespace.
+  } catch (error) { notify(error.message || 'Opslaan mislukt.', true); } finally { if (submit.isConnected) submit.disabled = false; }
OmniScout-preview.html:1848: trailing whitespace.
+});
OmniScout-preview.html:1849: trailing whitespace.
+for (const dialog of [dossierDialog, actionDialog]) dialog.addEventListener('click', event => { if (event.target === dialog) { const r = dialog.getBoundingClientRect(); if (event.clientX < r.left || event.clientX > r.right || event.clientY < r.top || event.clientY > r.bottom) dialog.close(); } });
OmniScout-preview.html:1850: trailing whitespace.
+document.querySelectorAll('[data-icon]').forEach(el => { el.innerHTML = icon(el.dataset.icon); });
OmniScout-preview.html:1851: trailing whitespace.
+render();
OmniScout-preview.html:1852: trailing whitespace.
+async function init() {
OmniScout-preview.html:1853: trailing whitespace.
+  if (!window.OMNI_INLINE && ['http:', 'https:'].includes(location.protocol)) {
OmniScout-preview.html:1854: trailing whitespace.
+    try {
OmniScout-preview.html:1855: trailing whitespace.
+      const session = await readJSON('/api/session');
OmniScout-preview.html:1856: trailing whitespace.
+      if (session.mode === 'local_single_user_demo' && session.csrf) { backend = session; await selectDataset(preferredDataset === 'import' && canImport() ? 'import' : 'demo', { remember: false }); }
OmniScout-preview.html:1857: trailing whitespace.
+    } catch (error) { if (backend) { workspaceError = error.message; notify(`Lokale gegevens konden niet worden geladen: ${error.message}`, true); } }
OmniScout-preview.html:1858: trailing whitespace.
+  }
OmniScout-preview.html:1859: trailing whitespace.
+  datasetLoading = false;
OmniScout-preview.html:1860: trailing whitespace.
+  if (preferredDataset === 'import' && !canImport()) notify('De bewaarde importkeuze vereist de lokale Node-app. Hier wordt de fictieve demo getoond.');
OmniScout-preview.html:1861: trailing whitespace.
+  render();
OmniScout-preview.html:1862: trailing whitespace.
+  updateCounts();
OmniScout-preview.html:1863: trailing whitespace.
+}
OmniScout-preview.html:1864: trailing whitespace.
+init();
README.md:61: trailing whitespace.
+De lokale sessiebeveiliging is geen productieauthenticatie, tenantisolatie of tamper-proof audit. Gebruik synthetische, niet-vertrouwelijke testgegevens. Accounts/organisatiescheiding, retentie, een aantoonbaar toegestane provider en relevante beoordeling blijven afzonderlijke vervolgstappen. Er is nog geen opensourcelicentie namens de eigenaar verleend.
dist/app.js:1: trailing whitespace.
+import { CATALOG } from '/modules/fixtures.mjs';
dist/app.js:2: trailing whitespace.
+let catalog = CATALOG;
dist/app.js:3: trailing whitespace.
+import { ROLE_LABELS, ACTION_LABELS, REASON_LABELS, COVERAGE_FIELDS, COVERAGE_LABELS, COVERAGE_STATES, buildDossier, findPlayers, dossierCSV } from '/modules/engine.mjs';
dist/app.js:4: trailing whitespace.
+
dist/app.js:5: trailing whitespace.
+const icons = {
dist/app.js:6: trailing whitespace.
+ radar: '<circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="4"/><path d="M12 12l7-7M12 2v2M2 12h2M12 20v2M20 12h2"/>',
dist/app.js:7: trailing whitespace.
+ board: '<rect x="3" y="4" width="18" height="16" rx="3"/><path d="M9 4v16M15 4v16M5 9h2M11 13h2M17 9h2"/>',
dist/app.js:8: trailing whitespace.
+ bookmark: '<path d="M6 4h12v17l-6-4-6 4z"/>',
dist/app.js:9: trailing whitespace.
+ globe: '<circle cx="12" cy="12" r="9"/><ellipse cx="12" cy="12" rx="4" ry="9"/><path d="M3 12h18M5 6h14M5 18h14"/>',
dist/app.js:10: trailing whitespace.
+ sliders: '<path d="M4 6h16M4 12h16M4 18h16"/><circle cx="8" cy="6" r="2"/><circle cx="16" cy="12" r="2"/><circle cx="10" cy="18" r="2"/>',
dist/app.js:11: trailing whitespace.
+ clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',
dist/app.js:12: trailing whitespace.
+ search: '<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',
dist/app.js:13: trailing whitespace.
+ arrow: '<path d="M5 12h14m-6-6 6 6-6 6"/>',
dist/app.js:14: trailing whitespace.
+ close: '<path d="m6 6 12 12M6 18 18 6"/>',
dist/app.js:15: trailing whitespace.
+ plus: '<path d="M12 5v14M5 12h14"/>',
dist/app.js:16: trailing whitespace.
+ check: '<path d="m5 12 4 4L19 6"/>',
dist/app.js:17: trailing whitespace.
+ export: '<path d="M12 3v12m-4-4 4 4 4-4M4 15v5h16v-5"/>',
dist/app.js:18: trailing whitespace.
+ eye: '<path d="M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12z"/><circle cx="12" cy="12" r="3"/>',
dist/app.js:19: trailing whitespace.
+ warning: '<path d="m12 3 10 18H2zM12 9v5M12 17v1"/>',
dist/app.js:20: trailing whitespace.
+ compare: '<path d="M5 3v18M19 3v18M3 7h6M15 17h6M9 7l3 3M15 17l-3-3"/>',
dist/app.js:21: trailing whitespace.
+ file: '<path d="M5 3h10l4 4v14H5zM14 3v5h5M8 12h8M8 16h8"/>'
dist/app.js:22: trailing whitespace.
+};
dist/app.js:23: trailing whitespace.
+const icon = (name, cls = '') => `<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name] || icons.file}</svg>`;
dist/app.js:24: trailing whitespace.
+const esc = value => String(value ?? '').replace(/[&<>"']/g, x => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[x]));
dist/app.js:25: trailing whitespace.
+const fmt = (value, decimals = 1) => value == null || !Number.isFinite(value) ? 'Onbekend' : value.toLocaleString('nl-NL', { maximumFractionDigits: decimals });
dist/app.js:26: trailing whitespace.
+const initials = name => name.split(' ').filter(Boolean).slice(0, 2).map(x => x[0]).join('');
dist/app.js:27: trailing whitespace.
+const dates = value => value ? new Date(value).toLocaleDateString('nl-NL', { day: 'numeric', month: 'short' }) : 'Onbekend';
dist/app.js:28: trailing whitespace.
+const newId = () => globalThis.crypto?.randomUUID?.() || `demo-${Date.now()}-${Math.random().toString(16).slice(2)}`;
dist/app.js:29: trailing whitespace.
+const blankState = () => ({ version: 1, decisions: [], tasks: [], brief: { role: '', minAge: 18, maxAge: 23, task: '', budgetScope: '' }, audit: [] });
dist/app.js:30: trailing whitespace.
+const storageKey = 'omniscout.synthetic-demo.v1';
dist/app.js:31: trailing whitespace.
+const datasetKey = 'omniscout.dataset.v1';
dist/app.js:32: trailing whitespace.
+let state = blankState(), backend = null, storageWorking = true, dataset = 'demo', preferredDataset = 'demo', datasetLoading = true, mutationCount = 0, workspaceError = '';
dist/app.js:33: trailing whitespace.
+try { const saved = JSON.parse(localStorage.getItem(storageKey) || 'null'); if (saved?.version === 1 && ['decisions', 'tasks', 'audit'].every(k => Array.isArray(saved[k]))) state = saved; } catch { storageWorking = false; }
dist/app.js:34: trailing whitespace.
+try { if (localStorage.getItem(datasetKey) === 'import') preferredDataset = 'import'; } catch { /* Dataset preference is optional. */ }
dist/app.js:35: trailing whitespace.
+let demoState = state;
dist/app.js:36: trailing whitespace.
+const importUI = { payload: null, preview: null, filename: '', error: '', busy: false, sequence: 0, jobs: [], snapshots: [], jobsError: '', jobsLoading: false, rolledBack: new Set() };
dist/app.js:37: trailing whitespace.
+let jobsTimer;
dist/app.js:38: trailing whitespace.
+let view = 'radar', filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 23, newOnly: false, lowerOnly: false };
dist/app.js:39: trailing whitespace.
+let comparison = new Set(), openPlayerId = null;
dist/app.js:40: trailing whitespace.
+const main = document.querySelector('#main'), dossierDialog = document.querySelector('#dossier-dialog'), actionDialog = document.querySelector('#action-dialog');
dist/app.js:41: trailing whitespace.
+const viewLabels = { radar: 'Wereldradar', tasks: 'Onderzoeksbord', shortlist: 'Shortlist', coverage: 'Datadekking', import: 'Bronimport', brief: 'Clubvraag', log: 'Beslislogboek' };
dist/app.js:42: trailing whitespace.
+let toastTimeout;
dist/app.js:43: trailing whitespace.
+function notify(message, error = false) { const el = document.querySelector('#toast'); el.textContent = message; el.className = `toast show ${error ? 'error' : ''}`; clearTimeout(toastTimeout); toastTimeout = setTimeout(() => el.classList.remove('show'), 4500); }
dist/app.js:44: trailing whitespace.
+function persist() { if (dataset !== 'demo') throw new Error('Importgegevens worden uitsluitend in de lokale backend opgeslagen.'); demoState = state; try { localStorage.setItem(storageKey, JSON.stringify(state)); } catch { storageWorking = false; notify('Browseropslag niet beschikbaar. Wijzigingen blijven alleen in deze tab.', true); } }
dist/app.js:45: trailing whitespace.
+function addAudit(action, objectId, detail = '') { state.audit.push({ id: newId(), action, objectId, detail, at: new Date().toISOString(), actor: 'browser-demo-user' }); }
dist/app.js:46: trailing whitespace.
+const scopedPath = (path, selected = dataset) => `${path}${path.includes('?') ? '&' : '?'}dataset=${encodeURIComponent(selected)}`;
dist/app.js:47: trailing whitespace.
+const canImport = () => Boolean(backend?.supportsImport && !window.OMNI_INLINE);
dist/app.js:48: trailing whitespace.
+const sourceLabel = source => source?.status === 'synthetic' ? 'Synthetische testimport' : source?.status === 'approved' ? 'Importverklaring · niet onafhankelijk geverifieerd' : 'Bronstatus ontbreekt of is ongeldig';
dist/app.js:49: trailing whitespace.
+const playerLabel = player => dataset === 'demo' ? 'Fictief profiel' : player?.synthetic ? 'Synthetische testimport' : 'Importverklaring · niet onafhankelijk geverifieerd';
dist/app.js:50: trailing whitespace.
+function datasetLabel() { return dataset === 'demo' ? 'Fictieve testgegevens' : !catalog.players.length ? 'Lokale import · geen beschikbare profielen' : catalog.players.every(p => p.synthetic) ? 'Synthetische testimport' : 'Importgegevens · verklaring niet onafhankelijk geverifieerd'; }
dist/app.js:51: trailing whitespace.
+async function readJSON(path) {
dist/app.js:52: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
dist/app.js:53: trailing whitespace.
+  const result = await response.json();
dist/app.js:54: trailing whitespace.
+  if (!response.ok) throw new Error(result.error || 'Lokale gegevens ophalen mislukt.');
dist/app.js:55: trailing whitespace.
+  return result;
dist/app.js:56: trailing whitespace.
+}
dist/app.js:57: trailing whitespace.
+async function refreshWorkspace(selected = dataset) {
dist/app.js:58: trailing whitespace.
+  const result = await readJSON(scopedPath('/api/state', selected));
dist/app.js:59: trailing whitespace.
+  if (dataset === selected) state = result;
dist/app.js:60: trailing whitespace.
+}
dist/app.js:61: trailing whitespace.
+async function request(path, method, data, { refresh = !path.startsWith('/api/import/') } = {}) {
dist/app.js:62: trailing whitespace.
+  if (!backend) throw new Error('Deze actie vereist de lokale Node-backend.');
dist/app.js:63: trailing whitespace.
+  const selected = dataset, workspaceMutation = !path.startsWith('/api/import/');
dist/app.js:64: trailing whitespace.
+  mutationCount++; updateCounts();
dist/app.js:65: trailing whitespace.
+  try {
dist/app.js:66: trailing whitespace.
+    const r = await fetch(workspaceMutation ? scopedPath(path, selected) : path, { method, headers: { 'Content-Type': 'application/json', 'X-Omniscout-CSRF': backend.csrf }, body: JSON.stringify(workspaceMutation ? { ...data, dataset: selected } : data) });
dist/app.js:67: trailing whitespace.
+    const result = await r.json();
dist/app.js:68: trailing whitespace.
+    if (!r.ok) { const error = new Error(result.error || 'Opslaan mislukt.'); error.details = result.details; throw error; }
dist/app.js:69: trailing whitespace.
+    if (refresh) await refreshWorkspace(selected);
dist/app.js:70: trailing whitespace.
+    return result;
dist/app.js:71: trailing whitespace.
+  } finally { mutationCount--; updateCounts(); }
dist/app.js:72: trailing whitespace.
+}
dist/app.js:73: trailing whitespace.
+async function selectDataset(selected, { remember = true } = {}) {
dist/app.js:74: trailing whitespace.
+  if (!['demo', 'import'].includes(selected)) return;
dist/app.js:75: trailing whitespace.
+  if (selected === 'import' && !canImport()) throw new Error('Lokale import vereist de Node-app. De zelfstandige en edge-demo tonen alleen fictieve gegevens.');
dist/app.js:76: trailing whitespace.
+  datasetLoading = true; updateCounts();
dist/app.js:77: trailing whitespace.
+  try {
dist/app.js:78: trailing whitespace.
+    const [nextCatalog, nextState] = backend ? await Promise.all([readJSON(scopedPath('/api/catalog', selected)), readJSON(scopedPath('/api/state', selected))]) : [CATALOG, demoState];
dist/app.js:79: trailing whitespace.
+    dataset = selected; catalog = nextCatalog; state = nextState; workspaceError = '';
dist/app.js:80: trailing whitespace.
+    comparison.clear(); filters = { ...filters, search: '', competition: '', region: '', quality: '' };
dist/app.js:81: trailing whitespace.
+    dossierDialog.close(); actionDialog.close(); openPlayerId = null;
dist/app.js:82: trailing whitespace.
+    if (remember) { preferredDataset = selected; try { localStorage.setItem(datasetKey, selected); } catch { /* Never cache imported data. */ } }
dist/app.js:83: trailing whitespace.
+    render();
dist/app.js:84: trailing whitespace.
+  } finally { datasetLoading = false; updateCounts(); }
dist/app.js:85: trailing whitespace.
+}
dist/app.js:86: trailing whitespace.
+async function saveDecision(playerId, action, reason, note = '') {
dist/app.js:87: trailing whitespace.
+  if (backend) await request('/api/decisions', 'POST', { playerId, action, reason, note });
dist/app.js:88: trailing whitespace.
+  else { const id = newId(); state.decisions.push({ id, playerId, action, reason, note, at: new Date().toISOString() }); addAudit('decision.created', id, `${playerId}: ${action} (${reason})`); persist(); }
dist/app.js:89: trailing whitespace.
+  updateCounts();
dist/app.js:90: trailing whitespace.
+}
dist/app.js:91: trailing whitespace.
+function latestDecision(id) { return state.decisions.filter(x => x.playerId === id).at(-1); }
dist/app.js:92: trailing whitespace.
+function isShortlisted(id) { return latestDecision(id)?.action === 'follow'; }
dist/app.js:93: trailing whitespace.
+function updateCounts() {
dist/app.js:94: trailing whitespace.
+  document.querySelector('#task-count').textContent = state.tasks.filter(t => t.status === 'todo').length;
dist/app.js:95: trailing whitespace.
+  document.querySelector('#shortlist-count').textContent = catalog.players.filter(p => isShortlisted(p.id)).length;
dist/app.js:96: trailing whitespace.
+  document.querySelector('#player-count').textContent = catalog.players.length;
dist/app.js:97: trailing whitespace.
+  document.querySelector('#storage-status').textContent = workspaceError ? 'Backendgegevens niet geladen' : backend ? backend.persistence === 'memory' ? 'Node-backend · tijdelijk geheugen' : 'Lokaal opgeslagen op dit apparaat' : storageWorking ? 'Preview · browseropslag' : 'Preview · tijdelijk geheugen';
dist/app.js:98: trailing whitespace.
+  const picker = document.querySelector('#dataset-select');
dist/app.js:99: trailing whitespace.
+  picker.value = dataset; picker.disabled = datasetLoading || mutationCount > 0;
dist/app.js:100: trailing whitespace.
+  picker.querySelector('[value="import"]').disabled = !canImport();
dist/app.js:101: trailing whitespace.
+  document.querySelector('#dataset-status').textContent = datasetLoading ? 'Gegevens ophalen…' : workspaceError ? 'Backendgegevens konden niet worden geladen. Kies de gegevensset opnieuw om te proberen.' : dataset === 'import' ? 'Importcatalogus en onderzoek blijven in de lokale backend.' : canImport() ? 'Demo en import hebben een afzonderlijk onderzoekslogboek.' : 'Alleen fictieve demo · import vereist de lokale Node-app.';
dist/app.js:102: trailing whitespace.
+  document.querySelector('.date-label').textContent = `PEILDATUM ${dates(catalog.asOf)}`;
dist/app.js:103: trailing whitespace.
+  document.querySelector('.demo-chip').textContent = dataset === 'demo' ? 'DEMO' : 'IMPORT';
dist/app.js:104: trailing whitespace.
+  document.querySelector('.demo-banner p').innerHTML = dataset === 'demo' ? '<strong>Fictieve testomgeving.</strong> Alle spelers, clubs, competities en statistieken zijn voorbeelden. <strong>0 live databronnen.</strong>' : `<strong>${esc(datasetLabel())}.</strong> Bronrechten en inhoud zijn verklaringen van de importeur. Geen live providerverbinding of onafhankelijk geverifieerde scoutingdekking.`;
dist/app.js:105: trailing whitespace.
+}
dist/app.js:106: trailing whitespace.
+function heading(kicker, title, description, actions = '') { return `<div class="page-heading"><div><div class="eyebrow">${esc(kicker)}</div><h1>${esc(title)}</h1><p>${esc(description)}</p></div><div class="heading-actions">${actions}</div></div>`; }
dist/app.js:107: trailing whitespace.
+function empty(title, detail) { return `<div class="empty-state">${icon('radar')}<h3>${esc(title)}</h3><p>${esc(detail)}</p><button class="button secondary" data-action="reset">Toon alle spelers in deze set</button></div>`; }
dist/app.js:108: trailing whitespace.
+function options(obj, current, all) { return `<option value="">${esc(all)}</option>` + Object.entries(obj).map(([key, label]) => `<option value="${esc(key)}" ${String(current) === key ? 'selected' : ''}>${esc(label)}</option>`).join(''); }
dist/app.js:109: trailing whitespace.
+function qualityTag(d) { return `<span class="status-tag ${d.quality === 'documented' ? 'green' : 'amber'}"><i></i>${d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'}</span>`; }
dist/app.js:110: trailing whitespace.
+function metric(d) { return d.roleMetric ? `${fmt(d.metrics[d.roleMetric.metric])}${d.metrics[d.roleMetric.metric] == null ? '' : ' ' + d.roleMetric.unit}` : 'Onbekend'; }
dist/app.js:111: trailing whitespace.
+function worldMap() {
dist/app.js:112: trailing whitespace.
+  const regions = [ ['Noord-Amerika', 151, 112, 'NOORD-AMERIKA'], ['Zuid-Amerika', 250, 208, 'ZUID-AMERIKA'], ['Europa', 385, 76, 'EUROPA'], ['Afrika', 369, 160, 'AFRIKA'], ['Azië', 558, 98, 'AZIË'], ['Oceanië', 603, 226, 'OCEANIË'] ];
dist/app.js:113: trailing whitespace.
+  return `<div class="world-card panel"><div class="panel-head"><div><span class="eyebrow">WERELDWIJD ONTDEKKEN</span><h2>Niet elke kans staat al op de radar.</h2></div><span class="status-tag neutral">${dataset === 'demo' ? 'SCHEMA + TESTDATA' : 'LOKALE IMPORT'}</span></div>
dist/app.js:114: trailing whitespace.
+   <div class="map-wrap"><svg viewBox="0 0 720 285" class="world-map" role="img" aria-label="Schematisch overzicht van de geselecteerde dataset. Geen kaart van live scoutingdekking."><defs><pattern id="grid" width="36" height="28" patternUnits="userSpaceOnUse"><path d="M36 0H0V28" fill="none" stroke="#274357" stroke-width=".5"/></pattern><pattern id="dots" width="5" height="5" patternUnits="userSpaceOnUse"><circle cx="1" cy="1" r=".9" fill="#36526a"/></pattern><radialGradient id="glow"><stop stop-color="#a6f27a" stop-opacity=".13"/><stop offset="1" stop-color="#a6f27a" stop-opacity="0"/></radialGradient></defs><rect width="720" height="285" fill="url(#grid)" opacity=".5"/><ellipse cx="385" cy="140" rx="190" ry="140" fill="url(#glow)"/>
dist/app.js:115: trailing whitespace.
+    <g fill="url(#dots)" stroke="#3e596c" stroke-width=".65" opacity=".85"><path d="M48 70l20-23 44-9 23-17 53 9 35 25-21 22-16 29-28 2-9 26-24 2 2 22 32 20 21 1 12 15-19 7-19-12-22-8-24-26-22-25-28-13z"/><path d="m244 193 28-14 28 15 12 23-17 29-11 30-14-11-9-34-16-18z"/><path d="m248 17 35-9 23 12-14 26-32 3-14-13z"/><path d="m337 70 21-20 12 9 15-16 14 9 13-23 16 16-1 24-29 6-14 15-9 18-34-6 6-17z"/><path d="m340 115 20-17 40 9 27 24 5 25-28 24-9 30-18-4-12-29-27-14-12-28z"/><path d="m418 43 36-11 26 9 18-16 36 9 45-1 27 17 27-4 30 22-23 18-29 5-8 17-31-3-3 28-26 21-14-12-13 14-19-6-14-29-30-12-13-15-22-3-22-14z"/><path d="m545 151 12 8 14 17 27 8 8 11-28-2-28-20z"/><path d="m579 209 26-13 33 8 8 24-15 21-30-5-27-15z"/><path d="m665 246 12-11 3 15-15 19z"/><path d="m409 194 7-5 3 16-8 18-5-12z"/></g>
dist/app.js:116: trailing whitespace.
+    <g stroke="#94d675" stroke-opacity=".2" fill="none" stroke-dasharray="3 7"><path d="M385 76Q223 3 151 112M385 76Q291 139 250 208M385 76Q390 102 369 160M385 76Q462 21 558 98M385 76Q553 150 603 226"/></g>
dist/app.js:117: trailing whitespace.
+    ${regions.map(([name, x, y, label]) => { const count = catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length; return `<g><circle cx="${x}" cy="${y}" r="14" fill="${count ? '#aff57d' : '#64798b'}" opacity=".1"/><circle cx="${x}" cy="${y}" r="5" fill="${count ? '#b8f384' : '#64798b'}"/><text x="${x + 13}" y="${y - 2}" class="map-label">${label}</text><text x="${x + 13}" y="${y + 13}" class="map-value">${count ? `${count} spelers` : 'Niet aangesloten'}</text></g>`; }).join('')}
dist/app.js:118: trailing whitespace.
+   </svg></div><div class="region-pills">${regions.map(([name]) => `<button data-region="${esc(name)}">${esc(name)}<b>${catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length}</b></button>`).join('')}</div><div class="map-footer"><span><i class="legend-dot"></i>${catalog.competitions.filter(c => catalog.players.some(p => p.competitionId === c.id)).length} competities met spelers</span><span><i class="legend-dot muted"></i>${catalog.competitions.filter(c => !catalog.players.some(p => p.competitionId === c.id)).length} zonder spelersdata</span><button class="text-button" data-view="coverage">Alle dekking ${icon('arrow')}</button></div></div>`;
dist/app.js:119: trailing whitespace.
+}
dist/app.js:120: trailing whitespace.
+function tableRows(dossiers) {
dist/app.js:121: trailing whitespace.
+  return dossiers.map((d, index) => `<tr><td class="check-cell"><input type="checkbox" aria-label="Vergelijk ${esc(d.player.name)}" data-compare="${esc(d.player.id)}" ${comparison.has(d.player.id) ? 'checked' : ''}></td><td><button class="player-link" data-open="${esc(d.player.id)}"><span class="player-avatar variant-${index % 4}">${esc(initials(d.player.name))}</span><span><strong>${esc(d.player.name)}</strong><small>${esc(d.player.club)}</small>${dataset === 'import' ? `<small>${esc(playerLabel(d.player))}</small>` : ''}<small class="mobile-player-meta">${esc(d.player.role)} · ${d.age} jaar · ${esc(d.competition.country)} · niveau ${d.competition.tier}</small></span></button></td><td><span class="role-label">${esc(d.player.role)}</span><small>${d.age} jaar · ${esc(d.player.preferredFoot)}</small></td><td><strong class="cell-normal">${esc(d.competition.country)}</strong><small>Niveau ${esc(d.competition.tier)} · ${esc(d.competition.name)}</small></td><td><strong class="numeric">${esc(metric(d))}</strong><small>${esc(d.roleMetric?.label || 'Rolgegevens')}</small></td><td><strong class="cell-normal">${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></td><td>${qualityTag(d)}<small>${d.minutesChange !== null && d.minutesChange >= 120 ? `+${d.minutesChange} min · gelijke vensters` : d.quality === 'exploration' ? 'Aanvullend onderzoek nodig' : 'Geen toekomstvoorspelling'}</small></td><td><button class="icon-button ${isShortlisted(d.player.id) ? 'selected' : ''}" data-save="${esc(d.player.id)}" aria-label="${isShortlisted(d.player.id) ? 'Verwijder van shortlist' : 'Zet op shortlist'}: ${esc(d.player.name)}">${icon('bookmark')}</button><button class="icon-button" data-open="${esc(d.player.id)}" aria-label="Open dossier ${esc(d.player.name)}">${icon('arrow')}</button></td></tr>`).join('');
dist/app.js:122: trailing whitespace.
+}
dist/app.js:123: trailing whitespace.
+function playerTable(dossiers) {
dist/app.js:124: trailing whitespace.
+  return dossiers.length ? `<div class="table-scroll"><table class="player-table"><thead><tr><th aria-label="Vergelijken"></th><th>SPELER / CLUB</th><th>ROL / LEEFTIJD</th><th>COMPETITIE</th><th>ROLSIGNAAL</th><th>MINUTEN</th><th>BEWIJSROUTE</th><th aria-label="Acties"></th></tr></thead><tbody>${tableRows(dossiers)}</tbody></table></div>` : empty('Geen kandidaten binnen deze filters', 'Dit resultaat geldt alleen voor de geselecteerde gegevensset. Het zegt niets over talent in niet-aangesloten competities.');
dist/app.js:125: trailing whitespace.
+}
dist/app.js:126: trailing whitespace.
+function renderRadar(shortlist = false) {
dist/app.js:127: trailing whitespace.
+  const all = findPlayers(catalog, {}, state), documented = all.filter(d => d.quality === 'documented').length;
dist/app.js:128: trailing whitespace.
+  const current = findPlayers(catalog, { ...filters, shortlistOnly: shortlist }, state);
dist/app.js:129: trailing whitespace.
+  const regions = Object.fromEntries([...new Set(catalog.competitions.map(c => c.region))].map(x => [x, x]));
dist/app.js:130: trailing whitespace.
+  const compOptions = Object.fromEntries(catalog.competitions.map(c => [c.id, `${c.country} · ${c.name}`]));
dist/app.js:131: trailing whitespace.
+  main.innerHTML = heading(shortlist ? 'JOUW SCOUTINGSELECTIE' : 'VERBORGEN TALENT · ZICHTBAAR BEWIJS', shortlist ? 'Jouw shortlist.' : 'Zie wat anderen missen.', shortlist ? 'Opgeslagen onderzoekskandidaten. Een shortlist is geen contractbeslissing.' : 'Een wereld aan mogelijkheden. Eén werkplek voor gericht scoutingonderzoek.', `<button class="button secondary" data-action="export">${icon('export')} Export selectie</button><button class="button primary" data-view="brief">${icon('sliders')} Clubvraag instellen</button>`) +
dist/app.js:132: trailing whitespace.
+  (!shortlist ? `<div class="kpi-grid"><div class="kpi-card"><span class="kpi-label">${dataset === 'demo' ? 'SPELERS IN DE TESTSET' : 'BESCHIKBARE IMPORTSPELERS'} ${icon('eye')}</span><div class="kpi-number">${all.length}<span>${esc(datasetLabel())}</span></div><p>${catalog.competitions.length} competities in deze gegevensset</p></div><div class="kpi-card"><span class="kpi-label">MEER ONDERLIGGEND BEWIJS ${icon('file')}</span><div class="kpi-number">${documented}<span class="green-text">onderzoekskandidaten</span></div><p>Voldoende minuten voor de voorbeeldregel</p></div><div class="kpi-card"><span class="kpi-label">VERKENNINGSROUTE ${icon('radar')}</span><div class="kpi-number">${all.length - documented}<span class="amber-text">nog nader te bekijken</span></div><p>Weinig data betekent niet weinig talent</p></div></div><div class="radar-top-grid">${worldMap()}<div class="focus-card panel"><div class="eyebrow">ONDERZOEK, GEEN GOKWERK</div><div class="focus-orbit">${icon('radar')}</div><h2>De beste volgende<br>vraag.</h2><p>Geen onverklaarbare talentscore. Wel de bron, het tegenbewijs en wat je nu moet onderzoeken.</p><div class="focus-steps"><span><b>01</b>Ontdek een relevant signaal</span><span><b>02</b>Controleer wat nog ontbreekt</span><span><b>03</b>Geef de scout een gerichte taak</span></div><button class="text-button" data-view="tasks">Open onderzoeksbord ${icon('arrow')}</button></div></div>` : '') +
dist/app.js:133: trailing whitespace.
+  `<section class="panel candidates"><div class="candidates-top"><div><h2>${shortlist ? 'Opgeslagen spelers' : 'Ontdekkingsradar'} <span class="count-badge">${current.length}</span></h2><p>Gesorteerd op beschikbare informatie. Niet op voorspeld talent.</p></div><button class="button secondary compact" id="compare-button" data-action="compare" ${comparison.size < 2 ? 'disabled' : ''}>${icon('compare')} Vergelijk <span>${comparison.size}/3</span></button></div>
dist/app.js:134: trailing whitespace.
+    <div class="filters"><label class="search-input">${icon('search')}<input id="search" data-filter="search" type="search" value="${esc(filters.search)}" placeholder="Zoek speler, club of land" aria-label="Zoek speler, club of land"></label><label class="select-filter"><span class="sr-only">Positie</span><select data-filter="role" aria-label="Positie">${options(ROLE_LABELS, filters.role, 'Alle posities')}</select></label><label class="select-filter"><span class="sr-only">Regio</span><select data-filter="region" aria-label="Regio">${options(regions, filters.region, 'Alle regio’s')}</select></label><label class="age-filter"><span>Leeftijd</span><input type="number" min="18" max="60" data-filter="minAge" aria-label="Minimumleeftijd" value="${esc(filters.minAge)}"><span>–</span><input type="number" min="18" max="60" data-filter="maxAge" aria-label="Maximumleeftijd" value="${esc(filters.maxAge)}"></label></div>
dist/app.js:135: trailing whitespace.
+    <div class="filter-secondary"><div class="segment-control"><button data-quality="" class="${filters.quality === '' ? 'active' : ''}">Alle kandidaten</button><button data-quality="documented" class="${filters.quality === 'documented' ? 'active' : ''}">Meer bewijs</button><button data-quality="exploration" class="${filters.quality === 'exploration' ? 'active' : ''}">Verkenning</button></div><div class="quick-filters"><label><input type="checkbox" data-filter="lowerOnly" ${filters.lowerOnly ? 'checked' : ''}> Niveau 3 en lager</label><label><input type="checkbox" data-filter="newOnly" ${filters.newOnly ? 'checked' : ''}> Nieuw voor onze club</label></div></div>
dist/app.js:136: trailing whitespace.
+    ${filters.competition ? `<div class="active-filter">Competitie: ${esc(compOptions[filters.competition])}<button data-action="clear-competition">${icon('close')} Wissen</button></div>` : ''}
dist/app.js:137: trailing whitespace.
+    ${playerTable(current)}<div class="table-foot"><span><i class="legend-dot"></i>${esc(datasetLabel())} · Statistieken zijn niet tussen competities genormaliseerd</span><span>Peildatum ${dates(catalog.asOf)}</span></div></section>`;
dist/app.js:138: trailing whitespace.
+}
dist/app.js:139: trailing whitespace.
+function renderCoverage() {
dist/app.js:140: trailing whitespace.
+  main.innerHTML = heading('TRANSPARANT OVER WAT WE WETEN', 'Dekking, zonder blinde claims.', 'Een competitienaam in de database is geen volledige spelersanalyse.') + `<div class="notice-card">${icon('warning')}<div><strong>${esc(datasetLabel())}.</strong><p>${dataset === 'demo' ? 'Onderstaande statussen demonstreren het dekkingsregister met fictieve competities. 0 echte databronnen aangesloten.' : 'Onderstaande bronrechten en dekkingsvelden zijn aangeleverd door de importeur; ze zijn niet onafhankelijk geverifieerd. Geen live providerverbinding.'}</p></div></div><section class="panel"><div class="panel-head"><div><span class="eyebrow">DEKKINGSREGISTER</span><h2>Per bron. Per competitie. Per gegevenstype.</h2></div></div><div class="table-scroll"><table class="coverage-table"><thead><tr><th>COMPETITIE</th><th>NIVEAU</th>${COVERAGE_FIELDS.map(k => `<th>${esc(COVERAGE_LABELS[k])}</th>`).join('')}<th>ACTUALITEIT</th></tr></thead><tbody>${catalog.competitions.map(c => `<tr><td><button class="coverage-link" data-competition="${esc(c.id)}">${esc(c.country)} ${icon('arrow')}</button><small>${esc(c.name)} · ${esc(c.season || 'Seizoen onbekend')} · ${esc(dataset === 'demo' ? 'fictief' : sourceLabel(catalog.sources.find(s => s.id === c.sourceId)))}</small></td><td>${esc(c.tier)}</td>${COVERAGE_FIELDS.map(k => `<td><span class="coverage-dot ${esc(c.coverage[k])}" title="${esc(COVERAGE_STATES[c.coverage[k]])}" aria-label="${esc(COVERAGE_LABELS[k] + ': ' + COVERAGE_STATES[c.coverage[k]])}"><span class="sr-only">${esc(COVERAGE_STATES[c.coverage[k]])}</span>${c.coverage[k] === 'available' ? '✓' : c.coverage[k] === 'partial' ? '◐' : '—'}</span></td>`).join('')}<td>${dates(c.lastReceivedAt)}<small>${fmt(c.observedPlayers, 0)} spelers</small></td></tr>`).join('')}</tbody></table></div><div class="coverage-legend">${Object.entries(COVERAGE_STATES).map(([k, v]) => `<span><i class="coverage-dot ${k}"></i>${esc(v)}</span>`).join('')}</div></section><div class="info-grid"><div class="panel info-panel"><h3>Onbekend blijft onbekend</h3><p>Geen minuten betekent geen berekening per 90 minuten. Geen volledige noemer betekent geen dekkingspercentage. Een niet-aangesloten competitie blijft zichtbaar als een gat.</p></div><div class="panel info-panel"><h3>Bronrechten vóór verwerking</h3><p>Voor bronnen zijn rechten nodig voor ophalen, bewaren, weergeven, analyse en export. De importeur verklaart deze rechten; de toepassing controleert velden en vervaldatums, geen externe overeenkomsten. Er worden geen externe verbindingen gelegd.</p></div></div>`;
dist/app.js:141: trailing whitespace.
+}
dist/app.js:142: trailing whitespace.
+function taskCard(task) {
dist/app.js:143: trailing whitespace.
+  const p = catalog.players.find(p => p.id === task.playerId);
dist/app.js:144: trailing whitespace.
+  return `<article class="task-card"><div class="task-card-head"><span class="status-tag ${task.status === 'done' ? 'green' : 'amber'}">${task.status === 'done' ? 'Afgerond' : 'Te onderzoeken'}</span><span>${dates(task.at)}</span></div><button class="task-player" data-open="${esc(task.playerId)}">${esc(p?.name || task.playerId)} ${icon('arrow')}</button><p>${esc(task.question)}</p>${task.result ? `<div class="task-result"><strong>Waargenomen uitkomst</strong><p>${esc(task.result)}</p></div>` : ''}<div class="task-card-bottom"><span>Interne opdracht · niet verstuurd</span><button class="button secondary compact" data-task="${esc(task.id)}">${task.status === 'done' ? 'Heropenen' : 'Uitkomst vastleggen'}</button></div></article>`;
dist/app.js:145: trailing whitespace.
+}
dist/app.js:146: trailing whitespace.
+function renderTasks() {
dist/app.js:147: trailing whitespace.
+  const todo = state.tasks.filter(t => t.status !== 'done'), done = state.tasks.filter(t => t.status === 'done');
dist/app.js:148: trailing whitespace.
+  main.innerHTML = heading('VAN SIGNAAL NAAR WAARNEMING', 'Onderzoek met een doel.', 'Leg eerst de vraag vast. Beoordeel daarna wat het nieuwe bewijs werkelijk verandert.', `<button class="button primary" data-view="radar">${icon('plus')} Kies een kandidaat</button>`) + `<div class="board"><section class="board-column"><div class="board-title"><span class="legend-dot amber-dot"></span><h2>Te onderzoeken</h2><span class="count-badge">${todo.length}</span></div>${todo.length ? todo.map(taskCard).join('') : '<div class="board-empty">Nog geen opdrachten. Open een spelersdossier en kies “Maak onderzoeksopdracht”.</div>'}</section><section class="board-column"><div class="board-title"><span class="legend-dot"></span><h2>Uitkomst vastgelegd</h2><span class="count-badge">${done.length}</span></div>${done.length ? done.map(taskCard).join('') : '<div class="board-empty">Een afgeronde opdracht krijgt een geschreven uitkomst. Een klik alleen is geen bewijs.</div>'}</section></div>`;
dist/app.js:149: trailing whitespace.
+}
dist/app.js:150: trailing whitespace.
+function renderBrief() {
dist/app.js:151: trailing whitespace.
+  const b = state.brief;
dist/app.js:152: trailing whitespace.
+  main.innerHTML = heading('BEGIN BIJ DE CLUBVRAAG', 'Welke rol ontbreekt?', 'Benoem taken en grenzen. De scout bepaalt hoe de informatie wordt gewogen.') + `<div class="brief-grid"><form id="brief-form" class="panel form-panel"><div class="panel-head"><h2>Recruitmentprofiel</h2><span class="status-tag neutral">${dataset === 'demo' ? 'LOKALE DEMO' : 'LOKALE IMPORT'}</span></div><label>Gezochte positie<select name="role">${options(ROLE_LABELS, b.role, 'Nog niet beperkt')}</select></label><div class="form-row"><label>Minimumleeftijd<input required name="minAge" type="number" min="18" max="60" value="${b.minAge}"></label><label>Maximumleeftijd<input required name="maxAge" type="number" min="18" max="60" value="${b.maxAge}"></label></div><label>Welke taken moet deze speler vervullen?<textarea name="task" maxlength="2400" rows="5" placeholder="Bijvoorbeeld: ruimte achter een hoge verdediging verdedigen en onder druk vooruit spelen.">${esc(b.task)}</textarea></label><label>Budgetscope en nog te verifiëren voorwaarden<textarea name="budgetScope" maxlength="1000" rows="3" placeholder="Bijvoorbeeld: totaalbudget inclusief salaris, niet alleen transfersom. Onbekende bedragen blijven onbekend.">${esc(b.budgetScope)}</textarea></label><p class="form-note">De tekst wordt bewaard als menselijke onderzoeksinstructie. Deze versie vertaalt de tekst niet met AI naar een automatische rolscore.</p><div class="form-actions"><button class="button primary" type="submit">${icon('check')} Profiel opslaan</button><button class="button secondary" type="button" data-action="apply-brief">Gebruik positie en leeftijd</button></div></form><div class="panel info-panel brief-explanation"><span class="eyebrow">VIER APARTE VRAGEN</span><h2>Geen alles-in-één<br>talentcijfer.</h2><div class="principle"><b>01</b><div><strong>Wat zien we nu?</strong><p>Prestaties en waarnemingen met hun bron.</p></div></div><div class="principle"><b>02</b><div><strong>Wat kan veranderen?</strong><p>Een ontwikkelhypothese is nog geen bewezen groeipad.</p></div></div><div class="principle"><b>03</b><div><strong>Past de gevraagde taak?</strong><p>Dit beoordeelt de scout met passend bewijs.</p></div></div><div class="principle"><b>04</b><div><strong>Is de overstap haalbaar?</strong><p>Budget, beschikbaarheid en registratie blijven afzonderlijk te controleren.</p></div></div></div></div>`;
dist/app.js:153: trailing whitespace.
+}
dist/app.js:154: trailing whitespace.
+function renderLog() {
dist/app.js:155: trailing whitespace.
+  const actions = { 'decision.created': 'Scoutbesluit vastgelegd', 'task.created': 'Onderzoek aangemaakt', 'task.updated': 'Onderzoek bijgewerkt', 'brief.updated': 'Clubvraag gewijzigd' };
dist/app.js:156: trailing whitespace.
+  main.innerHTML = heading('HET GEHEUGEN VAN JE SCOUTING', 'Bewaar de reden.', 'Een budgetafwijzing is geen negatief talentlabel. Alle wijzigingen blijven als nieuwe gebeurtenissen zichtbaar.', `<button class="button secondary" data-action="export-state">${icon('export')} Exporteer logboek</button>`) + `<section class="panel log-panel">${state.audit.length ? `<div class="timeline">${[...state.audit].reverse().map(e => `<article class="timeline-item"><span class="timeline-icon">${icon(e.action.startsWith('task') ? 'board' : e.action.startsWith('brief') ? 'sliders' : 'bookmark')}</span><div><h3>${esc(actions[e.action] || e.action)}</h3><p>${esc(e.detail || 'Lokaal onderzoeksprofiel opgeslagen')}</p><small>${esc(new Date(e.at).toLocaleString('nl-NL'))} · ${esc(e.actor)}</small></div></article>`).join('')}</div>` : '<div class="board-empty">Nog geen scoutbesluiten of onderzoeksacties. Het logboek wordt gevuld zodra je een actie opslaat.</div>'}<div class="table-foot">Dit lokale logboek is geen productie-audit met onafhankelijk gecontroleerde integriteit.</div></section>`;
dist/app.js:157: trailing whitespace.
+}
dist/app.js:158: trailing whitespace.
+function importIssues(items, title, kind) {
dist/app.js:159: trailing whitespace.
+  if (!Array.isArray(items) || !items.length) return '';
dist/app.js:160: trailing whitespace.
+  return `<section class="import-issues ${kind}" aria-label="${esc(title)}"><h3>${esc(title)} (${items.length})</h3><ul>${items.slice(0, 100).map(item => `<li><code>${esc(item.path)}</code> ${esc(item.message)}</li>`).join('')}</ul>${items.length > 100 ? '<p>De eerste 100 meldingen worden getoond. Herstel deze en controleer opnieuw.</p>' : ''}</section>`;
dist/app.js:161: trailing whitespace.
+}
dist/app.js:162: trailing whitespace.
+function importSummary() {
dist/app.js:163: trailing whitespace.
+  if (!importUI.preview) return '';
dist/app.js:164: trailing whitespace.
+  const payload = importUI.payload && typeof importUI.payload === 'object' ? importUI.payload : {}, preview = importUI.preview, summary = preview.summary || {}, source = summary.source || payload.source || {};
dist/app.js:165: trailing whitespace.
+  const competitions = Array.isArray(payload.competitions) ? payload.competitions : [], players = Array.isArray(payload.players) ? payload.players : [];
dist/app.js:166: trailing whitespace.
+  const timestamps = ['eventAt', 'publishedAt', 'retrievedAt', 'availableAt'];
dist/app.js:167: trailing whitespace.
+  const timeLabels = { eventAt: 'Gebeurtenis', publishedAt: 'Gepubliceerd', retrievedAt: 'Opgehaald', availableAt: 'Beschikbaar' };
dist/app.js:168: trailing whitespace.
+  const stampRange = key => {
dist/app.js:169: trailing whitespace.
+    const values = players.flatMap(p => [p?.[key], p?.stats?.[key]]).filter(value => typeof value === 'string').sort();
dist/app.js:170: trailing whitespace.
+    return values.length ? `${values[0]} — ${values.at(-1)}` : 'Onbekend';
dist/app.js:171: trailing whitespace.
+  };
dist/app.js:172: trailing whitespace.
+  return `<section class="panel import-preview" id="import-preview" tabindex="-1"><div class="panel-head"><div><span class="eyebrow">CONTROLE ZONDER OPSLAG</span><h2>${preview.valid ? 'Bestand voldoet aan de importcontroles' : 'Import geblokkeerd'}</h2></div><span class="status-tag ${preview.valid ? 'green' : 'amber'}">${preview.valid ? 'Klaar voor bevestiging' : 'Herstel fouten / conflicten'}</span></div>
dist/app.js:173: trailing whitespace.
+    <div class="import-preview-body"><p class="muted-copy">${esc(sourceLabel(source))}. Een geslaagde controle verifieert geen externe bronrechten of voetbalclaims.</p>
dist/app.js:174: trailing whitespace.
+    <dl class="import-summary"><div><dt>Snapshot</dt><dd>${esc(payload.snapshotId)}</dd></div><div><dt>Correctie van</dt><dd>${esc(payload.correctionOf || 'Geen')}</dd></div><div><dt>Peildatum</dt><dd>${esc(payload.asOf)}</dd></div><div><dt>Aantallen in upload</dt><dd>${esc(summary.playerCount ?? players.length)} spelers · ${esc(summary.competitionCount ?? competitions.length)} competities · ${esc(summary.sourceCount ?? (payload.source ? 1 : 0))} bron</dd></div><div><dt>Bron</dt><dd>${esc(source.name)} (${esc(source.id)})</dd></div><div><dt>Toegestane verwerking</dt><dd>${esc(Array.isArray(source.allowedUses) ? source.allowedUses.join(', ') : 'Ontbreekt')}</dd></div><div><dt>Rechten verklaard</dt><dd>${source.rightsAttested === true ? 'Ja, volgens de importeur' : 'Nee'}</dd></div><div><dt>Rechten geldig vanaf</dt><dd>${esc(source.validFrom || 'Ontbreekt')}</dd></div><div><dt>Rechten vervallen</dt><dd>${esc(source.expiresAt === null && source.status === 'synthetic' ? 'Geen vervaldatum (synthetische bron)' : source.expiresAt || 'Ontbreekt')}</dd></div><div><dt>Rechtennotitie</dt><dd>${esc(source.rightsNote || 'Ontbreekt')}</dd></div>${timestamps.map(key => `<div><dt>${timeLabels[key]} (bereik)</dt><dd>${esc(stampRange(key))}</dd></div>`).join('')}</dl>
dist/app.js:175: trailing whitespace.
+    <p class="form-note">${esc(summary.missingMinutes ?? players.filter(p => p?.stats?.minutes === null).length)} spelers met onbekende minuten; deze krijgen geen per-90-meting. ${summary.idempotent ? 'Deze ongewijzigde snapshot bestaat al: herhaling maakt geen dubbele waarnemingen.' : ''}</p><h3>Competities en seizoenen</h3><ul class="import-seasons">${competitions.slice(0, 100).map(c => `<li>${esc(c?.name)} · ${esc(c?.season || 'Seizoen ontbreekt')} · ${esc(c?.country)} · ontvangen ${esc(c?.lastReceivedAt || 'Onbekend')}</li>`).join('') || '<li>Geen competities aangeleverd.</li>'}</ul>
dist/app.js:176: trailing whitespace.
+    ${importIssues(preview.errors, 'Blokkerende fouten en identiteitsconflicten', 'error')}${importIssues(preview.warnings, 'Waarschuwingen — geen automatische samenvoeging', 'warning')}
dist/app.js:177: trailing whitespace.
+    ${preview.valid ? `<form id="import-confirm-form"><label class="import-consent"><input type="checkbox" id="import-confirm-check" required><span>Ik heb de bron, rechtenverklaring, peildatum en waarschuwingen gecontroleerd en wil deze snapshot in de lokale importdataset opslaan.</span></label><button type="submit" class="button primary" id="import-confirm" ${importUI.busy ? 'disabled' : ''}>${icon('check')} Bevestig lokale import</button><p class="form-note">Pas na bevestiging wordt een verwerkingsopdracht aangemaakt. De backend controleert rechten en identiteit opnieuw.</p></form>` : '<p class="form-note">Er is niets geïmporteerd. Corrigeer het bronbestand en kies het opnieuw om de controle te herhalen.</p>'}</div></section>`;
dist/app.js:178: trailing whitespace.
+}
dist/app.js:179: trailing whitespace.
+function renderImportJobs() {
dist/app.js:180: trailing whitespace.
+  const host = document.querySelector('#import-jobs'); if (!host) return;
dist/app.js:181: trailing whitespace.
+  const statusLabels = { queued: 'In wachtrij', running: 'Wordt verwerkt', succeeded: 'Geslaagd', failed: 'Mislukt' };
dist/app.js:182: trailing whitespace.
+  host.innerHTML = `${importUI.jobsError ? `<p class="import-error" role="alert">${esc(importUI.jobsError)}</p>` : ''}${importUI.jobsLoading && !importUI.jobs.length ? '<p class="muted-copy">Werkelijke taakstatus ophalen…</p>' : !importUI.jobs.length ? '<p class="muted-copy">Nog geen importopdrachten in deze lokale backend.</p>' : ''}${[...importUI.jobs].reverse().map(job => `<article class="import-job" data-job-id="${esc(job.id)}" data-job-status="${esc(job.status)}"><div class="import-job-heading"><h3>${esc(job.snapshotId)}</h3><span class="status-tag ${job.status === 'succeeded' ? 'green' : 'amber'}">${esc(statusLabels[job.status] || job.status)}</span></div><p class="muted-copy">Poging ${esc(job.attempts)} / ${esc(job.maxAttempts)} · Bijgewerkt ${esc(job.updatedAt)}</p><small>Taak ${esc(job.id)}</small>${job.error ? `<p class="import-error">${esc(typeof job.error === 'string' ? job.error : job.error.message)}</p>${importIssues(job.error.details, 'Foutdetails', 'error')}` : ''}<div class="form-actions">${job.status === 'failed' && job.attempts < job.maxAttempts ? `<button class="button secondary" data-import-retry="${esc(job.id)}" ${importUI.busy ? 'disabled' : ''}>Opnieuw proberen</button>` : ''}${job.status === 'succeeded' ? (snapshotStatus(job.snapshotId) === 'rolled_back' || importUI.rolledBack.has(job.snapshotId)) ? '<p class="muted-copy">Snapshot teruggedraaid · backendgeschiedenis blijft bewaard.</p>' : snapshotStatus(job.snapshotId) === 'superseded' ? '<p class="muted-copy">Vervangen door een correctie. Draai eerst die correctie terug.</p>' : `<button class="button secondary" data-import-rollback="${esc(job.snapshotId)}" ${importUI.busy ? 'disabled' : ''}>Snapshot terugdraaien</button>` : ''}</div></article>`).join('')}`;
dist/app.js:183: trailing whitespace.
+}
dist/app.js:184: trailing whitespace.
+function snapshotStatus(snapshotId) { return importUI.snapshots.find(snapshot => snapshot.snapshotId === snapshotId)?.status; }
dist/app.js:185: trailing whitespace.
+function renderImport() {
dist/app.js:186: trailing whitespace.
+  main.innerHTML = heading('LOKALE BRONIMPORT', 'Controleer. Bevestig. Volg.', 'Importeer één JSON-snapshot met expliciete bronrechten, seizoenen, provideridentiteiten en tijdstempels.') + (!canImport() ? `<section class="panel info-panel"><h2>Start de lokale Node-app voor bronimport</h2><p>De zelfstandige HTML-preview en de edge-demo bevatten alleen synthetische testgegevens. Import, taakverwerking en rollback vereisen de lokale backend.</p><p>Open de toepassing via het adres dat <code>npm start</code> toont. Je huidige demogegevens blijven in de fictieve gegevensset.</p></section>` : `<section class="panel form-panel import-upload"><div class="panel-head"><h2>1. Kies en controleer een bronbestand</h2><button class="button secondary compact" data-action="import-sample">${icon('export')} Synthetisch voorbeeld</button></div><label for="import-file">JSON-bestand · maximaal 1 MiB<input type="file" id="import-file" accept=".json,application/json" ${importUI.busy ? 'disabled' : ''} aria-describedby="import-file-help"></label><p id="import-file-help" class="form-note">Alleen het gekozen bestand wordt gelezen. Een voorvertoning schrijft niets naar de importcatalogus. Bronlocators worden als tekst behandeld.</p>${importUI.filename ? `<p class="muted-copy">Bestand: <strong>${esc(importUI.filename)}</strong></p>` : ''}${importUI.busy ? '<p class="muted-copy" role="status">Lokale backend verwerkt het verzoek…</p>' : ''}${importUI.error ? `<p class="import-error" id="import-error" role="alert">${esc(importUI.error)}</p>` : ''}</section>${importSummary()}<section class="panel import-jobs-panel"><div class="panel-head"><div><span class="eyebrow">STATUS UIT DE BACKEND</span><h2>Verwerkingsopdrachten</h2></div><button class="button secondary compact" data-action="import-refresh">${icon('clock')} Vernieuwen</button></div><p class="form-note">Lopende taken worden automatisch gecontroleerd. Een geslaagde taak blijft in de geschiedenis staan na rollback; de actuele catalogus bepaalt welke gegevens beschikbaar zijn.</p><div id="import-jobs" aria-live="polite" aria-atomic="false"></div></section>`);
dist/app.js:187: trailing whitespace.
+  renderImportJobs();
dist/app.js:188: trailing whitespace.
+}
dist/app.js:189: trailing whitespace.
+async function previewFile(file) {
dist/app.js:190: trailing whitespace.
+  if (!file || !canImport()) return;
dist/app.js:191: trailing whitespace.
+  const sequence = ++importUI.sequence;
dist/app.js:192: trailing whitespace.
+  importUI.busy = true; importUI.payload = null; importUI.preview = null; importUI.error = ''; importUI.filename = file.name; renderImport();
dist/app.js:193: trailing whitespace.
+  try {
dist/app.js:194: trailing whitespace.
+    if (file.size > 1024 * 1024) throw new Error('Bestand is groter dan 1 MiB. Kies een kleinere JSON-snapshot.');
dist/app.js:195: trailing whitespace.
+    let payload; try { payload = JSON.parse(await file.text()); } catch { throw new Error('Ongeldige JSON. Er is niets geïmporteerd.'); }
dist/app.js:196: trailing whitespace.
+    const preview = await request('/api/import/preview', 'POST', payload);
dist/app.js:197: trailing whitespace.
+    if (sequence !== importUI.sequence) return;
dist/app.js:198: trailing whitespace.
+    importUI.payload = payload; importUI.preview = preview;
dist/app.js:199: trailing whitespace.
+  } catch (error) { if (sequence === importUI.sequence) importUI.error = error.message || 'Voorvertoning mislukt.'; }
dist/app.js:200: trailing whitespace.
+  finally { if (sequence === importUI.sequence) { importUI.busy = false; if (view === 'import') { renderImport(); document.querySelector('#import-preview')?.focus(); } } }
dist/app.js:201: trailing whitespace.
+}
dist/app.js:202: trailing whitespace.
+async function refreshImportCatalog() {
dist/app.js:203: trailing whitespace.
+  if (dataset !== 'import') return;
dist/app.js:204: trailing whitespace.
+  const [nextCatalog, nextState] = await Promise.all([readJSON(scopedPath('/api/catalog', 'import')), readJSON(scopedPath('/api/state', 'import'))]);
dist/app.js:205: trailing whitespace.
+  if (dataset !== 'import') return;
dist/app.js:206: trailing whitespace.
+  catalog = nextCatalog; state = nextState;
dist/app.js:207: trailing whitespace.
+  comparison = new Set([...comparison].filter(id => catalog.players.some(player => player.id === id)));
dist/app.js:208: trailing whitespace.
+  if (dossierDialog.open) { if (catalog.players.some(player => player.id === openPlayerId)) showDossier(openPlayerId); else dossierDialog.close(); }
dist/app.js:209: trailing whitespace.
+  if (view !== 'import') render(); else updateCounts();
dist/app.js:210: trailing whitespace.
+}
dist/app.js:211: trailing whitespace.
+async function loadImportJobs() {
dist/app.js:212: trailing whitespace.
+  if (!canImport() || importUI.jobsLoading) return;
dist/app.js:213: trailing whitespace.
+  clearTimeout(jobsTimer); importUI.jobsLoading = true;
dist/app.js:214: trailing whitespace.
+  try {
dist/app.js:215: trailing whitespace.
+    const previous = new Map(importUI.jobs.map(job => [job.id, job.status]));
dist/app.js:216: trailing whitespace.
+    const previousSnapshots = JSON.stringify(importUI.snapshots);
dist/app.js:217: trailing whitespace.
+    const [result, importedCatalog] = await Promise.all([readJSON('/api/import/jobs'), readJSON(scopedPath('/api/catalog', 'import'))]);
dist/app.js:218: trailing whitespace.
+    importUI.jobs = result.jobs; importUI.snapshots = importedCatalog.importSnapshots || []; importUI.jobsError = '';
dist/app.js:219: trailing whitespace.
+    if (JSON.stringify(importUI.snapshots) !== previousSnapshots || importUI.jobs.some(job => job.status === 'succeeded' && previous.get(job.id) !== 'succeeded')) await refreshImportCatalog();
dist/app.js:220: trailing whitespace.
+  } catch (error) { importUI.jobsError = `${error.message} Gebruik Vernieuwen om opnieuw te controleren.`; }
dist/app.js:221: trailing whitespace.
+  finally {
dist/app.js:222: trailing whitespace.
+    importUI.jobsLoading = false; renderImportJobs();
dist/app.js:223: trailing whitespace.
+    if (!importUI.jobsError && importUI.jobs.some(job => ['queued', 'running'].includes(job.status))) jobsTimer = setTimeout(loadImportJobs, 1200);
dist/app.js:224: trailing whitespace.
+  }
dist/app.js:225: trailing whitespace.
+}
dist/app.js:226: trailing whitespace.
+async function downloadResponse(path, name, type) {
dist/app.js:227: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
dist/app.js:228: trailing whitespace.
+  if (!response.ok) { const error = await response.json(); throw new Error(error.error || 'Export niet toegestaan.'); }
dist/app.js:229: trailing whitespace.
+  download(name, await response.text(), type);
dist/app.js:230: trailing whitespace.
+}
dist/app.js:231: trailing whitespace.
+function render() {
dist/app.js:232: trailing whitespace.
+  document.querySelector('#breadcrumb').textContent = viewLabels[view]; document.title = `Omni-Scout · ${viewLabels[view]}`;
dist/app.js:233: trailing whitespace.
+  document.querySelectorAll('#nav [data-view]').forEach(el => { const active = el.dataset.view === view; el.classList.toggle('active', active); if (active) el.setAttribute('aria-current', 'page'); else el.removeAttribute('aria-current'); });
dist/app.js:234: trailing whitespace.
+  ({ radar: () => renderRadar(), shortlist: () => renderRadar(true), tasks: renderTasks, coverage: renderCoverage, import: renderImport, brief: renderBrief, log: renderLog })[view]();
dist/app.js:235: trailing whitespace.
+  updateCounts();
dist/app.js:236: trailing whitespace.
+}
dist/app.js:237: trailing whitespace.
+function navigate(next) { if (!viewLabels[next]) return; view = next; render(); if (next === 'import') loadImportJobs(); window.scrollTo({ top: 0, behavior: 'instant' }); }
dist/app.js:238: trailing whitespace.
+function showDossier(id) {
dist/app.js:239: trailing whitespace.
+  const d = buildDossier(catalog.players.find(p => p.id === id), catalog); if (!d) return;
dist/app.js:240: trailing whitespace.
+  openPlayerId = id;
dist/app.js:241: trailing whitespace.
+  const decisions = state.decisions.filter(x => x.playerId === id);
dist/app.js:242: trailing whitespace.
+  document.querySelector('#dossier-content').innerHTML = `<div class="dialog-top"><span class="eyebrow">SPELERSDOSSIER · ${esc(playerLabel(d.player))}</span><button class="icon-button" data-close="dossier" aria-label="Dossier sluiten">${icon('close')}</button></div><div class="dossier-header"><span class="player-avatar huge variant-0">${esc(initials(d.player.name))}</span><div><h2 id="dossier-title">${esc(d.player.name)}</h2><p>${esc(d.player.club)} · ${esc(d.competition.country)}</p><div class="dossier-tags"><span class="status-tag neutral">${esc(ROLE_LABELS[d.player.role])}</span><span class="status-tag neutral">${d.age} jaar</span>${qualityTag(d)}</div></div></div><div class="dossier-actions"><button class="button ${isShortlisted(id) ? 'secondary' : 'primary'}" data-save="${esc(id)}">${icon('bookmark')}${isShortlisted(id) ? 'Van shortlist halen' : 'Zet op shortlist'}</button><button class="button secondary" data-action="decision" data-player="${esc(id)}">${icon('file')} Leg besluit vast</button></div><div class="dossier-metrics"><div><span>${esc(d.roleMetric.label)}</span><strong>${esc(metric(d))}</strong><small>Testregel · geen universele norm</small></div><div><span>Speelminuten</span><strong>${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></div><div><span>Competitieniveau</span><strong>${d.competition.tier}</strong><small>${esc(d.competition.name)}</small></div></div>
dist/app.js:243: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot"></span><h3>Waarom nader onderzoeken?</h3></div>${d.signals.length ? d.signals.map(s => `<div class="evidence-item"><strong>${esc(s.title)}</strong><p>${esc(s.detail)}</p><small>Beschikbaar sinds ${dates(s.availableAt)} · ${esc(d.source.name)}</small></div>`).join('') : '<p>Geen aantoonbaar signaal binnen de beschikbare gegevens. Niet automatisch geïnterpreteerd als een slechte speler.</p>'}</section>
dist/app.js:244: trailing whitespace.
+    <section class="next-action"><span class="eyebrow">DE BESTE VOLGENDE VRAAG · REGELGEBASEERD</span><h3>${esc(d.nextQuestion)}</h3><p>Gericht op ontbrekend bewijs. Niet op bevestiging van een vooraf gekozen conclusie.</p><button class="button primary" data-action="create-task" data-player="${esc(id)}">${icon('plus')} Maak onderzoeksopdracht</button></section>
dist/app.js:245: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot amber-dot"></span><h3>Wat spreekt tegen de aanbeveling?</h3></div>${d.counters.length ? d.counters.map(e => `<div class="evidence-item counter"><strong>${esc(e.title)}</strong><p>${esc(e.text)}</p><small>${esc(e.locator)} · ${d.player.synthetic ? 'menselijke testobservatie' : 'aangeleverde menselijke observatie'}</small></div>`).join('') : '<p>Geen tegenobservaties vastgelegd. Dat is niet hetzelfde als afwezigheid van risico.</p>'}</section>
dist/app.js:246: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('warning')}<h3>Wat weten we nog niet?</h3></div><div class="gap-list">${d.gaps.map(g => `<p><span>?</span>${esc(g)}</p>`).join('')}</div></section>
dist/app.js:247: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('file')}<h3>Bronnen en bewijs</h3></div>${d.evidence.map(e => `<div class="source-row"><strong>${esc(e.locator)}</strong><span>${esc(e.kind === 'counter' ? 'Tegenobservatie' : 'Positieve observatie')}</span><small>Waargenomen ${dates(e.eventAt)} · Beschikbaar ${dates(e.availableAt)}<br>${esc(e.text)}</small></div>`).join('')}<p class="muted-copy">${d.player.synthetic ? 'Dit profiel bevat synthetische softwaretestgegevens.' : 'De aangeleverde broninhoud en rechtenverklaring zijn niet onafhankelijk geverifieerd.'} “Meer bewijs” betekent dat de beschikbare metingen aan een demonstratieregel voldoen. Bronlocators zijn tekst, geen geverifieerde beelden.</p></section>
dist/app.js:248: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('clock')}<h3>Eerdere besluiten</h3></div>${decisions.length ? [...decisions].reverse().map(e => `<div class="source-row"><strong>${esc(ACTION_LABELS[e.action])} · ${esc(REASON_LABELS[e.reason])}</strong><small>${esc(e.note || 'Geen aanvullende notitie')}<br>${esc(new Date(e.at).toLocaleString('nl-NL'))}</small></div>`).join('') : '<p class="muted-copy">Nog niet beoordeeld door deze lokale werkruimte. Geen uitspraak over bekendheid bij andere clubs.</p>'}</section><div class="dialog-footer">Bron: ${esc(d.source.name)} · Peildatum ${dates(d.asOf)}</div>`;
dist/app.js:249: trailing whitespace.
+  if (!dossierDialog.open) dossierDialog.showModal();
dist/app.js:250: trailing whitespace.
+}
dist/app.js:251: trailing whitespace.
+function openAction(title, body) {
dist/app.js:252: trailing whitespace.
+  document.querySelector('#action-content').innerHTML = `<div class="dialog-top"><h2 id="action-title">${esc(title)}</h2><button class="icon-button" data-close="action" aria-label="Dialoog sluiten">${icon('close')}</button></div>${body}`;
dist/app.js:253: trailing whitespace.
+  actionDialog.showModal();
dist/app.js:254: trailing whitespace.
+}
dist/app.js:255: trailing whitespace.
+function download(name, content, type) { const url = URL.createObjectURL(new Blob([content], { type })); const a = document.createElement('a'); a.href = url; a.download = name; a.click(); setTimeout(() => URL.revokeObjectURL(url), 2000); }
dist/app.js:256: trailing whitespace.
+function applyFiltersFromControl(el) {
dist/app.js:257: trailing whitespace.
+  const key = el.dataset.filter; if (!key) return;
dist/app.js:258: trailing whitespace.
+  filters[key] = el.type === 'checkbox' ? el.checked : el.value;
dist/app.js:259: trailing whitespace.
+  const pos = el.type === 'search' ? el.selectionStart : null;
dist/app.js:260: trailing whitespace.
+  render(); const next = main.querySelector(`[data-filter="${key}"]`); next?.focus(); if (pos !== null) next?.setSelectionRange(pos, pos);
dist/app.js:261: trailing whitespace.
+}
dist/app.js:262: trailing whitespace.
+main.addEventListener('input', event => { if (event.target.matches('input[type="search"][data-filter]')) applyFiltersFromControl(event.target); });
dist/app.js:263: trailing whitespace.
+document.addEventListener('change', event => {
dist/app.js:264: trailing whitespace.
+  const el = event.target;
dist/app.js:265: trailing whitespace.
+  if (el.id === 'dataset-select') { selectDataset(el.value).catch(error => { updateCounts(); notify(error.message, true); }); return; }
dist/app.js:266: trailing whitespace.
+  if (el.id === 'import-file') { previewFile(el.files?.[0]); return; }
dist/app.js:267: trailing whitespace.
+  if (el.matches('[data-filter]') && el.type !== 'search') applyFiltersFromControl(el);
dist/app.js:268: trailing whitespace.
+  if (el.dataset.compare) {
dist/app.js:269: trailing whitespace.
+    if (el.checked && comparison.size >= 3) { el.checked = false; return notify('Vergelijk maximaal drie profielen tegelijk.'); }
dist/app.js:270: trailing whitespace.
+    el.checked ? comparison.add(el.dataset.compare) : comparison.delete(el.dataset.compare);
dist/app.js:271: trailing whitespace.
+    const button = document.querySelector('#compare-button'); if (button) { button.disabled = comparison.size < 2; button.innerHTML = `${icon('compare')} Vergelijk <span>${comparison.size}/3</span>`; }
dist/app.js:272: trailing whitespace.
+  }
dist/app.js:273: trailing whitespace.
+});
dist/app.js:274: trailing whitespace.
+document.addEventListener('click', async event => {
dist/app.js:275: trailing whitespace.
+  const el = event.target.closest('button, a.brand'); if (!el) return;
dist/app.js:276: trailing whitespace.
+  try {
dist/app.js:277: trailing whitespace.
+    if (el.matches('a.brand')) { event.preventDefault(); return navigate('radar'); }
dist/app.js:278: trailing whitespace.
+    if (el.dataset.close) { (el.dataset.close === 'dossier' ? dossierDialog : actionDialog).close(); return; }
dist/app.js:279: trailing whitespace.
+    if (el.dataset.view) return navigate(el.dataset.view);
dist/app.js:280: trailing whitespace.
+    if (el.dataset.importRetry) {
dist/app.js:281: trailing whitespace.
+      importUI.busy = true; el.disabled = true;
dist/app.js:282: trailing whitespace.
+      try { await request(`/api/import/jobs/${encodeURIComponent(el.dataset.importRetry)}/retry`, 'POST', {}); await loadImportJobs(); notify('Nieuwe poging geregistreerd. Volg de werkelijke taakstatus hieronder.'); }
dist/app.js:283: trailing whitespace.
+      catch (error) { importUI.jobsError = error.message; throw error; }
dist/app.js:284: trailing whitespace.
+      finally { importUI.busy = false; renderImportJobs(); }
dist/app.js:285: trailing whitespace.
+      return;
dist/app.js:286: trailing whitespace.
+    }
dist/app.js:287: trailing whitespace.
+    if (el.dataset.importRollback) {
dist/app.js:288: trailing whitespace.
+      openAction('Importsnapshot terugdraaien', `<form id="import-rollback-form" data-snapshot="${esc(el.dataset.importRollback)}"><p class="muted-copy">Snapshot <strong>${esc(el.dataset.importRollback)}</strong> wordt uit de actieve importcatalogus teruggenomen. Eerdere versies en de mutatiegeschiedenis blijven bewaard. Onderzoeksbesluiten worden niet stilzwijgend herschreven.</p><button type="submit" class="button primary">Bevestig rollback</button></form>`); return;
dist/app.js:289: trailing whitespace.
+    }
dist/app.js:290: trailing whitespace.
+    if (el.dataset.open) return showDossier(el.dataset.open);
dist/app.js:291: trailing whitespace.
+    if (el.dataset.quality !== undefined) { filters.quality = el.dataset.quality; return render(); }
dist/app.js:292: trailing whitespace.
+    if (el.dataset.region) { filters.region = el.dataset.region; filters.competition = ''; render(); document.querySelector('.candidates')?.scrollIntoView({ behavior: 'smooth', block: 'start' }); return; }
dist/app.js:293: trailing whitespace.
+    if (el.dataset.competition) { filters = { ...filters, competition: el.dataset.competition, region: '', role: '', quality: '', search: '' }; return navigate('radar'); }
dist/app.js:294: trailing whitespace.
+    if (el.dataset.save) {
dist/app.js:295: trailing whitespace.
+      el.disabled = true; const id = el.dataset.save, removing = isShortlisted(id);
dist/app.js:296: trailing whitespace.
+      await saveDecision(id, removing ? 'archive' : 'follow', removing ? 'other' : 'positive', removing ? 'Van shortlist gehaald; dit is geen negatief talentlabel.' : 'Opgeslagen als onderzoekskandidaat.');
dist/app.js:297: trailing whitespace.
+      render(); if (dossierDialog.open) showDossier(id); notify(removing ? 'Van shortlist gehaald. Reden is vastgelegd.' : 'Opgeslagen op je shortlist.'); return;
dist/app.js:298: trailing whitespace.
+    }
dist/app.js:299: trailing whitespace.
+    if (el.dataset.task) {
dist/app.js:300: trailing whitespace.
+      const task = state.tasks.find(t => t.id === el.dataset.task);
dist/app.js:301: trailing whitespace.
+      openAction(task.status === 'done' ? 'Onderzoek heropenen' : 'Uitkomst vastleggen', `<form id="task-result-form" data-id="${esc(task.id)}" data-status="${task.status === 'done' ? 'todo' : 'done'}"><p class="muted-copy">${esc(task.question)}</p><label>Wat heb je waargenomen? Wat bleef onzeker?<textarea name="result" rows="5" maxlength="2400" ${task.status !== 'done' ? 'required' : ''}>${esc(task.result)}</textarea></label><button class="button primary" type="submit">${icon('check')} ${task.status === 'done' ? 'Heropenen' : 'Uitkomst opslaan'}</button></form>`); return;
dist/app.js:302: trailing whitespace.
+    }
dist/app.js:303: trailing whitespace.
+    const action = el.dataset.action;
dist/app.js:304: trailing whitespace.
+    if (action === 'import-refresh') { await loadImportJobs(); await refreshImportCatalog(); return; }
dist/app.js:305: trailing whitespace.
+    if (action === 'import-sample') { await downloadResponse('/api/import/sample', 'OmniScout-import-SYNTHETISCH.json', 'application/json'); return; }
dist/app.js:306: trailing whitespace.
+    if (action === 'reset') { filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 35, newOnly: false, lowerOnly: false }; return navigate('radar'); }
dist/app.js:307: trailing whitespace.
+    if (action === 'clear-competition') { filters.competition = ''; return render(); }
dist/app.js:308: trailing whitespace.
+    if (action === 'apply-brief') { filters.role = state.brief.role; filters.minAge = state.brief.minAge; filters.maxAge = state.brief.maxAge; return navigate('radar'); }
dist/app.js:309: trailing whitespace.
+    if (action === 'export') {
dist/app.js:310: trailing whitespace.
+      const selectedFilters = { ...filters, shortlistOnly: view === 'shortlist' };
dist/app.js:311: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath(`/api/export?${new URLSearchParams(selectedFilters)}`), `OmniScout-selectie-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.csv`, 'text/csv;charset=utf-8');
dist/app.js:312: trailing whitespace.
+      else download('OmniScout-selectie-FICTIEF.csv', dossierCSV(findPlayers(catalog, selectedFilters, state)), 'text/csv;charset=utf-8');
dist/app.js:313: trailing whitespace.
+      return notify('Selectie geëxporteerd met bron- en gegevensstatus.');
dist/app.js:314: trailing whitespace.
+    }
dist/app.js:315: trailing whitespace.
+    if (action === 'export-state') {
dist/app.js:316: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath('/api/export/state'), `OmniScout-logboek-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.json`, 'application/json');
dist/app.js:317: trailing whitespace.
+      else download('OmniScout-demologboek.json', JSON.stringify({ mode: 'synthetic_demo', exportedAt: new Date().toISOString(), ...state }, null, 2), 'application/json');
dist/app.js:318: trailing whitespace.
+      return;
dist/app.js:319: trailing whitespace.
+    }
dist/app.js:320: trailing whitespace.
+    if (action === 'create-task') {
dist/app.js:321: trailing whitespace.
+      const d = buildDossier(catalog.players.find(p => p.id === el.dataset.player), catalog);
dist/app.js:322: trailing whitespace.
+      openAction('Gerichte onderzoeksopdracht', `<form id="create-task-form" data-player="${esc(d.player.id)}" data-request="${newId()}"><p class="muted-copy">${esc(d.player.name)} · ${esc(d.player.club)} · ${esc(playerLabel(d.player))}</p><label>Welke onzekerheid ga je onderzoeken?<textarea name="question" rows="5" required maxlength="1600">${esc(d.nextQuestion)}</textarea></label><p class="form-note">Alleen intern vastleggen. Deze toepassing verstuurt geen bericht en koopt geen beelden in.</p><button class="button primary" type="submit">${icon('plus')} Opdracht vastleggen</button></form>`); return;
dist/app.js:323: trailing whitespace.
+    }
dist/app.js:324: trailing whitespace.
+    if (action === 'decision') {
dist/app.js:325: trailing whitespace.
+      openAction('Scoutbesluit vastleggen', `<form id="decision-form" data-player="${esc(el.dataset.player)}"><label>Vervolgactie<select name="action" required>${Object.entries(ACTION_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Reden<select name="reason" required>${Object.entries(REASON_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Onderbouwing<textarea name="note" required maxlength="2400" rows="4" placeholder="Wat ondersteunt dit besluit? Wat blijft onzeker?"></textarea></label><p class="form-note">Een budget- of rolafwijzing wordt niet omgezet in een label ‘slechte speler’.</p><button class="button primary" type="submit">${icon('check')} Besluit opslaan</button></form>`); return;
dist/app.js:326: trailing whitespace.
+    }
dist/app.js:327: trailing whitespace.
+    if (action === 'compare') {
dist/app.js:328: trailing whitespace.
+      const ds = [...comparison].map(id => buildDossier(catalog.players.find(p => p.id === id), catalog));
dist/app.js:329: trailing whitespace.
+      const rows = [['Competitie', d => d.competition.name], ['Rol', d => ROLE_LABELS[d.player.role]], ['Leeftijd', d => `${d.age} jaar`], ['Minuten', d => fmt(d.minutes, 0)], ['Rolmeting', d => `${d.roleMetric.label}: ${metric(d)}`], ['Bewijsroute', d => d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'], ['Ontbrekend', d => d.gaps[0]], ['Volgende vraag', d => d.nextQuestion]];
dist/app.js:330: trailing whitespace.
+      openAction('Vergelijk bewijs, niet een talentscore', `<p class="form-note">${esc(datasetLabel())}. De statistieken zijn niet gecorrigeerd voor verschillen tussen competities of rollen.</p><div class="table-scroll"><table class="compare-table"><thead><tr><th>Onderdeel</th>${ds.map(d => `<th>${esc(d.player.name)}</th>`).join('')}</tr></thead><tbody>${rows.map(([label, get]) => `<tr><th>${label}</th>${ds.map(d => `<td>${esc(get(d))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`); return;
dist/app.js:331: trailing whitespace.
+    }
dist/app.js:332: trailing whitespace.
+  } catch (error) { notify(error.message || 'Actie mislukt.', true); } finally { if (el.isConnected) el.disabled = false; }
dist/app.js:333: trailing whitespace.
+});
dist/app.js:334: trailing whitespace.
+document.addEventListener('submit', async event => {
dist/app.js:335: trailing whitespace.
+  const form = event.target; if (!['brief-form', 'create-task-form', 'task-result-form', 'decision-form', 'import-confirm-form', 'import-rollback-form'].includes(form.id)) return;
dist/app.js:336: trailing whitespace.
+  event.preventDefault(); const submit = form.querySelector('[type="submit"]'); submit.disabled = true;
dist/app.js:337: trailing whitespace.
+  const data = Object.fromEntries(new FormData(form));
dist/app.js:338: trailing whitespace.
+  try {
dist/app.js:339: trailing whitespace.
+    if (form.id === 'import-confirm-form') {
dist/app.js:340: trailing whitespace.
+      if (importUI.busy || !importUI.preview?.valid || !form.querySelector('#import-confirm-check').checked) throw new Error('Controleer het bestand en bevestig de importverklaring eerst.');
dist/app.js:341: trailing whitespace.
+      importUI.busy = true; importUI.error = '';
dist/app.js:342: trailing whitespace.
+      try {
dist/app.js:343: trailing whitespace.
+        const result = await request('/api/import/confirm', 'POST', { payload: importUI.payload, digest: importUI.preview.digest });
dist/app.js:344: trailing whitespace.
+        importUI.preview = null; importUI.payload = null;
dist/app.js:345: trailing whitespace.
+        importUI.jobs = [result.job, ...importUI.jobs.filter(job => job.id !== result.job.id)];
dist/app.js:346: trailing whitespace.
+        await selectDataset('import'); await loadImportJobs();
dist/app.js:347: trailing whitespace.
+        notify('Importopdracht geregistreerd. De taakstatus toont of verwerking is geslaagd.');
dist/app.js:348: trailing whitespace.
+      } catch (error) { importUI.error = `${error.message} Kies het bestand opnieuw voor een actuele controle.`; importUI.preview = null; throw error; }
dist/app.js:349: trailing whitespace.
+      finally { importUI.busy = false; if (view === 'import') renderImport(); }
dist/app.js:350: trailing whitespace.
+      return;
dist/app.js:351: trailing whitespace.
+    }
dist/app.js:352: trailing whitespace.
+    if (form.id === 'import-rollback-form') {
dist/app.js:353: trailing whitespace.
+      importUI.busy = true;
dist/app.js:354: trailing whitespace.
+      try {
dist/app.js:355: trailing whitespace.
+        await request('/api/import/rollback', 'POST', { snapshotId: form.dataset.snapshot });
dist/app.js:356: trailing whitespace.
+        importUI.rolledBack.add(form.dataset.snapshot); importUI.preview = null;
dist/app.js:357: trailing whitespace.
+        await refreshImportCatalog(); await loadImportJobs(); actionDialog.close();
dist/app.js:358: trailing whitespace.
+        notify('Rollback bevestigd door de backend. Actieve importcatalogus bijgewerkt.');
dist/app.js:359: trailing whitespace.
+      } finally { importUI.busy = false; if (view === 'import') renderImport(); }
dist/app.js:360: trailing whitespace.
+      return;
dist/app.js:361: trailing whitespace.
+    }
dist/app.js:362: trailing whitespace.
+    if (form.id === 'brief-form') {
dist/app.js:363: trailing whitespace.
+      const brief = { ...data, minAge: Number(data.minAge), maxAge: Number(data.maxAge) };
dist/app.js:364: trailing whitespace.
+      if (brief.minAge > brief.maxAge) throw new Error('Minimumleeftijd mag niet hoger zijn dan maximumleeftijd.');
dist/app.js:365: trailing whitespace.
+      if (backend) await request('/api/brief', 'PUT', brief); else { state.brief = brief; addAudit('brief.updated', 'local-brief'); persist(); }
dist/app.js:366: trailing whitespace.
+      notify('Clubvraag opgeslagen. Positie en leeftijd kunnen op de radar worden toegepast.');
dist/app.js:367: trailing whitespace.
+    }
dist/app.js:368: trailing whitespace.
+    if (form.id === 'create-task-form') {
dist/app.js:369: trailing whitespace.
+      if (!data.question.trim()) throw new Error('Vul een onderzoeksvraag in.');
dist/app.js:370: trailing whitespace.
+      const payload = { playerId: form.dataset.player, question: data.question.trim(), requestId: form.dataset.request };
dist/app.js:371: trailing whitespace.
+      if (backend) await request('/api/tasks', 'POST', payload);
dist/app.js:372: trailing whitespace.
+      else if (!state.tasks.some(t => t.requestId === payload.requestId)) { const task = { id: newId(), ...payload, status: 'todo', result: '', at: new Date().toISOString(), completedAt: null }; state.tasks.push(task); addAudit('task.created', task.id, task.playerId); persist(); }
dist/app.js:373: trailing whitespace.
+      actionDialog.close(); dossierDialog.close(); navigate('tasks'); notify('Onderzoeksopdracht vastgelegd. Er is niets extern verstuurd.');
dist/app.js:374: trailing whitespace.
+    }
dist/app.js:375: trailing whitespace.
+    if (form.id === 'task-result-form') {
dist/app.js:376: trailing whitespace.
+      const payload = { status: form.dataset.status, result: data.result.trim() };
dist/app.js:377: trailing whitespace.
+      if (payload.status === 'done' && !payload.result) throw new Error('Beschrijf eerst de waargenomen uitkomst.');
dist/app.js:378: trailing whitespace.
+      if (backend) await request(`/api/tasks/${form.dataset.id}`, 'PATCH', payload);
dist/app.js:379: trailing whitespace.
+      else { const task = state.tasks.find(t => t.id === form.dataset.id); Object.assign(task, payload, { completedAt: payload.status === 'done' ? new Date().toISOString() : null }); addAudit('task.updated', task.id, payload.status); persist(); }
dist/app.js:380: trailing whitespace.
+      actionDialog.close(); render(); notify('Onderzoeksuitkomst opgeslagen.');
dist/app.js:381: trailing whitespace.
+    }
dist/app.js:382: trailing whitespace.
+    if (form.id === 'decision-form') {
dist/app.js:383: trailing whitespace.
+      if (!data.note.trim()) throw new Error('Een geschreven onderbouwing is vereist.');
dist/app.js:384: trailing whitespace.
+      await saveDecision(form.dataset.player, data.action, data.reason, data.note.trim()); actionDialog.close(); showDossier(form.dataset.player); render(); notify('Besluit en reden vastgelegd.');
dist/app.js:385: trailing whitespace.
+    }
dist/app.js:386: trailing whitespace.
+    updateCounts();
dist/app.js:387: trailing whitespace.
+  } catch (error) { notify(error.message || 'Opslaan mislukt.', true); } finally { if (submit.isConnected) submit.disabled = false; }
dist/app.js:388: trailing whitespace.
+});
dist/app.js:389: trailing whitespace.
+for (const dialog of [dossierDialog, actionDialog]) dialog.addEventListener('click', event => { if (event.target === dialog) { const r = dialog.getBoundingClientRect(); if (event.clientX < r.left || event.clientX > r.right || event.clientY < r.top || event.clientY > r.bottom) dialog.close(); } });
dist/app.js:390: trailing whitespace.
+document.querySelectorAll('[data-icon]').forEach(el => { el.innerHTML = icon(el.dataset.icon); });
dist/app.js:391: trailing whitespace.
+render();
dist/app.js:392: trailing whitespace.
+async function init() {
dist/app.js:393: trailing whitespace.
+  if (!window.OMNI_INLINE && ['http:', 'https:'].includes(location.protocol)) {
dist/app.js:394: trailing whitespace.
+    try {
dist/app.js:395: trailing whitespace.
+      const session = await readJSON('/api/session');
dist/app.js:396: trailing whitespace.
+      if (session.mode === 'local_single_user_demo' && session.csrf) { backend = session; await selectDataset(preferredDataset === 'import' && canImport() ? 'import' : 'demo', { remember: false }); }
dist/app.js:397: trailing whitespace.
+    } catch (error) { if (backend) { workspaceError = error.message; notify(`Lokale gegevens konden niet worden geladen: ${error.message}`, true); } }
dist/app.js:398: trailing whitespace.
+  }
dist/app.js:399: trailing whitespace.
+  datasetLoading = false;
dist/app.js:400: trailing whitespace.
+  if (preferredDataset === 'import' && !canImport()) notify('De bewaarde importkeuze vereist de lokale Node-app. Hier wordt de fictieve demo getoond.');
dist/app.js:401: trailing whitespace.
+  render();
dist/app.js:402: trailing whitespace.
+  updateCounts();
dist/app.js:403: trailing whitespace.
+}
dist/app.js:404: trailing whitespace.
+init();
docs/IMPLEMENTATION_STATUS.md:29: trailing whitespace.
+Zie `IMPORT_SCHEMA.md`, `MIGRATION_ROLLBACK.md`, `NEXT_CODEX_TASK.md` en `reports/current/worker-d-review.md` voor concrete contracten en grenzen. Oude statussen in oorspronkelijke handoffbijlagen beschrijven het verpakkingsmoment, niet deze uitgevoerde sessie.
docs/NEXT_CODEX_TASK.md:11: trailing whitespace.
+Geen publieke hosting, periodieke inzameling, betaalde provider of LLM is in deze levering gestart. Dit vervolgdocument start op zichzelf geen nieuwe taak.
handoff/TRANSFER_STATUS.json:28: trailing whitespace.
+}
package.json:1: trailing whitespace.
+{
package.json:2: trailing whitespace.
+  "name": "omniscout-research-radar",
package.json:3: trailing whitespace.
+  "version": "0.2.0",
package.json:4: trailing whitespace.
+  "private": true,
package.json:5: trailing whitespace.
+  "type": "module",
package.json:6: trailing whitespace.
+  "description": "Werkend lokaal scoutingprototype met expliciete datadekking, bewijs en onderzoeksacties.",
package.json:7: trailing whitespace.
+  "engines": {
package.json:8: trailing whitespace.
+    "node": ">=22"
package.json:9: trailing whitespace.
+  },
package.json:10: trailing whitespace.
+  "scripts": {
package.json:11: trailing whitespace.
+    "start": "node src/server.mjs",
package.json:12: trailing whitespace.
+    "test": "node --test tests/*.test.mjs",
package.json:13: trailing whitespace.
+    "test:browser": "node tools/browser-e2e.mjs",
package.json:14: trailing whitespace.
+    "check": "node tools/check.mjs",
package.json:15: trailing whitespace.
+    "preview:build": "node tools/build-preview.mjs",
package.json:16: trailing whitespace.
+    "verify": "npm run check && npm test && npm run preview:build"
package.json:17: trailing whitespace.
+  }
package.json:18: trailing whitespace.
+}
public/app.js:1: trailing whitespace.
+import { CATALOG } from '/modules/fixtures.mjs';
public/app.js:2: trailing whitespace.
+let catalog = CATALOG;
public/app.js:3: trailing whitespace.
+import { ROLE_LABELS, ACTION_LABELS, REASON_LABELS, COVERAGE_FIELDS, COVERAGE_LABELS, COVERAGE_STATES, buildDossier, findPlayers, dossierCSV } from '/modules/engine.mjs';
public/app.js:4: trailing whitespace.
+
public/app.js:5: trailing whitespace.
+const icons = {
public/app.js:6: trailing whitespace.
+ radar: '<circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="4"/><path d="M12 12l7-7M12 2v2M2 12h2M12 20v2M20 12h2"/>',
public/app.js:7: trailing whitespace.
+ board: '<rect x="3" y="4" width="18" height="16" rx="3"/><path d="M9 4v16M15 4v16M5 9h2M11 13h2M17 9h2"/>',
public/app.js:8: trailing whitespace.
+ bookmark: '<path d="M6 4h12v17l-6-4-6 4z"/>',
public/app.js:9: trailing whitespace.
+ globe: '<circle cx="12" cy="12" r="9"/><ellipse cx="12" cy="12" rx="4" ry="9"/><path d="M3 12h18M5 6h14M5 18h14"/>',
public/app.js:10: trailing whitespace.
+ sliders: '<path d="M4 6h16M4 12h16M4 18h16"/><circle cx="8" cy="6" r="2"/><circle cx="16" cy="12" r="2"/><circle cx="10" cy="18" r="2"/>',
public/app.js:11: trailing whitespace.
+ clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',
public/app.js:12: trailing whitespace.
+ search: '<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',
public/app.js:13: trailing whitespace.
+ arrow: '<path d="M5 12h14m-6-6 6 6-6 6"/>',
public/app.js:14: trailing whitespace.
+ close: '<path d="m6 6 12 12M6 18 18 6"/>',
public/app.js:15: trailing whitespace.
+ plus: '<path d="M12 5v14M5 12h14"/>',
public/app.js:16: trailing whitespace.
+ check: '<path d="m5 12 4 4L19 6"/>',
public/app.js:17: trailing whitespace.
+ export: '<path d="M12 3v12m-4-4 4 4 4-4M4 15v5h16v-5"/>',
public/app.js:18: trailing whitespace.
+ eye: '<path d="M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12z"/><circle cx="12" cy="12" r="3"/>',
public/app.js:19: trailing whitespace.
+ warning: '<path d="m12 3 10 18H2zM12 9v5M12 17v1"/>',
public/app.js:20: trailing whitespace.
+ compare: '<path d="M5 3v18M19 3v18M3 7h6M15 17h6M9 7l3 3M15 17l-3-3"/>',
public/app.js:21: trailing whitespace.
+ file: '<path d="M5 3h10l4 4v14H5zM14 3v5h5M8 12h8M8 16h8"/>'
public/app.js:22: trailing whitespace.
+};
public/app.js:23: trailing whitespace.
+const icon = (name, cls = '') => `<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name] || icons.file}</svg>`;
public/app.js:24: trailing whitespace.
+const esc = value => String(value ?? '').replace(/[&<>"']/g, x => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[x]));
public/app.js:25: trailing whitespace.
+const fmt = (value, decimals = 1) => value == null || !Number.isFinite(value) ? 'Onbekend' : value.toLocaleString('nl-NL', { maximumFractionDigits: decimals });
public/app.js:26: trailing whitespace.
+const initials = name => name.split(' ').filter(Boolean).slice(0, 2).map(x => x[0]).join('');
public/app.js:27: trailing whitespace.
+const dates = value => value ? new Date(value).toLocaleDateString('nl-NL', { day: 'numeric', month: 'short' }) : 'Onbekend';
public/app.js:28: trailing whitespace.
+const newId = () => globalThis.crypto?.randomUUID?.() || `demo-${Date.now()}-${Math.random().toString(16).slice(2)}`;
public/app.js:29: trailing whitespace.
+const blankState = () => ({ version: 1, decisions: [], tasks: [], brief: { role: '', minAge: 18, maxAge: 23, task: '', budgetScope: '' }, audit: [] });
public/app.js:30: trailing whitespace.
+const storageKey = 'omniscout.synthetic-demo.v1';
public/app.js:31: trailing whitespace.
+const datasetKey = 'omniscout.dataset.v1';
public/app.js:32: trailing whitespace.
+let state = blankState(), backend = null, storageWorking = true, dataset = 'demo', preferredDataset = 'demo', datasetLoading = true, mutationCount = 0, workspaceError = '';
public/app.js:33: trailing whitespace.
+try { const saved = JSON.parse(localStorage.getItem(storageKey) || 'null'); if (saved?.version === 1 && ['decisions', 'tasks', 'audit'].every(k => Array.isArray(saved[k]))) state = saved; } catch { storageWorking = false; }
public/app.js:34: trailing whitespace.
+try { if (localStorage.getItem(datasetKey) === 'import') preferredDataset = 'import'; } catch { /* Dataset preference is optional. */ }
public/app.js:35: trailing whitespace.
+let demoState = state;
public/app.js:36: trailing whitespace.
+const importUI = { payload: null, preview: null, filename: '', error: '', busy: false, sequence: 0, jobs: [], snapshots: [], jobsError: '', jobsLoading: false, rolledBack: new Set() };
public/app.js:37: trailing whitespace.
+let jobsTimer;
public/app.js:38: trailing whitespace.
+let view = 'radar', filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 23, newOnly: false, lowerOnly: false };
public/app.js:39: trailing whitespace.
+let comparison = new Set(), openPlayerId = null;
public/app.js:40: trailing whitespace.
+const main = document.querySelector('#main'), dossierDialog = document.querySelector('#dossier-dialog'), actionDialog = document.querySelector('#action-dialog');
public/app.js:41: trailing whitespace.
+const viewLabels = { radar: 'Wereldradar', tasks: 'Onderzoeksbord', shortlist: 'Shortlist', coverage: 'Datadekking', import: 'Bronimport', brief: 'Clubvraag', log: 'Beslislogboek' };
public/app.js:42: trailing whitespace.
+let toastTimeout;
public/app.js:43: trailing whitespace.
+function notify(message, error = false) { const el = document.querySelector('#toast'); el.textContent = message; el.className = `toast show ${error ? 'error' : ''}`; clearTimeout(toastTimeout); toastTimeout = setTimeout(() => el.classList.remove('show'), 4500); }
public/app.js:44: trailing whitespace.
+function persist() { if (dataset !== 'demo') throw new Error('Importgegevens worden uitsluitend in de lokale backend opgeslagen.'); demoState = state; try { localStorage.setItem(storageKey, JSON.stringify(state)); } catch { storageWorking = false; notify('Browseropslag niet beschikbaar. Wijzigingen blijven alleen in deze tab.', true); } }
public/app.js:45: trailing whitespace.
+function addAudit(action, objectId, detail = '') { state.audit.push({ id: newId(), action, objectId, detail, at: new Date().toISOString(), actor: 'browser-demo-user' }); }
public/app.js:46: trailing whitespace.
+const scopedPath = (path, selected = dataset) => `${path}${path.includes('?') ? '&' : '?'}dataset=${encodeURIComponent(selected)}`;
public/app.js:47: trailing whitespace.
+const canImport = () => Boolean(backend?.supportsImport && !window.OMNI_INLINE);
public/app.js:48: trailing whitespace.
+const sourceLabel = source => source?.status === 'synthetic' ? 'Synthetische testimport' : source?.status === 'approved' ? 'Importverklaring · niet onafhankelijk geverifieerd' : 'Bronstatus ontbreekt of is ongeldig';
public/app.js:49: trailing whitespace.
+const playerLabel = player => dataset === 'demo' ? 'Fictief profiel' : player?.synthetic ? 'Synthetische testimport' : 'Importverklaring · niet onafhankelijk geverifieerd';
public/app.js:50: trailing whitespace.
+function datasetLabel() { return dataset === 'demo' ? 'Fictieve testgegevens' : !catalog.players.length ? 'Lokale import · geen beschikbare profielen' : catalog.players.every(p => p.synthetic) ? 'Synthetische testimport' : 'Importgegevens · verklaring niet onafhankelijk geverifieerd'; }
public/app.js:51: trailing whitespace.
+async function readJSON(path) {
public/app.js:52: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
public/app.js:53: trailing whitespace.
+  const result = await response.json();
public/app.js:54: trailing whitespace.
+  if (!response.ok) throw new Error(result.error || 'Lokale gegevens ophalen mislukt.');
public/app.js:55: trailing whitespace.
+  return result;
public/app.js:56: trailing whitespace.
+}
public/app.js:57: trailing whitespace.
+async function refreshWorkspace(selected = dataset) {
public/app.js:58: trailing whitespace.
+  const result = await readJSON(scopedPath('/api/state', selected));
public/app.js:59: trailing whitespace.
+  if (dataset === selected) state = result;
public/app.js:60: trailing whitespace.
+}
public/app.js:61: trailing whitespace.
+async function request(path, method, data, { refresh = !path.startsWith('/api/import/') } = {}) {
public/app.js:62: trailing whitespace.
+  if (!backend) throw new Error('Deze actie vereist de lokale Node-backend.');
public/app.js:63: trailing whitespace.
+  const selected = dataset, workspaceMutation = !path.startsWith('/api/import/');
public/app.js:64: trailing whitespace.
+  mutationCount++; updateCounts();
public/app.js:65: trailing whitespace.
+  try {
public/app.js:66: trailing whitespace.
+    const r = await fetch(workspaceMutation ? scopedPath(path, selected) : path, { method, headers: { 'Content-Type': 'application/json', 'X-Omniscout-CSRF': backend.csrf }, body: JSON.stringify(workspaceMutation ? { ...data, dataset: selected } : data) });
public/app.js:67: trailing whitespace.
+    const result = await r.json();
public/app.js:68: trailing whitespace.
+    if (!r.ok) { const error = new Error(result.error || 'Opslaan mislukt.'); error.details = result.details; throw error; }
public/app.js:69: trailing whitespace.
+    if (refresh) await refreshWorkspace(selected);
public/app.js:70: trailing whitespace.
+    return result;
public/app.js:71: trailing whitespace.
+  } finally { mutationCount--; updateCounts(); }
public/app.js:72: trailing whitespace.
+}
public/app.js:73: trailing whitespace.
+async function selectDataset(selected, { remember = true } = {}) {
public/app.js:74: trailing whitespace.
+  if (!['demo', 'import'].includes(selected)) return;
public/app.js:75: trailing whitespace.
+  if (selected === 'import' && !canImport()) throw new Error('Lokale import vereist de Node-app. De zelfstandige en edge-demo tonen alleen fictieve gegevens.');
public/app.js:76: trailing whitespace.
+  datasetLoading = true; updateCounts();
public/app.js:77: trailing whitespace.
+  try {
public/app.js:78: trailing whitespace.
+    const [nextCatalog, nextState] = backend ? await Promise.all([readJSON(scopedPath('/api/catalog', selected)), readJSON(scopedPath('/api/state', selected))]) : [CATALOG, demoState];
public/app.js:79: trailing whitespace.
+    dataset = selected; catalog = nextCatalog; state = nextState; workspaceError = '';
public/app.js:80: trailing whitespace.
+    comparison.clear(); filters = { ...filters, search: '', competition: '', region: '', quality: '' };
public/app.js:81: trailing whitespace.
+    dossierDialog.close(); actionDialog.close(); openPlayerId = null;
public/app.js:82: trailing whitespace.
+    if (remember) { preferredDataset = selected; try { localStorage.setItem(datasetKey, selected); } catch { /* Never cache imported data. */ } }
public/app.js:83: trailing whitespace.
+    render();
public/app.js:84: trailing whitespace.
+  } finally { datasetLoading = false; updateCounts(); }
public/app.js:85: trailing whitespace.
+}
public/app.js:86: trailing whitespace.
+async function saveDecision(playerId, action, reason, note = '') {
public/app.js:87: trailing whitespace.
+  if (backend) await request('/api/decisions', 'POST', { playerId, action, reason, note });
public/app.js:88: trailing whitespace.
+  else { const id = newId(); state.decisions.push({ id, playerId, action, reason, note, at: new Date().toISOString() }); addAudit('decision.created', id, `${playerId}: ${action} (${reason})`); persist(); }
public/app.js:89: trailing whitespace.
+  updateCounts();
public/app.js:90: trailing whitespace.
+}
public/app.js:91: trailing whitespace.
+function latestDecision(id) { return state.decisions.filter(x => x.playerId === id).at(-1); }
public/app.js:92: trailing whitespace.
+function isShortlisted(id) { return latestDecision(id)?.action === 'follow'; }
public/app.js:93: trailing whitespace.
+function updateCounts() {
public/app.js:94: trailing whitespace.
+  document.querySelector('#task-count').textContent = state.tasks.filter(t => t.status === 'todo').length;
public/app.js:95: trailing whitespace.
+  document.querySelector('#shortlist-count').textContent = catalog.players.filter(p => isShortlisted(p.id)).length;
public/app.js:96: trailing whitespace.
+  document.querySelector('#player-count').textContent = catalog.players.length;
public/app.js:97: trailing whitespace.
+  document.querySelector('#storage-status').textContent = workspaceError ? 'Backendgegevens niet geladen' : backend ? backend.persistence === 'memory' ? 'Node-backend · tijdelijk geheugen' : 'Lokaal opgeslagen op dit apparaat' : storageWorking ? 'Preview · browseropslag' : 'Preview · tijdelijk geheugen';
public/app.js:98: trailing whitespace.
+  const picker = document.querySelector('#dataset-select');
public/app.js:99: trailing whitespace.
+  picker.value = dataset; picker.disabled = datasetLoading || mutationCount > 0;
public/app.js:100: trailing whitespace.
+  picker.querySelector('[value="import"]').disabled = !canImport();
public/app.js:101: trailing whitespace.
+  document.querySelector('#dataset-status').textContent = datasetLoading ? 'Gegevens ophalen…' : workspaceError ? 'Backendgegevens konden niet worden geladen. Kies de gegevensset opnieuw om te proberen.' : dataset === 'import' ? 'Importcatalogus en onderzoek blijven in de lokale backend.' : canImport() ? 'Demo en import hebben een afzonderlijk onderzoekslogboek.' : 'Alleen fictieve demo · import vereist de lokale Node-app.';
public/app.js:102: trailing whitespace.
+  document.querySelector('.date-label').textContent = `PEILDATUM ${dates(catalog.asOf)}`;
public/app.js:103: trailing whitespace.
+  document.querySelector('.demo-chip').textContent = dataset === 'demo' ? 'DEMO' : 'IMPORT';
public/app.js:104: trailing whitespace.
+  document.querySelector('.demo-banner p').innerHTML = dataset === 'demo' ? '<strong>Fictieve testomgeving.</strong> Alle spelers, clubs, competities en statistieken zijn voorbeelden. <strong>0 live databronnen.</strong>' : `<strong>${esc(datasetLabel())}.</strong> Bronrechten en inhoud zijn verklaringen van de importeur. Geen live providerverbinding of onafhankelijk geverifieerde scoutingdekking.`;
public/app.js:105: trailing whitespace.
+}
public/app.js:106: trailing whitespace.
+function heading(kicker, title, description, actions = '') { return `<div class="page-heading"><div><div class="eyebrow">${esc(kicker)}</div><h1>${esc(title)}</h1><p>${esc(description)}</p></div><div class="heading-actions">${actions}</div></div>`; }
public/app.js:107: trailing whitespace.
+function empty(title, detail) { return `<div class="empty-state">${icon('radar')}<h3>${esc(title)}</h3><p>${esc(detail)}</p><button class="button secondary" data-action="reset">Toon alle spelers in deze set</button></div>`; }
public/app.js:108: trailing whitespace.
+function options(obj, current, all) { return `<option value="">${esc(all)}</option>` + Object.entries(obj).map(([key, label]) => `<option value="${esc(key)}" ${String(current) === key ? 'selected' : ''}>${esc(label)}</option>`).join(''); }
public/app.js:109: trailing whitespace.
+function qualityTag(d) { return `<span class="status-tag ${d.quality === 'documented' ? 'green' : 'amber'}"><i></i>${d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'}</span>`; }
public/app.js:110: trailing whitespace.
+function metric(d) { return d.roleMetric ? `${fmt(d.metrics[d.roleMetric.metric])}${d.metrics[d.roleMetric.metric] == null ? '' : ' ' + d.roleMetric.unit}` : 'Onbekend'; }
public/app.js:111: trailing whitespace.
+function worldMap() {
public/app.js:112: trailing whitespace.
+  const regions = [ ['Noord-Amerika', 151, 112, 'NOORD-AMERIKA'], ['Zuid-Amerika', 250, 208, 'ZUID-AMERIKA'], ['Europa', 385, 76, 'EUROPA'], ['Afrika', 369, 160, 'AFRIKA'], ['Azië', 558, 98, 'AZIË'], ['Oceanië', 603, 226, 'OCEANIË'] ];
public/app.js:113: trailing whitespace.
+  return `<div class="world-card panel"><div class="panel-head"><div><span class="eyebrow">WERELDWIJD ONTDEKKEN</span><h2>Niet elke kans staat al op de radar.</h2></div><span class="status-tag neutral">${dataset === 'demo' ? 'SCHEMA + TESTDATA' : 'LOKALE IMPORT'}</span></div>
public/app.js:114: trailing whitespace.
+   <div class="map-wrap"><svg viewBox="0 0 720 285" class="world-map" role="img" aria-label="Schematisch overzicht van de geselecteerde dataset. Geen kaart van live scoutingdekking."><defs><pattern id="grid" width="36" height="28" patternUnits="userSpaceOnUse"><path d="M36 0H0V28" fill="none" stroke="#274357" stroke-width=".5"/></pattern><pattern id="dots" width="5" height="5" patternUnits="userSpaceOnUse"><circle cx="1" cy="1" r=".9" fill="#36526a"/></pattern><radialGradient id="glow"><stop stop-color="#a6f27a" stop-opacity=".13"/><stop offset="1" stop-color="#a6f27a" stop-opacity="0"/></radialGradient></defs><rect width="720" height="285" fill="url(#grid)" opacity=".5"/><ellipse cx="385" cy="140" rx="190" ry="140" fill="url(#glow)"/>
public/app.js:115: trailing whitespace.
+    <g fill="url(#dots)" stroke="#3e596c" stroke-width=".65" opacity=".85"><path d="M48 70l20-23 44-9 23-17 53 9 35 25-21 22-16 29-28 2-9 26-24 2 2 22 32 20 21 1 12 15-19 7-19-12-22-8-24-26-22-25-28-13z"/><path d="m244 193 28-14 28 15 12 23-17 29-11 30-14-11-9-34-16-18z"/><path d="m248 17 35-9 23 12-14 26-32 3-14-13z"/><path d="m337 70 21-20 12 9 15-16 14 9 13-23 16 16-1 24-29 6-14 15-9 18-34-6 6-17z"/><path d="m340 115 20-17 40 9 27 24 5 25-28 24-9 30-18-4-12-29-27-14-12-28z"/><path d="m418 43 36-11 26 9 18-16 36 9 45-1 27 17 27-4 30 22-23 18-29 5-8 17-31-3-3 28-26 21-14-12-13 14-19-6-14-29-30-12-13-15-22-3-22-14z"/><path d="m545 151 12 8 14 17 27 8 8 11-28-2-28-20z"/><path d="m579 209 26-13 33 8 8 24-15 21-30-5-27-15z"/><path d="m665 246 12-11 3 15-15 19z"/><path d="m409 194 7-5 3 16-8 18-5-12z"/></g>
public/app.js:116: trailing whitespace.
+    <g stroke="#94d675" stroke-opacity=".2" fill="none" stroke-dasharray="3 7"><path d="M385 76Q223 3 151 112M385 76Q291 139 250 208M385 76Q390 102 369 160M385 76Q462 21 558 98M385 76Q553 150 603 226"/></g>
public/app.js:117: trailing whitespace.
+    ${regions.map(([name, x, y, label]) => { const count = catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length; return `<g><circle cx="${x}" cy="${y}" r="14" fill="${count ? '#aff57d' : '#64798b'}" opacity=".1"/><circle cx="${x}" cy="${y}" r="5" fill="${count ? '#b8f384' : '#64798b'}"/><text x="${x + 13}" y="${y - 2}" class="map-label">${label}</text><text x="${x + 13}" y="${y + 13}" class="map-value">${count ? `${count} spelers` : 'Niet aangesloten'}</text></g>`; }).join('')}
public/app.js:118: trailing whitespace.
+   </svg></div><div class="region-pills">${regions.map(([name]) => `<button data-region="${esc(name)}">${esc(name)}<b>${catalog.players.filter(p => catalog.competitions.find(c => c.id === p.competitionId)?.region === name).length}</b></button>`).join('')}</div><div class="map-footer"><span><i class="legend-dot"></i>${catalog.competitions.filter(c => catalog.players.some(p => p.competitionId === c.id)).length} competities met spelers</span><span><i class="legend-dot muted"></i>${catalog.competitions.filter(c => !catalog.players.some(p => p.competitionId === c.id)).length} zonder spelersdata</span><button class="text-button" data-view="coverage">Alle dekking ${icon('arrow')}</button></div></div>`;
public/app.js:119: trailing whitespace.
+}
public/app.js:120: trailing whitespace.
+function tableRows(dossiers) {
public/app.js:121: trailing whitespace.
+  return dossiers.map((d, index) => `<tr><td class="check-cell"><input type="checkbox" aria-label="Vergelijk ${esc(d.player.name)}" data-compare="${esc(d.player.id)}" ${comparison.has(d.player.id) ? 'checked' : ''}></td><td><button class="player-link" data-open="${esc(d.player.id)}"><span class="player-avatar variant-${index % 4}">${esc(initials(d.player.name))}</span><span><strong>${esc(d.player.name)}</strong><small>${esc(d.player.club)}</small>${dataset === 'import' ? `<small>${esc(playerLabel(d.player))}</small>` : ''}<small class="mobile-player-meta">${esc(d.player.role)} · ${d.age} jaar · ${esc(d.competition.country)} · niveau ${d.competition.tier}</small></span></button></td><td><span class="role-label">${esc(d.player.role)}</span><small>${d.age} jaar · ${esc(d.player.preferredFoot)}</small></td><td><strong class="cell-normal">${esc(d.competition.country)}</strong><small>Niveau ${esc(d.competition.tier)} · ${esc(d.competition.name)}</small></td><td><strong class="numeric">${esc(metric(d))}</strong><small>${esc(d.roleMetric?.label || 'Rolgegevens')}</small></td><td><strong class="cell-normal">${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></td><td>${qualityTag(d)}<small>${d.minutesChange !== null && d.minutesChange >= 120 ? `+${d.minutesChange} min · gelijke vensters` : d.quality === 'exploration' ? 'Aanvullend onderzoek nodig' : 'Geen toekomstvoorspelling'}</small></td><td><button class="icon-button ${isShortlisted(d.player.id) ? 'selected' : ''}" data-save="${esc(d.player.id)}" aria-label="${isShortlisted(d.player.id) ? 'Verwijder van shortlist' : 'Zet op shortlist'}: ${esc(d.player.name)}">${icon('bookmark')}</button><button class="icon-button" data-open="${esc(d.player.id)}" aria-label="Open dossier ${esc(d.player.name)}">${icon('arrow')}</button></td></tr>`).join('');
public/app.js:122: trailing whitespace.
+}
public/app.js:123: trailing whitespace.
+function playerTable(dossiers) {
public/app.js:124: trailing whitespace.
+  return dossiers.length ? `<div class="table-scroll"><table class="player-table"><thead><tr><th aria-label="Vergelijken"></th><th>SPELER / CLUB</th><th>ROL / LEEFTIJD</th><th>COMPETITIE</th><th>ROLSIGNAAL</th><th>MINUTEN</th><th>BEWIJSROUTE</th><th aria-label="Acties"></th></tr></thead><tbody>${tableRows(dossiers)}</tbody></table></div>` : empty('Geen kandidaten binnen deze filters', 'Dit resultaat geldt alleen voor de geselecteerde gegevensset. Het zegt niets over talent in niet-aangesloten competities.');
public/app.js:125: trailing whitespace.
+}
public/app.js:126: trailing whitespace.
+function renderRadar(shortlist = false) {
public/app.js:127: trailing whitespace.
+  const all = findPlayers(catalog, {}, state), documented = all.filter(d => d.quality === 'documented').length;
public/app.js:128: trailing whitespace.
+  const current = findPlayers(catalog, { ...filters, shortlistOnly: shortlist }, state);
public/app.js:129: trailing whitespace.
+  const regions = Object.fromEntries([...new Set(catalog.competitions.map(c => c.region))].map(x => [x, x]));
public/app.js:130: trailing whitespace.
+  const compOptions = Object.fromEntries(catalog.competitions.map(c => [c.id, `${c.country} · ${c.name}`]));
public/app.js:131: trailing whitespace.
+  main.innerHTML = heading(shortlist ? 'JOUW SCOUTINGSELECTIE' : 'VERBORGEN TALENT · ZICHTBAAR BEWIJS', shortlist ? 'Jouw shortlist.' : 'Zie wat anderen missen.', shortlist ? 'Opgeslagen onderzoekskandidaten. Een shortlist is geen contractbeslissing.' : 'Een wereld aan mogelijkheden. Eén werkplek voor gericht scoutingonderzoek.', `<button class="button secondary" data-action="export">${icon('export')} Export selectie</button><button class="button primary" data-view="brief">${icon('sliders')} Clubvraag instellen</button>`) +
public/app.js:132: trailing whitespace.
+  (!shortlist ? `<div class="kpi-grid"><div class="kpi-card"><span class="kpi-label">${dataset === 'demo' ? 'SPELERS IN DE TESTSET' : 'BESCHIKBARE IMPORTSPELERS'} ${icon('eye')}</span><div class="kpi-number">${all.length}<span>${esc(datasetLabel())}</span></div><p>${catalog.competitions.length} competities in deze gegevensset</p></div><div class="kpi-card"><span class="kpi-label">MEER ONDERLIGGEND BEWIJS ${icon('file')}</span><div class="kpi-number">${documented}<span class="green-text">onderzoekskandidaten</span></div><p>Voldoende minuten voor de voorbeeldregel</p></div><div class="kpi-card"><span class="kpi-label">VERKENNINGSROUTE ${icon('radar')}</span><div class="kpi-number">${all.length - documented}<span class="amber-text">nog nader te bekijken</span></div><p>Weinig data betekent niet weinig talent</p></div></div><div class="radar-top-grid">${worldMap()}<div class="focus-card panel"><div class="eyebrow">ONDERZOEK, GEEN GOKWERK</div><div class="focus-orbit">${icon('radar')}</div><h2>De beste volgende<br>vraag.</h2><p>Geen onverklaarbare talentscore. Wel de bron, het tegenbewijs en wat je nu moet onderzoeken.</p><div class="focus-steps"><span><b>01</b>Ontdek een relevant signaal</span><span><b>02</b>Controleer wat nog ontbreekt</span><span><b>03</b>Geef de scout een gerichte taak</span></div><button class="text-button" data-view="tasks">Open onderzoeksbord ${icon('arrow')}</button></div></div>` : '') +
public/app.js:133: trailing whitespace.
+  `<section class="panel candidates"><div class="candidates-top"><div><h2>${shortlist ? 'Opgeslagen spelers' : 'Ontdekkingsradar'} <span class="count-badge">${current.length}</span></h2><p>Gesorteerd op beschikbare informatie. Niet op voorspeld talent.</p></div><button class="button secondary compact" id="compare-button" data-action="compare" ${comparison.size < 2 ? 'disabled' : ''}>${icon('compare')} Vergelijk <span>${comparison.size}/3</span></button></div>
public/app.js:134: trailing whitespace.
+    <div class="filters"><label class="search-input">${icon('search')}<input id="search" data-filter="search" type="search" value="${esc(filters.search)}" placeholder="Zoek speler, club of land" aria-label="Zoek speler, club of land"></label><label class="select-filter"><span class="sr-only">Positie</span><select data-filter="role" aria-label="Positie">${options(ROLE_LABELS, filters.role, 'Alle posities')}</select></label><label class="select-filter"><span class="sr-only">Regio</span><select data-filter="region" aria-label="Regio">${options(regions, filters.region, 'Alle regio’s')}</select></label><label class="age-filter"><span>Leeftijd</span><input type="number" min="18" max="60" data-filter="minAge" aria-label="Minimumleeftijd" value="${esc(filters.minAge)}"><span>–</span><input type="number" min="18" max="60" data-filter="maxAge" aria-label="Maximumleeftijd" value="${esc(filters.maxAge)}"></label></div>
public/app.js:135: trailing whitespace.
+    <div class="filter-secondary"><div class="segment-control"><button data-quality="" class="${filters.quality === '' ? 'active' : ''}">Alle kandidaten</button><button data-quality="documented" class="${filters.quality === 'documented' ? 'active' : ''}">Meer bewijs</button><button data-quality="exploration" class="${filters.quality === 'exploration' ? 'active' : ''}">Verkenning</button></div><div class="quick-filters"><label><input type="checkbox" data-filter="lowerOnly" ${filters.lowerOnly ? 'checked' : ''}> Niveau 3 en lager</label><label><input type="checkbox" data-filter="newOnly" ${filters.newOnly ? 'checked' : ''}> Nieuw voor onze club</label></div></div>
public/app.js:136: trailing whitespace.
+    ${filters.competition ? `<div class="active-filter">Competitie: ${esc(compOptions[filters.competition])}<button data-action="clear-competition">${icon('close')} Wissen</button></div>` : ''}
public/app.js:137: trailing whitespace.
+    ${playerTable(current)}<div class="table-foot"><span><i class="legend-dot"></i>${esc(datasetLabel())} · Statistieken zijn niet tussen competities genormaliseerd</span><span>Peildatum ${dates(catalog.asOf)}</span></div></section>`;
public/app.js:138: trailing whitespace.
+}
public/app.js:139: trailing whitespace.
+function renderCoverage() {
public/app.js:140: trailing whitespace.
+  main.innerHTML = heading('TRANSPARANT OVER WAT WE WETEN', 'Dekking, zonder blinde claims.', 'Een competitienaam in de database is geen volledige spelersanalyse.') + `<div class="notice-card">${icon('warning')}<div><strong>${esc(datasetLabel())}.</strong><p>${dataset === 'demo' ? 'Onderstaande statussen demonstreren het dekkingsregister met fictieve competities. 0 echte databronnen aangesloten.' : 'Onderstaande bronrechten en dekkingsvelden zijn aangeleverd door de importeur; ze zijn niet onafhankelijk geverifieerd. Geen live providerverbinding.'}</p></div></div><section class="panel"><div class="panel-head"><div><span class="eyebrow">DEKKINGSREGISTER</span><h2>Per bron. Per competitie. Per gegevenstype.</h2></div></div><div class="table-scroll"><table class="coverage-table"><thead><tr><th>COMPETITIE</th><th>NIVEAU</th>${COVERAGE_FIELDS.map(k => `<th>${esc(COVERAGE_LABELS[k])}</th>`).join('')}<th>ACTUALITEIT</th></tr></thead><tbody>${catalog.competitions.map(c => `<tr><td><button class="coverage-link" data-competition="${esc(c.id)}">${esc(c.country)} ${icon('arrow')}</button><small>${esc(c.name)} · ${esc(c.season || 'Seizoen onbekend')} · ${esc(dataset === 'demo' ? 'fictief' : sourceLabel(catalog.sources.find(s => s.id === c.sourceId)))}</small></td><td>${esc(c.tier)}</td>${COVERAGE_FIELDS.map(k => `<td><span class="coverage-dot ${esc(c.coverage[k])}" title="${esc(COVERAGE_STATES[c.coverage[k]])}" aria-label="${esc(COVERAGE_LABELS[k] + ': ' + COVERAGE_STATES[c.coverage[k]])}"><span class="sr-only">${esc(COVERAGE_STATES[c.coverage[k]])}</span>${c.coverage[k] === 'available' ? '✓' : c.coverage[k] === 'partial' ? '◐' : '—'}</span></td>`).join('')}<td>${dates(c.lastReceivedAt)}<small>${fmt(c.observedPlayers, 0)} spelers</small></td></tr>`).join('')}</tbody></table></div><div class="coverage-legend">${Object.entries(COVERAGE_STATES).map(([k, v]) => `<span><i class="coverage-dot ${k}"></i>${esc(v)}</span>`).join('')}</div></section><div class="info-grid"><div class="panel info-panel"><h3>Onbekend blijft onbekend</h3><p>Geen minuten betekent geen berekening per 90 minuten. Geen volledige noemer betekent geen dekkingspercentage. Een niet-aangesloten competitie blijft zichtbaar als een gat.</p></div><div class="panel info-panel"><h3>Bronrechten vóór verwerking</h3><p>Voor bronnen zijn rechten nodig voor ophalen, bewaren, weergeven, analyse en export. De importeur verklaart deze rechten; de toepassing controleert velden en vervaldatums, geen externe overeenkomsten. Er worden geen externe verbindingen gelegd.</p></div></div>`;
public/app.js:141: trailing whitespace.
+}
public/app.js:142: trailing whitespace.
+function taskCard(task) {
public/app.js:143: trailing whitespace.
+  const p = catalog.players.find(p => p.id === task.playerId);
public/app.js:144: trailing whitespace.
+  return `<article class="task-card"><div class="task-card-head"><span class="status-tag ${task.status === 'done' ? 'green' : 'amber'}">${task.status === 'done' ? 'Afgerond' : 'Te onderzoeken'}</span><span>${dates(task.at)}</span></div><button class="task-player" data-open="${esc(task.playerId)}">${esc(p?.name || task.playerId)} ${icon('arrow')}</button><p>${esc(task.question)}</p>${task.result ? `<div class="task-result"><strong>Waargenomen uitkomst</strong><p>${esc(task.result)}</p></div>` : ''}<div class="task-card-bottom"><span>Interne opdracht · niet verstuurd</span><button class="button secondary compact" data-task="${esc(task.id)}">${task.status === 'done' ? 'Heropenen' : 'Uitkomst vastleggen'}</button></div></article>`;
public/app.js:145: trailing whitespace.
+}
public/app.js:146: trailing whitespace.
+function renderTasks() {
public/app.js:147: trailing whitespace.
+  const todo = state.tasks.filter(t => t.status !== 'done'), done = state.tasks.filter(t => t.status === 'done');
public/app.js:148: trailing whitespace.
+  main.innerHTML = heading('VAN SIGNAAL NAAR WAARNEMING', 'Onderzoek met een doel.', 'Leg eerst de vraag vast. Beoordeel daarna wat het nieuwe bewijs werkelijk verandert.', `<button class="button primary" data-view="radar">${icon('plus')} Kies een kandidaat</button>`) + `<div class="board"><section class="board-column"><div class="board-title"><span class="legend-dot amber-dot"></span><h2>Te onderzoeken</h2><span class="count-badge">${todo.length}</span></div>${todo.length ? todo.map(taskCard).join('') : '<div class="board-empty">Nog geen opdrachten. Open een spelersdossier en kies “Maak onderzoeksopdracht”.</div>'}</section><section class="board-column"><div class="board-title"><span class="legend-dot"></span><h2>Uitkomst vastgelegd</h2><span class="count-badge">${done.length}</span></div>${done.length ? done.map(taskCard).join('') : '<div class="board-empty">Een afgeronde opdracht krijgt een geschreven uitkomst. Een klik alleen is geen bewijs.</div>'}</section></div>`;
public/app.js:149: trailing whitespace.
+}
public/app.js:150: trailing whitespace.
+function renderBrief() {
public/app.js:151: trailing whitespace.
+  const b = state.brief;
public/app.js:152: trailing whitespace.
+  main.innerHTML = heading('BEGIN BIJ DE CLUBVRAAG', 'Welke rol ontbreekt?', 'Benoem taken en grenzen. De scout bepaalt hoe de informatie wordt gewogen.') + `<div class="brief-grid"><form id="brief-form" class="panel form-panel"><div class="panel-head"><h2>Recruitmentprofiel</h2><span class="status-tag neutral">${dataset === 'demo' ? 'LOKALE DEMO' : 'LOKALE IMPORT'}</span></div><label>Gezochte positie<select name="role">${options(ROLE_LABELS, b.role, 'Nog niet beperkt')}</select></label><div class="form-row"><label>Minimumleeftijd<input required name="minAge" type="number" min="18" max="60" value="${b.minAge}"></label><label>Maximumleeftijd<input required name="maxAge" type="number" min="18" max="60" value="${b.maxAge}"></label></div><label>Welke taken moet deze speler vervullen?<textarea name="task" maxlength="2400" rows="5" placeholder="Bijvoorbeeld: ruimte achter een hoge verdediging verdedigen en onder druk vooruit spelen.">${esc(b.task)}</textarea></label><label>Budgetscope en nog te verifiëren voorwaarden<textarea name="budgetScope" maxlength="1000" rows="3" placeholder="Bijvoorbeeld: totaalbudget inclusief salaris, niet alleen transfersom. Onbekende bedragen blijven onbekend.">${esc(b.budgetScope)}</textarea></label><p class="form-note">De tekst wordt bewaard als menselijke onderzoeksinstructie. Deze versie vertaalt de tekst niet met AI naar een automatische rolscore.</p><div class="form-actions"><button class="button primary" type="submit">${icon('check')} Profiel opslaan</button><button class="button secondary" type="button" data-action="apply-brief">Gebruik positie en leeftijd</button></div></form><div class="panel info-panel brief-explanation"><span class="eyebrow">VIER APARTE VRAGEN</span><h2>Geen alles-in-één<br>talentcijfer.</h2><div class="principle"><b>01</b><div><strong>Wat zien we nu?</strong><p>Prestaties en waarnemingen met hun bron.</p></div></div><div class="principle"><b>02</b><div><strong>Wat kan veranderen?</strong><p>Een ontwikkelhypothese is nog geen bewezen groeipad.</p></div></div><div class="principle"><b>03</b><div><strong>Past de gevraagde taak?</strong><p>Dit beoordeelt de scout met passend bewijs.</p></div></div><div class="principle"><b>04</b><div><strong>Is de overstap haalbaar?</strong><p>Budget, beschikbaarheid en registratie blijven afzonderlijk te controleren.</p></div></div></div></div>`;
public/app.js:153: trailing whitespace.
+}
public/app.js:154: trailing whitespace.
+function renderLog() {
public/app.js:155: trailing whitespace.
+  const actions = { 'decision.created': 'Scoutbesluit vastgelegd', 'task.created': 'Onderzoek aangemaakt', 'task.updated': 'Onderzoek bijgewerkt', 'brief.updated': 'Clubvraag gewijzigd' };
public/app.js:156: trailing whitespace.
+  main.innerHTML = heading('HET GEHEUGEN VAN JE SCOUTING', 'Bewaar de reden.', 'Een budgetafwijzing is geen negatief talentlabel. Alle wijzigingen blijven als nieuwe gebeurtenissen zichtbaar.', `<button class="button secondary" data-action="export-state">${icon('export')} Exporteer logboek</button>`) + `<section class="panel log-panel">${state.audit.length ? `<div class="timeline">${[...state.audit].reverse().map(e => `<article class="timeline-item"><span class="timeline-icon">${icon(e.action.startsWith('task') ? 'board' : e.action.startsWith('brief') ? 'sliders' : 'bookmark')}</span><div><h3>${esc(actions[e.action] || e.action)}</h3><p>${esc(e.detail || 'Lokaal onderzoeksprofiel opgeslagen')}</p><small>${esc(new Date(e.at).toLocaleString('nl-NL'))} · ${esc(e.actor)}</small></div></article>`).join('')}</div>` : '<div class="board-empty">Nog geen scoutbesluiten of onderzoeksacties. Het logboek wordt gevuld zodra je een actie opslaat.</div>'}<div class="table-foot">Dit lokale logboek is geen productie-audit met onafhankelijk gecontroleerde integriteit.</div></section>`;
public/app.js:157: trailing whitespace.
+}
public/app.js:158: trailing whitespace.
+function importIssues(items, title, kind) {
public/app.js:159: trailing whitespace.
+  if (!Array.isArray(items) || !items.length) return '';
public/app.js:160: trailing whitespace.
+  return `<section class="import-issues ${kind}" aria-label="${esc(title)}"><h3>${esc(title)} (${items.length})</h3><ul>${items.slice(0, 100).map(item => `<li><code>${esc(item.path)}</code> ${esc(item.message)}</li>`).join('')}</ul>${items.length > 100 ? '<p>De eerste 100 meldingen worden getoond. Herstel deze en controleer opnieuw.</p>' : ''}</section>`;
public/app.js:161: trailing whitespace.
+}
public/app.js:162: trailing whitespace.
+function importSummary() {
public/app.js:163: trailing whitespace.
+  if (!importUI.preview) return '';
public/app.js:164: trailing whitespace.
+  const payload = importUI.payload && typeof importUI.payload === 'object' ? importUI.payload : {}, preview = importUI.preview, summary = preview.summary || {}, source = summary.source || payload.source || {};
public/app.js:165: trailing whitespace.
+  const competitions = Array.isArray(payload.competitions) ? payload.competitions : [], players = Array.isArray(payload.players) ? payload.players : [];
public/app.js:166: trailing whitespace.
+  const timestamps = ['eventAt', 'publishedAt', 'retrievedAt', 'availableAt'];
public/app.js:167: trailing whitespace.
+  const timeLabels = { eventAt: 'Gebeurtenis', publishedAt: 'Gepubliceerd', retrievedAt: 'Opgehaald', availableAt: 'Beschikbaar' };
public/app.js:168: trailing whitespace.
+  const stampRange = key => {
public/app.js:169: trailing whitespace.
+    const values = players.flatMap(p => [p?.[key], p?.stats?.[key]]).filter(value => typeof value === 'string').sort();
public/app.js:170: trailing whitespace.
+    return values.length ? `${values[0]} — ${values.at(-1)}` : 'Onbekend';
public/app.js:171: trailing whitespace.
+  };
public/app.js:172: trailing whitespace.
+  return `<section class="panel import-preview" id="import-preview" tabindex="-1"><div class="panel-head"><div><span class="eyebrow">CONTROLE ZONDER OPSLAG</span><h2>${preview.valid ? 'Bestand voldoet aan de importcontroles' : 'Import geblokkeerd'}</h2></div><span class="status-tag ${preview.valid ? 'green' : 'amber'}">${preview.valid ? 'Klaar voor bevestiging' : 'Herstel fouten / conflicten'}</span></div>
public/app.js:173: trailing whitespace.
+    <div class="import-preview-body"><p class="muted-copy">${esc(sourceLabel(source))}. Een geslaagde controle verifieert geen externe bronrechten of voetbalclaims.</p>
public/app.js:174: trailing whitespace.
+    <dl class="import-summary"><div><dt>Snapshot</dt><dd>${esc(payload.snapshotId)}</dd></div><div><dt>Correctie van</dt><dd>${esc(payload.correctionOf || 'Geen')}</dd></div><div><dt>Peildatum</dt><dd>${esc(payload.asOf)}</dd></div><div><dt>Aantallen in upload</dt><dd>${esc(summary.playerCount ?? players.length)} spelers · ${esc(summary.competitionCount ?? competitions.length)} competities · ${esc(summary.sourceCount ?? (payload.source ? 1 : 0))} bron</dd></div><div><dt>Bron</dt><dd>${esc(source.name)} (${esc(source.id)})</dd></div><div><dt>Toegestane verwerking</dt><dd>${esc(Array.isArray(source.allowedUses) ? source.allowedUses.join(', ') : 'Ontbreekt')}</dd></div><div><dt>Rechten verklaard</dt><dd>${source.rightsAttested === true ? 'Ja, volgens de importeur' : 'Nee'}</dd></div><div><dt>Rechten geldig vanaf</dt><dd>${esc(source.validFrom || 'Ontbreekt')}</dd></div><div><dt>Rechten vervallen</dt><dd>${esc(source.expiresAt === null && source.status === 'synthetic' ? 'Geen vervaldatum (synthetische bron)' : source.expiresAt || 'Ontbreekt')}</dd></div><div><dt>Rechtennotitie</dt><dd>${esc(source.rightsNote || 'Ontbreekt')}</dd></div>${timestamps.map(key => `<div><dt>${timeLabels[key]} (bereik)</dt><dd>${esc(stampRange(key))}</dd></div>`).join('')}</dl>
public/app.js:175: trailing whitespace.
+    <p class="form-note">${esc(summary.missingMinutes ?? players.filter(p => p?.stats?.minutes === null).length)} spelers met onbekende minuten; deze krijgen geen per-90-meting. ${summary.idempotent ? 'Deze ongewijzigde snapshot bestaat al: herhaling maakt geen dubbele waarnemingen.' : ''}</p><h3>Competities en seizoenen</h3><ul class="import-seasons">${competitions.slice(0, 100).map(c => `<li>${esc(c?.name)} · ${esc(c?.season || 'Seizoen ontbreekt')} · ${esc(c?.country)} · ontvangen ${esc(c?.lastReceivedAt || 'Onbekend')}</li>`).join('') || '<li>Geen competities aangeleverd.</li>'}</ul>
public/app.js:176: trailing whitespace.
+    ${importIssues(preview.errors, 'Blokkerende fouten en identiteitsconflicten', 'error')}${importIssues(preview.warnings, 'Waarschuwingen — geen automatische samenvoeging', 'warning')}
public/app.js:177: trailing whitespace.
+    ${preview.valid ? `<form id="import-confirm-form"><label class="import-consent"><input type="checkbox" id="import-confirm-check" required><span>Ik heb de bron, rechtenverklaring, peildatum en waarschuwingen gecontroleerd en wil deze snapshot in de lokale importdataset opslaan.</span></label><button type="submit" class="button primary" id="import-confirm" ${importUI.busy ? 'disabled' : ''}>${icon('check')} Bevestig lokale import</button><p class="form-note">Pas na bevestiging wordt een verwerkingsopdracht aangemaakt. De backend controleert rechten en identiteit opnieuw.</p></form>` : '<p class="form-note">Er is niets geïmporteerd. Corrigeer het bronbestand en kies het opnieuw om de controle te herhalen.</p>'}</div></section>`;
public/app.js:178: trailing whitespace.
+}
public/app.js:179: trailing whitespace.
+function renderImportJobs() {
public/app.js:180: trailing whitespace.
+  const host = document.querySelector('#import-jobs'); if (!host) return;
public/app.js:181: trailing whitespace.
+  const statusLabels = { queued: 'In wachtrij', running: 'Wordt verwerkt', succeeded: 'Geslaagd', failed: 'Mislukt' };
public/app.js:182: trailing whitespace.
+  host.innerHTML = `${importUI.jobsError ? `<p class="import-error" role="alert">${esc(importUI.jobsError)}</p>` : ''}${importUI.jobsLoading && !importUI.jobs.length ? '<p class="muted-copy">Werkelijke taakstatus ophalen…</p>' : !importUI.jobs.length ? '<p class="muted-copy">Nog geen importopdrachten in deze lokale backend.</p>' : ''}${[...importUI.jobs].reverse().map(job => `<article class="import-job" data-job-id="${esc(job.id)}" data-job-status="${esc(job.status)}"><div class="import-job-heading"><h3>${esc(job.snapshotId)}</h3><span class="status-tag ${job.status === 'succeeded' ? 'green' : 'amber'}">${esc(statusLabels[job.status] || job.status)}</span></div><p class="muted-copy">Poging ${esc(job.attempts)} / ${esc(job.maxAttempts)} · Bijgewerkt ${esc(job.updatedAt)}</p><small>Taak ${esc(job.id)}</small>${job.error ? `<p class="import-error">${esc(typeof job.error === 'string' ? job.error : job.error.message)}</p>${importIssues(job.error.details, 'Foutdetails', 'error')}` : ''}<div class="form-actions">${job.status === 'failed' && job.attempts < job.maxAttempts ? `<button class="button secondary" data-import-retry="${esc(job.id)}" ${importUI.busy ? 'disabled' : ''}>Opnieuw proberen</button>` : ''}${job.status === 'succeeded' ? (snapshotStatus(job.snapshotId) === 'rolled_back' || importUI.rolledBack.has(job.snapshotId)) ? '<p class="muted-copy">Snapshot teruggedraaid · backendgeschiedenis blijft bewaard.</p>' : snapshotStatus(job.snapshotId) === 'superseded' ? '<p class="muted-copy">Vervangen door een correctie. Draai eerst die correctie terug.</p>' : `<button class="button secondary" data-import-rollback="${esc(job.snapshotId)}" ${importUI.busy ? 'disabled' : ''}>Snapshot terugdraaien</button>` : ''}</div></article>`).join('')}`;
public/app.js:183: trailing whitespace.
+}
public/app.js:184: trailing whitespace.
+function snapshotStatus(snapshotId) { return importUI.snapshots.find(snapshot => snapshot.snapshotId === snapshotId)?.status; }
public/app.js:185: trailing whitespace.
+function renderImport() {
public/app.js:186: trailing whitespace.
+  main.innerHTML = heading('LOKALE BRONIMPORT', 'Controleer. Bevestig. Volg.', 'Importeer één JSON-snapshot met expliciete bronrechten, seizoenen, provideridentiteiten en tijdstempels.') + (!canImport() ? `<section class="panel info-panel"><h2>Start de lokale Node-app voor bronimport</h2><p>De zelfstandige HTML-preview en de edge-demo bevatten alleen synthetische testgegevens. Import, taakverwerking en rollback vereisen de lokale backend.</p><p>Open de toepassing via het adres dat <code>npm start</code> toont. Je huidige demogegevens blijven in de fictieve gegevensset.</p></section>` : `<section class="panel form-panel import-upload"><div class="panel-head"><h2>1. Kies en controleer een bronbestand</h2><button class="button secondary compact" data-action="import-sample">${icon('export')} Synthetisch voorbeeld</button></div><label for="import-file">JSON-bestand · maximaal 1 MiB<input type="file" id="import-file" accept=".json,application/json" ${importUI.busy ? 'disabled' : ''} aria-describedby="import-file-help"></label><p id="import-file-help" class="form-note">Alleen het gekozen bestand wordt gelezen. Een voorvertoning schrijft niets naar de importcatalogus. Bronlocators worden als tekst behandeld.</p>${importUI.filename ? `<p class="muted-copy">Bestand: <strong>${esc(importUI.filename)}</strong></p>` : ''}${importUI.busy ? '<p class="muted-copy" role="status">Lokale backend verwerkt het verzoek…</p>' : ''}${importUI.error ? `<p class="import-error" id="import-error" role="alert">${esc(importUI.error)}</p>` : ''}</section>${importSummary()}<section class="panel import-jobs-panel"><div class="panel-head"><div><span class="eyebrow">STATUS UIT DE BACKEND</span><h2>Verwerkingsopdrachten</h2></div><button class="button secondary compact" data-action="import-refresh">${icon('clock')} Vernieuwen</button></div><p class="form-note">Lopende taken worden automatisch gecontroleerd. Een geslaagde taak blijft in de geschiedenis staan na rollback; de actuele catalogus bepaalt welke gegevens beschikbaar zijn.</p><div id="import-jobs" aria-live="polite" aria-atomic="false"></div></section>`);
public/app.js:187: trailing whitespace.
+  renderImportJobs();
public/app.js:188: trailing whitespace.
+}
public/app.js:189: trailing whitespace.
+async function previewFile(file) {
public/app.js:190: trailing whitespace.
+  if (!file || !canImport()) return;
public/app.js:191: trailing whitespace.
+  const sequence = ++importUI.sequence;
public/app.js:192: trailing whitespace.
+  importUI.busy = true; importUI.payload = null; importUI.preview = null; importUI.error = ''; importUI.filename = file.name; renderImport();
public/app.js:193: trailing whitespace.
+  try {
public/app.js:194: trailing whitespace.
+    if (file.size > 1024 * 1024) throw new Error('Bestand is groter dan 1 MiB. Kies een kleinere JSON-snapshot.');
public/app.js:195: trailing whitespace.
+    let payload; try { payload = JSON.parse(await file.text()); } catch { throw new Error('Ongeldige JSON. Er is niets geïmporteerd.'); }
public/app.js:196: trailing whitespace.
+    const preview = await request('/api/import/preview', 'POST', payload);
public/app.js:197: trailing whitespace.
+    if (sequence !== importUI.sequence) return;
public/app.js:198: trailing whitespace.
+    importUI.payload = payload; importUI.preview = preview;
public/app.js:199: trailing whitespace.
+  } catch (error) { if (sequence === importUI.sequence) importUI.error = error.message || 'Voorvertoning mislukt.'; }
public/app.js:200: trailing whitespace.
+  finally { if (sequence === importUI.sequence) { importUI.busy = false; if (view === 'import') { renderImport(); document.querySelector('#import-preview')?.focus(); } } }
public/app.js:201: trailing whitespace.
+}
public/app.js:202: trailing whitespace.
+async function refreshImportCatalog() {
public/app.js:203: trailing whitespace.
+  if (dataset !== 'import') return;
public/app.js:204: trailing whitespace.
+  const [nextCatalog, nextState] = await Promise.all([readJSON(scopedPath('/api/catalog', 'import')), readJSON(scopedPath('/api/state', 'import'))]);
public/app.js:205: trailing whitespace.
+  if (dataset !== 'import') return;
public/app.js:206: trailing whitespace.
+  catalog = nextCatalog; state = nextState;
public/app.js:207: trailing whitespace.
+  comparison = new Set([...comparison].filter(id => catalog.players.some(player => player.id === id)));
public/app.js:208: trailing whitespace.
+  if (dossierDialog.open) { if (catalog.players.some(player => player.id === openPlayerId)) showDossier(openPlayerId); else dossierDialog.close(); }
public/app.js:209: trailing whitespace.
+  if (view !== 'import') render(); else updateCounts();
public/app.js:210: trailing whitespace.
+}
public/app.js:211: trailing whitespace.
+async function loadImportJobs() {
public/app.js:212: trailing whitespace.
+  if (!canImport() || importUI.jobsLoading) return;
public/app.js:213: trailing whitespace.
+  clearTimeout(jobsTimer); importUI.jobsLoading = true;
public/app.js:214: trailing whitespace.
+  try {
public/app.js:215: trailing whitespace.
+    const previous = new Map(importUI.jobs.map(job => [job.id, job.status]));
public/app.js:216: trailing whitespace.
+    const previousSnapshots = JSON.stringify(importUI.snapshots);
public/app.js:217: trailing whitespace.
+    const [result, importedCatalog] = await Promise.all([readJSON('/api/import/jobs'), readJSON(scopedPath('/api/catalog', 'import'))]);
public/app.js:218: trailing whitespace.
+    importUI.jobs = result.jobs; importUI.snapshots = importedCatalog.importSnapshots || []; importUI.jobsError = '';
public/app.js:219: trailing whitespace.
+    if (JSON.stringify(importUI.snapshots) !== previousSnapshots || importUI.jobs.some(job => job.status === 'succeeded' && previous.get(job.id) !== 'succeeded')) await refreshImportCatalog();
public/app.js:220: trailing whitespace.
+  } catch (error) { importUI.jobsError = `${error.message} Gebruik Vernieuwen om opnieuw te controleren.`; }
public/app.js:221: trailing whitespace.
+  finally {
public/app.js:222: trailing whitespace.
+    importUI.jobsLoading = false; renderImportJobs();
public/app.js:223: trailing whitespace.
+    if (!importUI.jobsError && importUI.jobs.some(job => ['queued', 'running'].includes(job.status))) jobsTimer = setTimeout(loadImportJobs, 1200);
public/app.js:224: trailing whitespace.
+  }
public/app.js:225: trailing whitespace.
+}
public/app.js:226: trailing whitespace.
+async function downloadResponse(path, name, type) {
public/app.js:227: trailing whitespace.
+  const response = await fetch(path, { cache: 'no-store' });
public/app.js:228: trailing whitespace.
+  if (!response.ok) { const error = await response.json(); throw new Error(error.error || 'Export niet toegestaan.'); }
public/app.js:229: trailing whitespace.
+  download(name, await response.text(), type);
public/app.js:230: trailing whitespace.
+}
public/app.js:231: trailing whitespace.
+function render() {
public/app.js:232: trailing whitespace.
+  document.querySelector('#breadcrumb').textContent = viewLabels[view]; document.title = `Omni-Scout · ${viewLabels[view]}`;
public/app.js:233: trailing whitespace.
+  document.querySelectorAll('#nav [data-view]').forEach(el => { const active = el.dataset.view === view; el.classList.toggle('active', active); if (active) el.setAttribute('aria-current', 'page'); else el.removeAttribute('aria-current'); });
public/app.js:234: trailing whitespace.
+  ({ radar: () => renderRadar(), shortlist: () => renderRadar(true), tasks: renderTasks, coverage: renderCoverage, import: renderImport, brief: renderBrief, log: renderLog })[view]();
public/app.js:235: trailing whitespace.
+  updateCounts();
public/app.js:236: trailing whitespace.
+}
public/app.js:237: trailing whitespace.
+function navigate(next) { if (!viewLabels[next]) return; view = next; render(); if (next === 'import') loadImportJobs(); window.scrollTo({ top: 0, behavior: 'instant' }); }
public/app.js:238: trailing whitespace.
+function showDossier(id) {
public/app.js:239: trailing whitespace.
+  const d = buildDossier(catalog.players.find(p => p.id === id), catalog); if (!d) return;
public/app.js:240: trailing whitespace.
+  openPlayerId = id;
public/app.js:241: trailing whitespace.
+  const decisions = state.decisions.filter(x => x.playerId === id);
public/app.js:242: trailing whitespace.
+  document.querySelector('#dossier-content').innerHTML = `<div class="dialog-top"><span class="eyebrow">SPELERSDOSSIER · ${esc(playerLabel(d.player))}</span><button class="icon-button" data-close="dossier" aria-label="Dossier sluiten">${icon('close')}</button></div><div class="dossier-header"><span class="player-avatar huge variant-0">${esc(initials(d.player.name))}</span><div><h2 id="dossier-title">${esc(d.player.name)}</h2><p>${esc(d.player.club)} · ${esc(d.competition.country)}</p><div class="dossier-tags"><span class="status-tag neutral">${esc(ROLE_LABELS[d.player.role])}</span><span class="status-tag neutral">${d.age} jaar</span>${qualityTag(d)}</div></div></div><div class="dossier-actions"><button class="button ${isShortlisted(id) ? 'secondary' : 'primary'}" data-save="${esc(id)}">${icon('bookmark')}${isShortlisted(id) ? 'Van shortlist halen' : 'Zet op shortlist'}</button><button class="button secondary" data-action="decision" data-player="${esc(id)}">${icon('file')} Leg besluit vast</button></div><div class="dossier-metrics"><div><span>${esc(d.roleMetric.label)}</span><strong>${esc(metric(d))}</strong><small>Testregel · geen universele norm</small></div><div><span>Speelminuten</span><strong>${fmt(d.minutes, 0)}</strong><small>${fmt(d.matches, 0)} wedstrijden</small></div><div><span>Competitieniveau</span><strong>${d.competition.tier}</strong><small>${esc(d.competition.name)}</small></div></div>
public/app.js:243: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot"></span><h3>Waarom nader onderzoeken?</h3></div>${d.signals.length ? d.signals.map(s => `<div class="evidence-item"><strong>${esc(s.title)}</strong><p>${esc(s.detail)}</p><small>Beschikbaar sinds ${dates(s.availableAt)} · ${esc(d.source.name)}</small></div>`).join('') : '<p>Geen aantoonbaar signaal binnen de beschikbare gegevens. Niet automatisch geïnterpreteerd als een slechte speler.</p>'}</section>
public/app.js:244: trailing whitespace.
+    <section class="next-action"><span class="eyebrow">DE BESTE VOLGENDE VRAAG · REGELGEBASEERD</span><h3>${esc(d.nextQuestion)}</h3><p>Gericht op ontbrekend bewijs. Niet op bevestiging van een vooraf gekozen conclusie.</p><button class="button primary" data-action="create-task" data-player="${esc(id)}">${icon('plus')} Maak onderzoeksopdracht</button></section>
public/app.js:245: trailing whitespace.
+    <section class="dossier-section"><div class="section-label"><span class="legend-dot amber-dot"></span><h3>Wat spreekt tegen de aanbeveling?</h3></div>${d.counters.length ? d.counters.map(e => `<div class="evidence-item counter"><strong>${esc(e.title)}</strong><p>${esc(e.text)}</p><small>${esc(e.locator)} · ${d.player.synthetic ? 'menselijke testobservatie' : 'aangeleverde menselijke observatie'}</small></div>`).join('') : '<p>Geen tegenobservaties vastgelegd. Dat is niet hetzelfde als afwezigheid van risico.</p>'}</section>
public/app.js:246: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('warning')}<h3>Wat weten we nog niet?</h3></div><div class="gap-list">${d.gaps.map(g => `<p><span>?</span>${esc(g)}</p>`).join('')}</div></section>
public/app.js:247: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('file')}<h3>Bronnen en bewijs</h3></div>${d.evidence.map(e => `<div class="source-row"><strong>${esc(e.locator)}</strong><span>${esc(e.kind === 'counter' ? 'Tegenobservatie' : 'Positieve observatie')}</span><small>Waargenomen ${dates(e.eventAt)} · Beschikbaar ${dates(e.availableAt)}<br>${esc(e.text)}</small></div>`).join('')}<p class="muted-copy">${d.player.synthetic ? 'Dit profiel bevat synthetische softwaretestgegevens.' : 'De aangeleverde broninhoud en rechtenverklaring zijn niet onafhankelijk geverifieerd.'} “Meer bewijs” betekent dat de beschikbare metingen aan een demonstratieregel voldoen. Bronlocators zijn tekst, geen geverifieerde beelden.</p></section>
public/app.js:248: trailing whitespace.
+    <section class="dossier-section"><div class="section-label">${icon('clock')}<h3>Eerdere besluiten</h3></div>${decisions.length ? [...decisions].reverse().map(e => `<div class="source-row"><strong>${esc(ACTION_LABELS[e.action])} · ${esc(REASON_LABELS[e.reason])}</strong><small>${esc(e.note || 'Geen aanvullende notitie')}<br>${esc(new Date(e.at).toLocaleString('nl-NL'))}</small></div>`).join('') : '<p class="muted-copy">Nog niet beoordeeld door deze lokale werkruimte. Geen uitspraak over bekendheid bij andere clubs.</p>'}</section><div class="dialog-footer">Bron: ${esc(d.source.name)} · Peildatum ${dates(d.asOf)}</div>`;
public/app.js:249: trailing whitespace.
+  if (!dossierDialog.open) dossierDialog.showModal();
public/app.js:250: trailing whitespace.
+}
public/app.js:251: trailing whitespace.
+function openAction(title, body) {
public/app.js:252: trailing whitespace.
+  document.querySelector('#action-content').innerHTML = `<div class="dialog-top"><h2 id="action-title">${esc(title)}</h2><button class="icon-button" data-close="action" aria-label="Dialoog sluiten">${icon('close')}</button></div>${body}`;
public/app.js:253: trailing whitespace.
+  actionDialog.showModal();
public/app.js:254: trailing whitespace.
+}
public/app.js:255: trailing whitespace.
+function download(name, content, type) { const url = URL.createObjectURL(new Blob([content], { type })); const a = document.createElement('a'); a.href = url; a.download = name; a.click(); setTimeout(() => URL.revokeObjectURL(url), 2000); }
public/app.js:256: trailing whitespace.
+function applyFiltersFromControl(el) {
public/app.js:257: trailing whitespace.
+  const key = el.dataset.filter; if (!key) return;
public/app.js:258: trailing whitespace.
+  filters[key] = el.type === 'checkbox' ? el.checked : el.value;
public/app.js:259: trailing whitespace.
+  const pos = el.type === 'search' ? el.selectionStart : null;
public/app.js:260: trailing whitespace.
+  render(); const next = main.querySelector(`[data-filter="${key}"]`); next?.focus(); if (pos !== null) next?.setSelectionRange(pos, pos);
public/app.js:261: trailing whitespace.
+}
public/app.js:262: trailing whitespace.
+main.addEventListener('input', event => { if (event.target.matches('input[type="search"][data-filter]')) applyFiltersFromControl(event.target); });
public/app.js:263: trailing whitespace.
+document.addEventListener('change', event => {
public/app.js:264: trailing whitespace.
+  const el = event.target;
public/app.js:265: trailing whitespace.
+  if (el.id === 'dataset-select') { selectDataset(el.value).catch(error => { updateCounts(); notify(error.message, true); }); return; }
public/app.js:266: trailing whitespace.
+  if (el.id === 'import-file') { previewFile(el.files?.[0]); return; }
public/app.js:267: trailing whitespace.
+  if (el.matches('[data-filter]') && el.type !== 'search') applyFiltersFromControl(el);
public/app.js:268: trailing whitespace.
+  if (el.dataset.compare) {
public/app.js:269: trailing whitespace.
+    if (el.checked && comparison.size >= 3) { el.checked = false; return notify('Vergelijk maximaal drie profielen tegelijk.'); }
public/app.js:270: trailing whitespace.
+    el.checked ? comparison.add(el.dataset.compare) : comparison.delete(el.dataset.compare);
public/app.js:271: trailing whitespace.
+    const button = document.querySelector('#compare-button'); if (button) { button.disabled = comparison.size < 2; button.innerHTML = `${icon('compare')} Vergelijk <span>${comparison.size}/3</span>`; }
public/app.js:272: trailing whitespace.
+  }
public/app.js:273: trailing whitespace.
+});
public/app.js:274: trailing whitespace.
+document.addEventListener('click', async event => {
public/app.js:275: trailing whitespace.
+  const el = event.target.closest('button, a.brand'); if (!el) return;
public/app.js:276: trailing whitespace.
+  try {
public/app.js:277: trailing whitespace.
+    if (el.matches('a.brand')) { event.preventDefault(); return navigate('radar'); }
public/app.js:278: trailing whitespace.
+    if (el.dataset.close) { (el.dataset.close === 'dossier' ? dossierDialog : actionDialog).close(); return; }
public/app.js:279: trailing whitespace.
+    if (el.dataset.view) return navigate(el.dataset.view);
public/app.js:280: trailing whitespace.
+    if (el.dataset.importRetry) {
public/app.js:281: trailing whitespace.
+      importUI.busy = true; el.disabled = true;
public/app.js:282: trailing whitespace.
+      try { await request(`/api/import/jobs/${encodeURIComponent(el.dataset.importRetry)}/retry`, 'POST', {}); await loadImportJobs(); notify('Nieuwe poging geregistreerd. Volg de werkelijke taakstatus hieronder.'); }
public/app.js:283: trailing whitespace.
+      catch (error) { importUI.jobsError = error.message; throw error; }
public/app.js:284: trailing whitespace.
+      finally { importUI.busy = false; renderImportJobs(); }
public/app.js:285: trailing whitespace.
+      return;
public/app.js:286: trailing whitespace.
+    }
public/app.js:287: trailing whitespace.
+    if (el.dataset.importRollback) {
public/app.js:288: trailing whitespace.
+      openAction('Importsnapshot terugdraaien', `<form id="import-rollback-form" data-snapshot="${esc(el.dataset.importRollback)}"><p class="muted-copy">Snapshot <strong>${esc(el.dataset.importRollback)}</strong> wordt uit de actieve importcatalogus teruggenomen. Eerdere versies en de mutatiegeschiedenis blijven bewaard. Onderzoeksbesluiten worden niet stilzwijgend herschreven.</p><button type="submit" class="button primary">Bevestig rollback</button></form>`); return;
public/app.js:289: trailing whitespace.
+    }
public/app.js:290: trailing whitespace.
+    if (el.dataset.open) return showDossier(el.dataset.open);
public/app.js:291: trailing whitespace.
+    if (el.dataset.quality !== undefined) { filters.quality = el.dataset.quality; return render(); }
public/app.js:292: trailing whitespace.
+    if (el.dataset.region) { filters.region = el.dataset.region; filters.competition = ''; render(); document.querySelector('.candidates')?.scrollIntoView({ behavior: 'smooth', block: 'start' }); return; }
public/app.js:293: trailing whitespace.
+    if (el.dataset.competition) { filters = { ...filters, competition: el.dataset.competition, region: '', role: '', quality: '', search: '' }; return navigate('radar'); }
public/app.js:294: trailing whitespace.
+    if (el.dataset.save) {
public/app.js:295: trailing whitespace.
+      el.disabled = true; const id = el.dataset.save, removing = isShortlisted(id);
public/app.js:296: trailing whitespace.
+      await saveDecision(id, removing ? 'archive' : 'follow', removing ? 'other' : 'positive', removing ? 'Van shortlist gehaald; dit is geen negatief talentlabel.' : 'Opgeslagen als onderzoekskandidaat.');
public/app.js:297: trailing whitespace.
+      render(); if (dossierDialog.open) showDossier(id); notify(removing ? 'Van shortlist gehaald. Reden is vastgelegd.' : 'Opgeslagen op je shortlist.'); return;
public/app.js:298: trailing whitespace.
+    }
public/app.js:299: trailing whitespace.
+    if (el.dataset.task) {
public/app.js:300: trailing whitespace.
+      const task = state.tasks.find(t => t.id === el.dataset.task);
public/app.js:301: trailing whitespace.
+      openAction(task.status === 'done' ? 'Onderzoek heropenen' : 'Uitkomst vastleggen', `<form id="task-result-form" data-id="${esc(task.id)}" data-status="${task.status === 'done' ? 'todo' : 'done'}"><p class="muted-copy">${esc(task.question)}</p><label>Wat heb je waargenomen? Wat bleef onzeker?<textarea name="result" rows="5" maxlength="2400" ${task.status !== 'done' ? 'required' : ''}>${esc(task.result)}</textarea></label><button class="button primary" type="submit">${icon('check')} ${task.status === 'done' ? 'Heropenen' : 'Uitkomst opslaan'}</button></form>`); return;
public/app.js:302: trailing whitespace.
+    }
public/app.js:303: trailing whitespace.
+    const action = el.dataset.action;
public/app.js:304: trailing whitespace.
+    if (action === 'import-refresh') { await loadImportJobs(); await refreshImportCatalog(); return; }
public/app.js:305: trailing whitespace.
+    if (action === 'import-sample') { await downloadResponse('/api/import/sample', 'OmniScout-import-SYNTHETISCH.json', 'application/json'); return; }
public/app.js:306: trailing whitespace.
+    if (action === 'reset') { filters = { search: '', role: '', region: '', competition: '', quality: '', minAge: 18, maxAge: 35, newOnly: false, lowerOnly: false }; return navigate('radar'); }
public/app.js:307: trailing whitespace.
+    if (action === 'clear-competition') { filters.competition = ''; return render(); }
public/app.js:308: trailing whitespace.
+    if (action === 'apply-brief') { filters.role = state.brief.role; filters.minAge = state.brief.minAge; filters.maxAge = state.brief.maxAge; return navigate('radar'); }
public/app.js:309: trailing whitespace.
+    if (action === 'export') {
public/app.js:310: trailing whitespace.
+      const selectedFilters = { ...filters, shortlistOnly: view === 'shortlist' };
public/app.js:311: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath(`/api/export?${new URLSearchParams(selectedFilters)}`), `OmniScout-selectie-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.csv`, 'text/csv;charset=utf-8');
public/app.js:312: trailing whitespace.
+      else download('OmniScout-selectie-FICTIEF.csv', dossierCSV(findPlayers(catalog, selectedFilters, state)), 'text/csv;charset=utf-8');
public/app.js:313: trailing whitespace.
+      return notify('Selectie geëxporteerd met bron- en gegevensstatus.');
public/app.js:314: trailing whitespace.
+    }
public/app.js:315: trailing whitespace.
+    if (action === 'export-state') {
public/app.js:316: trailing whitespace.
+      if (backend) await downloadResponse(scopedPath('/api/export/state'), `OmniScout-logboek-${dataset === 'import' ? 'IMPORT' : 'FICTIEF'}.json`, 'application/json');
public/app.js:317: trailing whitespace.
+      else download('OmniScout-demologboek.json', JSON.stringify({ mode: 'synthetic_demo', exportedAt: new Date().toISOString(), ...state }, null, 2), 'application/json');
public/app.js:318: trailing whitespace.
+      return;
public/app.js:319: trailing whitespace.
+    }
public/app.js:320: trailing whitespace.
+    if (action === 'create-task') {
public/app.js:321: trailing whitespace.
+      const d = buildDossier(catalog.players.find(p => p.id === el.dataset.player), catalog);
public/app.js:322: trailing whitespace.
+      openAction('Gerichte onderzoeksopdracht', `<form id="create-task-form" data-player="${esc(d.player.id)}" data-request="${newId()}"><p class="muted-copy">${esc(d.player.name)} · ${esc(d.player.club)} · ${esc(playerLabel(d.player))}</p><label>Welke onzekerheid ga je onderzoeken?<textarea name="question" rows="5" required maxlength="1600">${esc(d.nextQuestion)}</textarea></label><p class="form-note">Alleen intern vastleggen. Deze toepassing verstuurt geen bericht en koopt geen beelden in.</p><button class="button primary" type="submit">${icon('plus')} Opdracht vastleggen</button></form>`); return;
public/app.js:323: trailing whitespace.
+    }
public/app.js:324: trailing whitespace.
+    if (action === 'decision') {
public/app.js:325: trailing whitespace.
+      openAction('Scoutbesluit vastleggen', `<form id="decision-form" data-player="${esc(el.dataset.player)}"><label>Vervolgactie<select name="action" required>${Object.entries(ACTION_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Reden<select name="reason" required>${Object.entries(REASON_LABELS).map(([k,v]) => `<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Onderbouwing<textarea name="note" required maxlength="2400" rows="4" placeholder="Wat ondersteunt dit besluit? Wat blijft onzeker?"></textarea></label><p class="form-note">Een budget- of rolafwijzing wordt niet omgezet in een label ‘slechte speler’.</p><button class="button primary" type="submit">${icon('check')} Besluit opslaan</button></form>`); return;
public/app.js:326: trailing whitespace.
+    }
public/app.js:327: trailing whitespace.
+    if (action === 'compare') {
public/app.js:328: trailing whitespace.
+      const ds = [...comparison].map(id => buildDossier(catalog.players.find(p => p.id === id), catalog));
public/app.js:329: trailing whitespace.
+      const rows = [['Competitie', d => d.competition.name], ['Rol', d => ROLE_LABELS[d.player.role]], ['Leeftijd', d => `${d.age} jaar`], ['Minuten', d => fmt(d.minutes, 0)], ['Rolmeting', d => `${d.roleMetric.label}: ${metric(d)}`], ['Bewijsroute', d => d.quality === 'documented' ? 'Meer bewijs' : 'Verkenning'], ['Ontbrekend', d => d.gaps[0]], ['Volgende vraag', d => d.nextQuestion]];
public/app.js:330: trailing whitespace.
+      openAction('Vergelijk bewijs, niet een talentscore', `<p class="form-note">${esc(datasetLabel())}. De statistieken zijn niet gecorrigeerd voor verschillen tussen competities of rollen.</p><div class="table-scroll"><table class="compare-table"><thead><tr><th>Onderdeel</th>${ds.map(d => `<th>${esc(d.player.name)}</th>`).join('')}</tr></thead><tbody>${rows.map(([label, get]) => `<tr><th>${label}</th>${ds.map(d => `<td>${esc(get(d))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`); return;
public/app.js:331: trailing whitespace.
+    }
public/app.js:332: trailing whitespace.
+  } catch (error) { notify(error.message || 'Actie mislukt.', true); } finally { if (el.isConnected) el.disabled = false; }
public/app.js:333: trailing whitespace.
+});
public/app.js:334: trailing whitespace.
+document.addEventListener('submit', async event => {
public/app.js:335: trailing whitespace.
+  const form = event.target; if (!['brief-form', 'create-task-form', 'task-result-form', 'decision-form', 'import-confirm-form', 'import-rollback-form'].includes(form.id)) return;
public/app.js:336: trailing whitespace.
+  event.preventDefault(); const submit = form.querySelector('[type="submit"]'); submit.disabled = true;
public/app.js:337: trailing whitespace.
+  const data = Object.fromEntries(new FormData(form));
public/app.js:338: trailing whitespace.
+  try {
public/app.js:339: trailing whitespace.
+    if (form.id === 'import-confirm-form') {
public/app.js:340: trailing whitespace.
+      if (importUI.busy || !importUI.preview?.valid || !form.querySelector('#import-confirm-check').checked) throw new Error('Controleer het bestand en bevestig de importverklaring eerst.');
public/app.js:341: trailing whitespace.
+      importUI.busy = true; importUI.error = '';
public/app.js:342: trailing whitespace.
+      try {
public/app.js:343: trailing whitespace.
+        const result = await request('/api/import/confirm', 'POST', { payload: importUI.payload, digest: importUI.preview.digest });
public/app.js:344: trailing whitespace.
+        importUI.preview = null; importUI.payload = null;
public/app.js:345: trailing whitespace.
+        importUI.jobs = [result.job, ...importUI.jobs.filter(job => job.id !== result.job.id)];
public/app.js:346: trailing whitespace.
+        await selectDataset('import'); await loadImportJobs();
public/app.js:347: trailing whitespace.
+        notify('Importopdracht geregistreerd. De taakstatus toont of verwerking is geslaagd.');
public/app.js:348: trailing whitespace.
+      } catch (error) { importUI.error = `${error.message} Kies het bestand opnieuw voor een actuele controle.`; importUI.preview = null; throw error; }
public/app.js:349: trailing whitespace.
+      finally { importUI.busy = false; if (view === 'import') renderImport(); }
public/app.js:350: trailing whitespace.
+      return;
public/app.js:351: trailing whitespace.
+    }
public/app.js:352: trailing whitespace.
+    if (form.id === 'import-rollback-form') {
public/app.js:353: trailing whitespace.
+      importUI.busy = true;
public/app.js:354: trailing whitespace.
+      try {
public/app.js:355: trailing whitespace.
+        await request('/api/import/rollback', 'POST', { snapshotId: form.dataset.snapshot });
public/app.js:356: trailing whitespace.
+        importUI.rolledBack.add(form.dataset.snapshot); importUI.preview = null;
public/app.js:357: trailing whitespace.
+        await refreshImportCatalog(); await loadImportJobs(); actionDialog.close();
public/app.js:358: trailing whitespace.
+        notify('Rollback bevestigd door de backend. Actieve importcatalogus bijgewerkt.');
public/app.js:359: trailing whitespace.
+      } finally { importUI.busy = false; if (view === 'import') renderImport(); }
public/app.js:360: trailing whitespace.
+      return;
public/app.js:361: trailing whitespace.
+    }
public/app.js:362: trailing whitespace.
+    if (form.id === 'brief-form') {
public/app.js:363: trailing whitespace.
+      const brief = { ...data, minAge: Number(data.minAge), maxAge: Number(data.maxAge) };
public/app.js:364: trailing whitespace.
+      if (brief.minAge > brief.maxAge) throw new Error('Minimumleeftijd mag niet hoger zijn dan maximumleeftijd.');
public/app.js:365: trailing whitespace.
+      if (backend) await request('/api/brief', 'PUT', brief); else { state.brief = brief; addAudit('brief.updated', 'local-brief'); persist(); }
public/app.js:366: trailing whitespace.
+      notify('Clubvraag opgeslagen. Positie en leeftijd kunnen op de radar worden toegepast.');
public/app.js:367: trailing whitespace.
+    }
public/app.js:368: trailing whitespace.
+    if (form.id === 'create-task-form') {
public/app.js:369: trailing whitespace.
+      if (!data.question.trim()) throw new Error('Vul een onderzoeksvraag in.');
public/app.js:370: trailing whitespace.
+      const payload = { playerId: form.dataset.player, question: data.question.trim(), requestId: form.dataset.request };
public/app.js:371: trailing whitespace.
+      if (backend) await request('/api/tasks', 'POST', payload);
public/app.js:372: trailing whitespace.
+      else if (!state.tasks.some(t => t.requestId === payload.requestId)) { const task = { id: newId(), ...payload, status: 'todo', result: '', at: new Date().toISOString(), completedAt: null }; state.tasks.push(task); addAudit('task.created', task.id, task.playerId); persist(); }
public/app.js:373: trailing whitespace.
+      actionDialog.close(); dossierDialog.close(); navigate('tasks'); notify('Onderzoeksopdracht vastgelegd. Er is niets extern verstuurd.');
public/app.js:374: trailing whitespace.
+    }
public/app.js:375: trailing whitespace.
+    if (form.id === 'task-result-form') {
public/app.js:376: trailing whitespace.
+      const payload = { status: form.dataset.status, result: data.result.trim() };
public/app.js:377: trailing whitespace.
+      if (payload.status === 'done' && !payload.result) throw new Error('Beschrijf eerst de waargenomen uitkomst.');
public/app.js:378: trailing whitespace.
+      if (backend) await request(`/api/tasks/${form.dataset.id}`, 'PATCH', payload);
public/app.js:379: trailing whitespace.
+      else { const task = state.tasks.find(t => t.id === form.dataset.id); Object.assign(task, payload, { completedAt: payload.status === 'done' ? new Date().toISOString() : null }); addAudit('task.updated', task.id, payload.status); persist(); }
public/app.js:380: trailing whitespace.
+      actionDialog.close(); render(); notify('Onderzoeksuitkomst opgeslagen.');
public/app.js:381: trailing whitespace.
+    }
public/app.js:382: trailing whitespace.
+    if (form.id === 'decision-form') {
public/app.js:383: trailing whitespace.
+      if (!data.note.trim()) throw new Error('Een geschreven onderbouwing is vereist.');
public/app.js:384: trailing whitespace.
+      await saveDecision(form.dataset.player, data.action, data.reason, data.note.trim()); actionDialog.close(); showDossier(form.dataset.player); render(); notify('Besluit en reden vastgelegd.');
public/app.js:385: trailing whitespace.
+    }
public/app.js:386: trailing whitespace.
+    updateCounts();
public/app.js:387: trailing whitespace.
+  } catch (error) { notify(error.message || 'Opslaan mislukt.', true); } finally { if (submit.isConnected) submit.disabled = false; }
public/app.js:388: trailing whitespace.
+});
public/app.js:389: trailing whitespace.
+for (const dialog of [dossierDialog, actionDialog]) dialog.addEventListener('click', event => { if (event.target === dialog) { const r = dialog.getBoundingClientRect(); if (event.clientX < r.left || event.clientX > r.right || event.clientY < r.top || event.clientY > r.bottom) dialog.close(); } });
public/app.js:390: trailing whitespace.
+document.querySelectorAll('[data-icon]').forEach(el => { el.innerHTML = icon(el.dataset.icon); });
public/app.js:391: trailing whitespace.
+render();
public/app.js:392: trailing whitespace.
+async function init() {
public/app.js:393: trailing whitespace.
+  if (!window.OMNI_INLINE && ['http:', 'https:'].includes(location.protocol)) {
public/app.js:394: trailing whitespace.
+    try {
public/app.js:395: trailing whitespace.
+      const session = await readJSON('/api/session');
public/app.js:396: trailing whitespace.
+      if (session.mode === 'local_single_user_demo' && session.csrf) { backend = session; await selectDataset(preferredDataset === 'import' && canImport() ? 'import' : 'demo', { remember: false }); }
public/app.js:397: trailing whitespace.
+    } catch (error) { if (backend) { workspaceError = error.message; notify(`Lokale gegevens konden niet worden geladen: ${error.message}`, true); } }
public/app.js:398: trailing whitespace.
+  }
public/app.js:399: trailing whitespace.
+  datasetLoading = false;
public/app.js:400: trailing whitespace.
+  if (preferredDataset === 'import' && !canImport()) notify('De bewaarde importkeuze vereist de lokale Node-app. Hier wordt de fictieve demo getoond.');
public/app.js:401: trailing whitespace.
+  render();
public/app.js:402: trailing whitespace.
+  updateCounts();
public/app.js:403: trailing whitespace.
+}
public/app.js:404: trailing whitespace.
+init();
